# Jarvis Working Memory

Last updated: 2026-06-15 02:02 CST

This is Codex's local project notepad for Jarvis. It is intentionally informal:
use it to preserve short-term goals, product principles, test prompts, and
implementation context that should survive interruptions.

## Current Focus

- Keep Jarvis moving toward a full speech-in/action-out assistant loop.
- Maintain a real product surface: fast responses, visible text, reliable audio,
  and a menu-bar Shut Up control that is always reachable when Jarvis can speak.
- Treat every user-facing workaround as temporary unless it is good enough for
  the final Jarvis experience.
- Always keep the current task stack explicit. If work goes down nested layers,
  write down the top-level goal, the current branch, the immediate subtask, the
  proof needed to finish it, and the parent task to return to afterward.

## Goal Stack Discipline

Jarvis work can easily become nested: product area, subsystem, model/tool, bug,
then a tiny implementation decision. Do not lose the thread.

Before or during substantial work, keep this shape in mind:

- Top-level goal: what Leo ultimately wants Jarvis to become or do.
- Current branch: UI, Logic, Models, Skills, Voice, Browser, Music, Calendar,
  Codex, Memory, Safety, or another concrete lane.
- Immediate subtask: the specific thing being fixed or built right now.
- Why this matters: how the subtask moves the top-level goal forward.
- Done proof: the exact evidence needed, such as a test, live screenshot,
  endpoint result, voice-loop transcript, app behavior, or verifier report.
- Return point: the parent task to resume after this subtask is done.

If the work enters a rabbit hole, pause briefly and restate the stack. Do not
declare success just because a nested subtask passed; climb back up and continue
from the parent objective.

## Current Live State

- Latest verified app version: Jarvis 0.1.419 build 419.
- Live app path: output/Jarvis.app.
- Only normal `output/Jarvis.app` should remain in active use. The temporary
  `output/Jarvis-LocalOSOnly.app` bundle was an emergency workaround and has
  been removed after folding the LocalOS-only music fix back into normal Jarvis.
- Menu-bar head is owned by the bundled `jarvis-status-helper`, not the main
  SwiftUI app. This was done because the main app's NSStatusItem was invisible
  or reported offscreen, while a tiny native helper rendered correctly.
- The packaged app is running from bundled resources, and the bundled
  `jarvis-status-helper` is running beside it so the Shut Up menu is reachable.

## Current Task Stack

- Top-level goal: Jarvis can complete Leo's real spoken task prompts and close
  the loop with visible status, clean final text, and safe tool execution.
- Current branch: Skills / Logic / Music, then overnight regression prompts.
- Immediate subtask: continue the regression prompt suite after rebuilding and
  verifying Jarvis 0.1.408 as the one normal app. Music playback must stay in
  LocalOS; Jarvis should not start hidden `afplay`/QuickTime playback for normal
  play requests.
- Why this matters: Leo wants one normal Jarvis app, honest LocalOS playback,
  and no mysterious background music source.
- Done proof for 0.1.408 consolidation: full `python3 -m unittest
  tests.test_safety` passed 593/593; live `/api/health` reports
  `output/Jarvis.app`, Jarvis 0.1.408 build 408; only `Jarvis.app` remains in
  `output/`; no `Jarvis-LocalOSOnly.app` duplicate remains.
- Done proof for 0.1.409 router pass: full `python3 -m unittest
  tests.test_safety` passed 595/595. Safe planner previews now route the Magic
  Keyboard yuan prompt to `commerce.price_convert` deterministically and keep
  `Ms. Sharpay` as `Ms Sharpay` instead of truncating it to `Ms`.
- Done proof for 0.1.410 commerce hardening: `commerce.price_convert` now keeps
  the original open exchange-rate API and falls back to a jsDelivr-hosted USD
  currency file if the primary rate source fails. Focused commerce tests passed,
  and a live Magic Keyboard conversion returned about 671 yuan in 5.8s. Full
  `python3 -m unittest tests.test_safety` passed 596/596; no-prompt verifier
  passed 12/12 at `runtime/verification_no_prompt/verify-no-prompt-20260614-225331.json`;
  corrected silent speech-audit regression matrix passed 7/7 at
  `runtime/regression_prompt_matrix/20260614-225327/summary.json`.
- Done proof for 0.1.411 Codex chat route: requests such as "send a prompt
  called test in the Default chat" now choose `codex.chat_plan`, select Default,
  hide session IDs, and require typed confirmation instead of falling into a
  generic safety wall. Focused Codex routing tests passed 4/4.
- Done proof for 0.1.412 confirmation speech: confirmation-gated final replies
  are no longer blanket-silenced. Jarvis can say the safe final sentence, such
  as "I would use the Default Codex chat, but I need confirmation before sending
  anything," while still requiring approval and not sending anything. Focused
  server speech tests passed 5/5; full `python3 -m unittest tests.test_safety`
  passed 600/600; no-prompt verifier passed 12/12 at
  `runtime/verification_no_prompt/verify-no-prompt-20260614-231454.json`; live
  silent speech audit passed at `runtime/voice_loop_qa/20260614-231448/report.json`
  with 2 speech payloads and 0 leaks.
- Done proof for 0.1.415 regression matrix: live bundled Jarvis 0.1.415 build
  415 passed the full eight-prompt speech-audit matrix 8/8 at
  `runtime/regression_prompt_matrix/20260614-235525/summary.json`. This run
  verified Teams bookmark routing, LocalOS music routing, real RAM readout,
  Default Codex chat confirmation, Calendar schedule with duplicate Flag Day
  collapsed, safe Gemma 3 4B MacBook Air/local fallback planning, Ms. Sharpay
  contact-confirmation wording with the honorific preserved, and Magic Keyboard
  USD-to-yuan conversion. Full `python3 -m unittest tests.test_safety` passed
  604/604; no-prompt verifier passed 12/12 at
  `runtime/verification_no_prompt/verify-no-prompt-20260614-235444.json`.
- 0.1.415 specifics: Calendar all-day duplicate events now collapse even when
  duplicate holiday calendars disagree on location; public product price pages
  get a retry before Jarvis gives up; LocalOS alias replies use Leo's `LocalOS`
  spelling; email sender entities restore honorifics from the original prompt
  when the first model drops them; and actual email inspection requests recover
  through local email metadata if the first model fails without choosing a tool.
- Done proof for 0.1.416 honesty/voice-memory pass: live bundled Jarvis 0.1.416
  build 416 passed full `python3 -m unittest tests.test_safety` 606/606,
  no-prompt verification 12/12 at
  `runtime/verification_no_prompt/verify-no-prompt-20260615-004114.json`, and
  the eight-prompt speech-audit matrix 8/8 at
  `runtime/regression_prompt_matrix/20260615-004209/summary.json`. The runtime
  hygiene check showed one `jarvis-menu-bar`, one `jarvis-status-helper`, one
  bundled worker, only `output/Jarvis.app` in active output, and no `afplay`.
- 0.1.416 specifics: Teams handoff now states that Jarvis opens signed-in
  Chrome but has not inspected the assignment until a later page/screen read
  succeeds; Teams page-read failures now use product language instead of raw
  JavaScript/AppleScript internals; contact aliases preserve Leo's human label
  such as `Ms Sharpay` while tolerating `Sharpay` and dictated `his Sharpay`;
  and the wake/overlay plan uses visible `Listening.` instead of a routine
  spoken `Hello sir` greeting so Leo can keep speaking immediately.
- Done proof for verifier private-port fix: full `python3 scripts/verify_safe.py`
  passed 100/100 at `runtime/verification/verify-safe-20260615-010549.json`.
  The temporary bundle self-test now uses a private local port instead of
  fighting the live Jarvis worker on `127.0.0.1:8765`.
- Done proof for 0.1.417 native visible-screen read: live bundled Jarvis
  0.1.417 build 417 passed full `python3 -m unittest tests.test_safety`
  612/612, Swift routing self-test, no-prompt verification 12/12 at
  `runtime/verification_no_prompt/verify-no-prompt-20260615-013054.json`,
  the eight-prompt live regression matrix 8/8 at
  `runtime/regression_prompt_matrix/20260615-013149/summary.json`, and full
  `python3 scripts/verify_safe.py` 100/100 at
  `runtime/verification/verify-safe-20260615-013807.json`.
- 0.1.417 specifics: `screen.visible_text` is now a concrete native route.
  Explicit commands such as `read the visible Teams screen` or `what is on
  this page` use the macOS app's Apple Vision OCR path, send extracted text
  only to the local worker, scan it as untrusted content, omit private digest
  text from audit logs, and do not store screenshots by default. Normal
  `teams.assignment` still opens signed-in Chrome first and remains honest
  until a visible read succeeds.
- Done proof for 0.1.418 Teams handoff plus OCR follow-up: live bundled Jarvis
  0.1.418 build 418 passed full `python3 -m unittest tests.test_safety`
  612/612, Swift routing self-test, no-prompt verification 12/12 at
  `runtime/verification_no_prompt/verify-no-prompt-20260615-014800.json`,
  runtime hygiene with one app/helper/worker and no `afplay`, and full
  `python3 scripts/verify_safe.py` 100/100 at
  `runtime/verification/verify-safe-20260615-015136.json`.
- 0.1.418 specifics: after a `teams.assignment` command opens signed-in
  Chrome, Jarvis now automatically attempts a read-only native visible Teams
  OCR pass for assignment-reading requests such as "look in Teams for my
  newest Music assignment"; it does not trigger for submit/turn-in/send/delete
  wording, and it still treats OCR text as untrusted private local data.
- Done proof for 0.1.419 Teams assignment OCR digest: live bundled Jarvis
  0.1.419 build 419 passed full `python3 -m unittest tests.test_safety`
  613/613, no-prompt verification 12/12 at
  `runtime/verification_no_prompt/verify-no-prompt-20260615-015853.json`,
  runtime hygiene with one app/helper/worker and no `afplay`, a muted live
  `/api/screen/visible-text` probe that extracted assignment-related lines,
  and full `python3 scripts/verify_safe.py` 100/100 at
  `runtime/verification/verify-safe-20260615-020222.json`.
- 0.1.419 specifics: visible Teams OCR now prefers assignment-relevant lines
  such as class, assignment title, due time, instructions, and rubric over
  generic Teams navigation text. Audit redaction now also omits
  `assignment_digest_items`, after a focused test caught that private digest
  content was still being logged.
- Done proof for completed price lane: first model selected
  `commerce.price_convert`; live bundled app returned
  "Magic Keyboard (USB-C) - US English is $99.00..." with about 671 yuan; full
  `tests.test_safety` passed 572/572; `verify_safe.py` passed 98/98 at
  `runtime/verification/verify-safe-20260614-112440.json`.
- Return point: continue the larger regression prompt suite, especially browser
  task execution, Codex chat routing, final speech/action verification, and
  real app-control/browser follow-through.
- Done proof for this pass: full `python -m unittest tests/test_safety.py`
  passed 579/579; `python scripts/verify_safe.py` passed 99/99 at
  `runtime/verification/verify-safe-20260614-125612.json`; live bundled app
  health reports Jarvis 0.1.389 build 389 from bundled app resources; speech is
  muted; `jarvis-status-helper` is running.
- Live 0.1.387 probe for `Play Waving Through a Window` exposed the next real
  blocker: macOS denied direct Chrome JavaScript automation (`Access not
  allowed`, -1723). Jarvis 0.1.388 adds a fallback that opens the LocalOS Music
  Player in Chrome through LaunchServices, waits for its normal polling bridge,
  then queues playback through the bridge.
- Leo saw multiple LocalOS music tabs during testing. Jarvis 0.1.389 adds a
  player-open cooldown marker plus a recent-heartbeat guard, so a stale-but-
  recently-seen LocalOS page does not trigger another Chrome tab.
- Quiet live check for `Play Waving Through a Window.` with speech/audio
  suppressed did not open or play audio. It selected the existing Dear Evan
  Hansen Tony Awards file through the Waving alias; this still needs a real
  audible LocalOS confirmation later, preferably without annoying Leo.
- Leo explicitly asked to test the same music prompt live while listening and
  to enable the LocalOS bridge/direct-control path in the next version. Bridge
  status before the 0.1.390 test was live: v2, polling active, current track
  Dear Evan Hansen | 2017 Tony Awards, playing true.
- Live 0.1.390 audible-path test sent `Play Waving Through a Window.` through
  `/api/command` with Jarvis speech suppressed and audio actions enabled.
  LocalOS bridge command `music-edaccbcef3c5` reported `playing`; follow-up
  snapshots showed `playing: true`, current track `Dear Evan Hansen | 2017 Tony
  Awards`, artist `Matt Hagmeier Curtis`, and no bridge error. After that,
  0.1.391 changes the tool/planner status from `queued` to `playing` when the
  bridge confirms actual playback.
- Live 0.1.391 check returned `status: playing`, `playback_confirmation:
  playing`, and summary `Started Local OS Music playback.` for the same prompt.
  A later snapshot reflected a newer user music request for `Justin Bieber -
  Beauty And A Beat`, proving the bridge continues accepting live commands.
- Leo approved a primitive music fast path: for `play ...` requests, Jarvis may
  directly extract the song phrase, search LocalOS library metadata, and queue
  the chosen song without a model deciding the tool. 0.1.392 adds tiny dictation
  tolerance such as `beauty and the beast` matching `Beauty And A Beat`.
- LocalOS bridge v3 adds a muted-autoplay retry for Jarvis-originated commands
  because Chrome sometimes rejects `audio.play()` with "user didn't interact
  with the document first" after the page is refreshed or loses activation.
- Live 0.1.392 test: `play me beauty and the beast` matched and played
  `Justin Bieber - Beauty And A Beat` through LocalOS bridge v3. `play for
  forever` then found the right track but the bridge picked it up after Jarvis's
  short confirmation timeout, so 0.1.393 treats delayed polling on an already
  live bridge as `queued` instead of a false failure.
- Live 0.1.393 test: `play natural` found the right song but Chrome still
  rejected LocalOS page audio with the user-interaction/autoplay error. 0.1.394
  adds a primitive native fallback: when the LocalOS page finds the track but
  Chrome blocks audio, Jarvis plays the exact LocalOS MP3 file with `afplay`.
- Live 0.1.394 test exposed a race: LocalOS briefly returned `playing` for
  `Natural`, then flipped to the Chrome user-interaction failure. 0.1.395 waits
  for a short stable-playing window before claiming success, so the native
  fallback can catch that delayed failure.
- Live 0.1.395 proved the native fallback can play the exact LocalOS MP3, but
  it also exposed a serious ownership bug: `afplay` could keep playing after
  Jarvis and Chrome/LocalOS tabs were closed. 0.1.396 tracks the fallback PID,
  cleans it on server startup/exit, and adds `localos.music_stop` for
  Jarvis-owned fallback music only. Do not leave background audio unowned again.
- Live 0.1.396 health reported bundled app resources, version 0.1.396 build
  396. A no-audio live `stop the music` command routed to `localos.music_stop`
  and returned idle without speaking. Full `python -m unittest
  tests/test_safety.py` passed 587/587.
- 0.1.397 fixes the wrong LocalOS reconnect contract. If Jarvis finds a song
  but the LocalOS Music bridge is unknown, stale, or not polling, Jarvis should
  try to open/reconnect the LocalOS Music Player automatically. A stale
  recently-seen snapshot is no longer a reason to strand Leo with "refresh it
  yourself." Keep duplicate-tab prevention secondary to actually completing
  playback.
- 0.1.398 fixes duplicate Jarvis Dock/menu-bar icons. The launcher must use
  normal `open`, never `open -n`, because `open -n` creates a second app
  instance. The visible menu-bar head belongs to `jarvis-status-helper`; the
  main app status item is off by default and only re-enabled with
  `JARVIS_SHOW_MAIN_STATUS_ITEM=yes` for debugging. Live verification after
  `build_and_launch_app.sh`: exactly one `jarvis-menu-bar`, one
  `jarvis-status-helper`, and one `run_dashboard.py` worker.

## Regression Prompts Leo Expects Jarvis To Handle

- "Look in Teams for my newest Music assignment and ask me a list of questions
  to answer so that you have enough information to finish the assignment."
- "Play Waving Through a Window."
- "Check in Activity Monitor how much RAM my computer is using."
- "Open Codex and send a prompt called 'test' in the Default chat."
- "Check my calendar for my schedule today."
- "Test the Gemma 3 4B model for me."
- "Summarize all the emails from Ms. Sharpay in the past month."
- "Jarvis, could you search up the price of the Magic Keyboard and tell me its
  price converted to yuan?"

## Notes On Expected Behavior

- Teams task: use browser/bookmarks and logged-in Chrome when needed; do not
  pretend WebKit inherited Chrome cookies.
- Music task: LocalOS should control/play the Music app/player. Jarvis should
  only claim playback after confirmation. `accepted` is only an internal status;
  after the evidence window closes without actual audio, Jarvis should say
  LocalOS did not start the audio. If LocalOS is not connected, Jarvis should
  attempt to open/reconnect the music player automatically before asking Leo to
  do anything.
- RAM task: read real system memory state, ideally through a reliable native or
  terminal-backed tool.
- Codex task: choose the correct Codex chat, defaulting weird/general prompts to
  the Default chat, and mark prompts as Jarvis-generated.
- Calendar task: use local Calendar cache when permissions allow it; explain
  permission problems clearly.
- Model test task: avoid heavy local runs on Leo's 16 GB MacBook Pro when a
  cloud or MacBook Air route is safer.
- Ms. Sharpay task: infer contact aliases from local sender metadata and store
  useful contact mappings without leaking private content.
- Magic Keyboard task: `commerce.price_convert` now uses Apple's official U.S.
  Magic Keyboard (USB-C) product page plus a live USD-CNY public exchange-rate
  source. Keep replies short and spoken-friendly; source URLs stay in JSON.

## Product Principles From Leo

- No fake keyword-only skill demos. Let the model choose tools from clear tool
  definitions and keep tool calls hidden from spoken output.
- If Jarvis starts doing work, it should visibly and audibly acknowledge the
  task naturally, then return with the result.
- Spoken output must be clean English unless a specific non-English phrase is
  necessary.
- Never speak internal model/tool/debug details to Leo.
- If Jarvis is speaking, a visible menu-bar head with Shut Up must be available.
- Use tests for every bug Leo catches, especially speech mismatch, music false
  positives, and UI/menu-bar regressions.
