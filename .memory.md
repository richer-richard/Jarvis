# Jarvis Working Memory

Last updated: 2026-06-20 02:25 CST

This is Codex's local project notepad for Jarvis. It is intentionally informal:
use it to preserve short-term goals, product principles, test prompts, and
implementation context that should survive interruptions.

## Bug Memory

- Read `JARVIS_BUG_BACKLOG.md` before substantial Jarvis work. It records the
  bug classes Leo has already identified in chat: email summaries, fake keyword
  routing, speech/mute failures, menu-bar visibility, Hey Jarvis instability,
  LocalOS music ownership/playback, Chrome/Teams blockers, Codex routing, report
  timing, and the canonical real-world test prompts.

## Current Focus

- Keep Jarvis moving toward a full speech-in/action-out assistant loop.
- Overnight target for this run: keep working until Leo wakes, run a real
  `date` command at each prompt/checkpoint, keep the full-loop matrix green, and
  prepare a concise HTML report by the morning deadline.
- Before the morning report, close the Chrome tabs created by Codex, Jarvis, or
  the overnight tests. Do not close Leo's pre-existing personal tabs; identify
  obvious test/LocalOS/Teams/regression tabs first and use Chrome control
  carefully.
- Chrome cleanup inventory at 2026-06-17 23:42 CST: Chrome had two open tabs
  visible to Chrome control: a `chrome://newtab/` tab and a YouTube tab titled
  "We're 99.9% sure this pattern is true, but no one can prove it". Treat the
  YouTube tab as Leo's unless later evidence proves Codex/Jarvis opened it.
- 2026-06-19 00:38 CST checkpoint: live Jarvis is 0.1.454 build 454. Proof is
  current: `scripts/full_loop_regression.py --case all --no-report-refresh`
  passed 8/8 at `runtime/full_loop_regression/20260619-003336/summary.json`, and
  full `python3 -m unittest tests.test_safety` passed 854/854. The 8 full-loop
  cases are Music playback, RAM, Calendar, Magic Keyboard yuan conversion,
  Gemma 3 4B model planning, Codex Default-chat safety, Teams assignment
  honesty, and Ms. Sharpay email/contact proof.
- The Teams case is intentionally an honesty gate: it passes if Jarvis actually
  reads Music assignment content, clearly says the visible assignment is not
  Music, clearly says Chrome permission blocks reading, or clearly says it has
  not inspected the assignment yet. It fails generic Chrome/YouTube summaries
  and wrong-subject false success.
- The Ms. Sharpay case proves the local contact alias and Apple Mail sender
  filter through private metadata. The spoken summary may omit "Sharpay"; the
  proof must verify the sender filter rather than relying only on visible words.
- 2026-06-19 03:26 CST checkpoint: live Jarvis remains 0.1.454 build 454.
  Saved commits:
  `b795615 Fix sender email summary selection`,
  `8f01b35 Require music cleanup proof in full loop`, and
  `1d29fbe Compare voice QA against spoken payloads`.
  Full `python3 -m unittest tests.test_safety` passed 876/876, live
  `scripts/full_loop_regression.py --case all` passed 8/8 at
  `runtime/full_loop_regression/20260619-031014/summary.json`, and official
  `scripts/pre_build_gate.py --base-url http://127.0.0.1:8765` passed 4/4 at
  `runtime/pre_build_gate/20260619-032252/summary.json`.
- Sharpay email fix: prompts like "Summarize all the emails from Ms. Sharpay in
  the past month" now preserve the original all-matching intent over model
  `latest` shortcuts, scan a bounded 75 recent Mail items, select
  `sender_recent`, and summarize multiple recent matching messages in
  English-first voice-friendly wording.
- Music proof fix: the full-loop music case now verifies that the native Music
  app bridge stopped playback after the test. A pass can no longer hide Music
  still playing after cleanup.
- Voice QA fix: when visible text and spoken text intentionally differ, the
  voice-loop report scores the STT transcript against the spoken payload and
  records `reply_similarity_target`. This matters for cases like visible
  `Juneteenth` versus spoken `June nineteenth holiday`.
- Maintain a real product surface: fast responses, visible text, reliable audio,
  and a menu-bar Shut Up control that is always reachable when Jarvis can speak.
- Treat every user-facing workaround as temporary unless it is good enough for
  the final Jarvis experience.
- Progress updates to Leo during active work must always include, in bold:
  what task is being worked on, what it affects from the user's side, and
  progress percentage for that task. Only when a task has actually reached 100
  percent should the update add a short `End of:` line describing what that
  finished task now brings to Leo as a Jarvis user.
- Always keep the current task stack explicit. If work goes down nested layers,
  write down the top-level goal, the current branch, the immediate subtask, the
  proof needed to finish it, and the parent task to return to afterward.
- 2026-06-19 18:00 CST lane: hardening the "no fake keyword routing" rule.
  Jarvis may keep the direct named music primitive because Leo explicitly
  approved it, but it must be labeled/tested as an exception. The first-model
  prompt should tell the model to choose tools from intent, constraints,
  history, and tool descriptions, not keyword spotting.
- 2026-06-19 18:08 CST lane: hardening TTS leak firewall. Found sanitizer gaps:
  inline `Tool time`, inline Groq/model timing, and raw JSON `selected_tool`
  payloads could survive into spoken text. Server speech selection should return
  sanitized text, not the original reply.
- 2026-06-19 18:17 CST lane: menu-bar head reliability. Remove text fallback
  status-item titles (`J`/test-letter style); packaged resources must provide
  the colored head, and helper self-test should reject text fallback regressions.
- 2026-06-19 18:22 CST lane: offline model fallback. Keep model testing
  cloud/remote-first, but expose a plan-only local fallback policy: default to
  `gemma3n:e4b`, treat Qwen 0.6B as tiny/simple only, block heavy local
  `gpt-oss:20b`/DeepSeek-class runs, and require confirmation before any local
  model load on Leo's 16 GB Mac.
- 2026-06-19 18:28 CST lane: future computer-control safety. Before enabling
  real UI/browser control, planned `ui.automation` and `screen.ocr` must expose
  permission gates and a destructive-action block list; they remain non-executing
  and must not capture screens or mutate apps.
- 2026-06-20 00:32 CST checkpoint: live Jarvis is still 0.1.468 build 468.
  Stop-speaking route honesty was patched so direct and model-selected
  `voice.stop_speaking` summaries use the real tool reply. Live proof:
  `/api/command` with `stop talking` and `suppress_speech=true` returned
  summary `I was not speaking.`, result status `idle`, no speech payload, and no
  `say`/`afplay`/Piper process. Full `python3 -m unittest tests.test_safety`
  passed `935/935`; `python3 scripts/verify_safe.py` passed `105/105` at
  `runtime/verification/verify-safe-20260620-002805.json`; the rebuilt
  `output/Jarvis.app` launched from bundled resources with
  `worker_launch_matches_bundle: true`.
- 2026-06-20 00:39 CST checkpoint: added `scripts/probe_stop_speaking.py` and
  wired it into `scripts/pre_build_gate.py` as `stop_speaking_probe` with a
  `suppressed_for_stop_speaking` proof contract. Live probe passed, fast gate
  passed `3/3`, real pre-build gate passed `4/4` at
  `runtime/pre_build_gate/20260620-003454/summary.json`, full
  `tests.test_safety` passed `937/937`, and `scripts/verify_safe.py` passed
  `105/105` at `runtime/verification/verify-safe-20260620-003852.json`.
- 2026-06-20 00:44 CST checkpoint: `scripts/morning_status.py` now reports the
  latest pre-build gate result and step ids, including `stop_speaking_probe`.
  Live status printed `Latest pre-build gate: passed 4/4`; full
  `tests.test_safety` passed `938/938`.
- 2026-06-20 00:48 CST checkpoint: `scripts/morning_status.py` now also reports
  latest conversation-context smoke and wake-threshold proof. Live status prints
  `Latest context smoke: passed` and `Latest wake threshold: passed 10/10`;
  full `tests.test_safety` passed `940/940`.
- 2026-06-20 00:56 CST checkpoint: Hey Jarvis start is now idempotent. If
  `start()` is called while `shouldKeepRunning` is already true, the wake
  listener publishes the existing state and returns instead of starting another
  permission/session cycle. Swift build passed, `jarvis-menu-bar --self-test`
  passed, full `tests.test_safety` passed `940/940`, the rebuilt
  `output/Jarvis.app` relaunched with bundled worker proof, and
  `scripts/verify_safe.py` passed `105/105` at
  `runtime/verification/verify-safe-20260620-005458.json`.
- 2026-06-20 01:09 CST checkpoint: bumped the canonical bundle defaults and
  shipped-highlight report copy to Jarvis 0.1.469 build 469. Live
  `build_and_launch_app.sh` reported `Jarvis 0.1.469 build 469 is online and
  ready`, full `tests.test_safety` passed `940/940`, `scripts/verify_safe.py`
  passed `105/105` at `runtime/verification/verify-safe-20260620-010714.json`,
  and the 0.1.469 pre-build gate passed `4/4` at
  `runtime/pre_build_gate/20260620-010726/summary.json`.
- 2026-06-20 02:14 CST checkpoint: live Jarvis is 0.1.471 build 471. The
  report/workboard paths are now visible in both `scripts/morning_status.py` and
  backend `system.status` JSON while the spoken status reply stays short.
  Music full-loop proof is stricter: the verifier requires `currentTime >= 0.5s`
  before accepting playback, and live proof passed with playback around 4.0s
  before cleanup stopped it. Current proof: `scripts/verify_safe.py` passed
  `105/105` at `runtime/verification/verify-safe-20260620-020440.json`, full
  `tests.test_safety` passed `944/944`, and the current release gate passed
  `5/5` at `runtime/pre_build_gate/20260620-021401/summary.json`.
- 2026-06-20 02:24 CST checkpoint: live Jarvis is 0.1.472 build 472. The
  menu-bar colored-head path now fails closed: `build_app_bundle.sh` refuses to
  produce a bundle without `JarvisMenuHead.png`, and bundled
  `jarvis-status-helper --self-test` verifies the image loads so the helper
  cannot silently create an invisible no-text status item. Proof:
  `scripts/verify_safe.py` passed `105/105` at
  `runtime/verification/verify-safe-20260620-022400.json`, and full
  `tests.test_safety` passed `944/944`. After committing the menu-head guard,
  the current release gate passed `5/5` at
  `runtime/pre_build_gate/20260620-022537/summary.json`.

## Goal Stack Discipline

Jarvis work can easily become nested: product area, subsystem, model/tool, bug,
then a tiny implementation decision. Do not lose the thread.

Before or during substantial work, keep this shape in mind:

- Top-level goal: what Leo ultimately wants Jarvis to become or do.
- Current branch: UI, Logic, Models, Skills, Voice, Browser, Music, Calendar,
  Codex, Memory, Safety, or another concrete lane.
- Immediate subtask: the specific thing being fixed or built right now.
- Why this matters: how the subtask moves the top-level goal forward.
- Done proof: the exact evidence needed, such as a test, live screenshot,
  endpoint result, voice-loop transcript, app behavior, or verifier report.
- Return point: the parent task to resume after this subtask is done.

If the work enters a rabbit hole, pause briefly and restate the stack. Do not
declare success just because a nested subtask passed; climb back up and continue
from the parent objective.

## Current Live State

- Latest verified app version: Jarvis 0.1.444 build 444 is live.
- Jarvis 0.1.444 LocalOS music reconnect patch: when the LocalOS music bridge
  is stale, Jarvis now tries the native Local OS Host app first and only falls
  back to opening the Chrome/file music player if the host does not publish a
  live bridge. This is meant to reduce duplicate Chrome music tabs and make
  LocalOS itself the first playback owner. A live `/api/plan` for `play Waving
  Through a Window` still routes to `localos.music_play` without executing audio
  during overnight proof.
- Latest safe verifier: `runtime/verification/verify-safe-20260618-001149.json`
  passed 105/105 against live Jarvis 0.1.444.
- Current source-side music fix: LocalOS playback now has a user-activation
  recovery path. If the LocalOS page accepts/selects a track but audio is not
  playing, Jarvis focuses the real LocalOS play button in Chrome, sends a guarded
  Space keypress only when that button is focused, then rechecks playback before
  claiming success. Focused tests cover both the direct Chrome helper and the
  normal LocalOS polling bridge.
- Current stop-music fix: ordinary Stop Music no longer falls back to muting the
  whole Mac when LocalOS is stale or Chrome pause automation fails. It only
  pauses known LocalOS/browser/Music surfaces and reports if the stop command is
  merely queued.
- Current live LocalOS blocker: Chrome returns `Access not allowed (-1723)` for
  JavaScript-from-Apple-Events control when Jarvis tries to direct-control the
  LocalOS music page, and the LocalOS heartbeat snapshot is stale. Jarvis now
  reports this distinctly instead of hiding it behind a vague reconnect message.
  Browser and music guidance now tells Leo to check both macOS Automation access
  and Chrome > View > Developer > Allow JavaScript from Apple Events.
- Latest regression matrix proof: `runtime/regression_prompt_matrix/20260617-233458/summary.json`
  passed 8/8 with 0 speech leaks. The Teams assignment case now keeps the useful
  assignment-focused visible-screen/browser follow-up instead of falling through
  to noisy Chrome menu OCR.
- Report proof-surface patch: `scripts/render_overnight_status.py` now pulls
  `runtime/regression_prompt_matrix/latest.json` into the master report proof
  list, so the HTML explicitly says the latest eight-prompt matrix passed 8/8
  with 16 speech payloads, 0 speech leaks, and max first visible 1.845s. Targeted
  report tests passed and the full Python suite passed 809/809 at 23:53 CST.
- Live app path: output/Jarvis.app.
- Only normal `output/Jarvis.app` should remain in active use. The temporary
  `output/Jarvis-LocalOSOnly.app` bundle was an emergency workaround and has
  been removed after folding the LocalOS-only music fix back into normal Jarvis.
- Menu-bar head is owned by the bundled `jarvis-status-helper`, not the main
  SwiftUI app. This was done because the main app's NSStatusItem was invisible
  or reported offscreen, while a tiny native helper rendered correctly.
- The packaged app is running from bundled resources, and the bundled
  `jarvis-status-helper` is running beside it so the Shut Up menu is reachable.
- The overnight report/workboard now surface `scripts/report_refresh.py` as the
  standalone helper that can rerender both pages from the loopback worker.
- The readiness footer now names pending permission labels, so the `App perms`
  chip no longer hides which items are still pending behind a bare count.
- The live Jarvis panel now opens at a large enough default size for the full
  debug surface: the identity header is visible again in the bundled app, and
  the lowest permission tiles are no longer clipped off the bottom edge.
- Latest closed-loop speech proof: `python3 scripts/voice_loop_qa.py --command
  'Hey Jarvis, status.' --stt-provider local --no-permission-prompts` routed
  the spoken command to `status` and transcribed the spoken reply back as
  `Jarvis 0.1.439, build 439 is online and ready.` with similarity 1.0.
- Latest live-speech-path proof: `python3 scripts/voice_loop_qa.py --command
  'Hello Jarvis.' --stt-provider local --no-permission-prompts
  --exercise-live-speech` reported `Speech mode: live_playback_exercised` plus
  `Live playback requested: True | Active observed: True`, while still matching
  the spoken reply transcript with similarity 1.0.
- Latest source-server calendar speech proof: current source on
  `http://127.0.0.1:8766` now gives Calendar replies a separate
  `spoken_summary`, and `python3 scripts/voice_loop_qa.py --command 'Check my
  calendar for my schedule today.' --base-url http://127.0.0.1:8766
  --stt-provider local --no-permission-prompts --exercise-live-speech`
  completed with `Similarity: 1.0` and `Live playback requested: True | Active
  observed: True`. The older bundled worker on `127.0.0.1:8765` was still
  running pre-patch code during this check, so the live app will need a rebuild
  before that exact improvement appears there.
- Latest live-bundle calendar speech proof: after rebuilding `output/Jarvis.app`
  back onto `127.0.0.1:8765`, a suppressed `/api/command` check showed the live
  worker emitting the new calendar `spoken_summary`, and then
  `python3 scripts/voice_loop_qa.py --command 'Check my calendar for my
  schedule today.' --base-url http://127.0.0.1:8765 --stt-provider local
  --no-permission-prompts --exercise-live-speech` completed with
  `Similarity: 1.0` plus `Live playback requested: True | Active observed:
  True`.
- Latest post-relaunch verification sweep: `python3 scripts/verify_no_prompt.py`
  passed `12/12`, `python3 scripts/run_regression_prompt_matrix.py
  --base-url http://127.0.0.1:8765 --stt-provider local
  --no-permission-prompts` passed `8/8`, full `python3 scripts/verify_safe.py`
  now passes `104/104`, and the cold-start verifier flake was fixed by giving
  Swift `readiness()` / `preflight()` a dedicated 20-second timeout plus a more
  honest `verify_safe.run_command` failure summary.
- Latest browser-permission truthfulness proof: the Swift app now preflights
  Chrome Automation before the Teams browser-read follow-up. When macOS already
  says Jarvis needs manual Automation access, the app can stop immediately and
  tell Leo `macOS is blocking Jarvis from controlling Google Chrome. Grant
  Jarvis Automation access to Chrome, then try again.` instead of waiting for a
  doomed browser read plus OCR fallback.
- Latest harness-alignment proof: the worker-side speech-audit matrix still
  fails the `teams_assignment` row honestly because Chrome Automation remains
  blocked in the environment, but the harness now stops after the definitive
  `browser_permission_blocked` result instead of wasting one more OCR attempt.
  The targeted row improved from about `23.226s` at
  `runtime/regression_prompt_matrix/20260617-030714/summary.json` to about
  `19.58s` at `runtime/regression_prompt_matrix/20260617-031214/summary.json`,
  with the same final visible reply and no speech leaks.

## Current Task Stack

- Top-level goal: Jarvis can complete Leo's real spoken task prompts and close
  the loop with visible status, clean final text, and safe tool execution.
- Current branch: Models / Codex / Verification, after the 0.1.439 report
  freshness and speech-truthfulness passes, plus the 0.1.438 proof-surface
  refresh helper, 0.1.432 Codex speed status visibility patch, 0.1.431 Codex
  CLI proxy speed patch, 0.1.430 sound-loop/status pass, 0.1.429 command
  payload hardening, and the 0.1.428 LocalOS music wording polish.
- Immediate subtask: keep moving on non-music capabilities while preserving
  the speed proof and product contracts. The live panel/window sizing is back
  in good shape, so the next active lane is stronger voice-loop verification
  and then the remaining browser/tool/calendar/email reliability work. Music
  app implementation is intentionally handed off to another AI from the
  generated prompt; do not resume LocalOS music hacks unless Leo explicitly
  brings that lane back.
- Updated immediate subtask: continue tightening the browser-backed task lane,
  especially the gap between worker-only overnight regression harnesses and the
  real Swift app follow-up behavior. The app-side Chrome Automation preflight
  is in; next likely work is deciding how nightly reports should classify
  environment-blocked browser rows versus true product regressions.
- Return point after the latest subtask: the report/workboard lane is in good
  shape, and source-side calendar speech QA is now fixed. The next decision is
  whether to do a live bundle rebuild/relaunch to move that calendar speech fix
  onto `127.0.0.1:8765`, or keep pushing another non-disruptive source-side
  reliability lane first.
- Updated return point: the calendar speech lane and the cold-start verifier
  lane are both green in the live bundle now. Next highest-value work should
  probably target either response-speed measurement/profiling or another real
  spoken-task lane beyond calendar that still lacks the same level of live proof.
- Why this matters: Leo wants Jarvis to handle real spoken prompts end to end,
  not only route them plausibly. Each capability needs a visible answer, a safe
  action owner, and a clean spoken reply with no backend internals.
- Done proof for 0.1.408 consolidation: full `python3 -m unittest
  tests.test_safety` passed 593/593; live `/api/health` reports
  `output/Jarvis.app`, Jarvis 0.1.408 build 408; only `Jarvis.app` remains in
  `output/`; no `Jarvis-LocalOSOnly.app` duplicate remains.
- Done proof for 0.1.409 router pass: full `python3 -m unittest
  tests.test_safety` passed 595/595. Safe planner previews now route the Magic
  Keyboard yuan prompt to `commerce.price_convert` deterministically and keep
  `Ms. Sharpay` as `Ms Sharpay` instead of truncating it to `Ms`.
- Done proof for 0.1.410 commerce hardening: `commerce.price_convert` now keeps
  the original open exchange-rate API and falls back to a jsDelivr-hosted USD
  currency file if the primary rate source fails. Focused commerce tests passed,
  and a live Magic Keyboard conversion returned about 671 yuan in 5.8s. Full
  `python3 -m unittest tests.test_safety` passed 596/596; no-prompt verifier
  passed 12/12 at `runtime/verification_no_prompt/verify-no-prompt-20260614-225331.json`;
  corrected silent speech-audit regression matrix passed 7/7 at
  `runtime/regression_prompt_matrix/20260614-225327/summary.json`.
- Done proof for 0.1.411 Codex chat route: requests such as "send a prompt
  called test in the Default chat" now choose `codex.chat_plan`, select Default,
  hide session IDs, and require typed confirmation instead of falling into a
  generic safety wall. Focused Codex routing tests passed 4/4.
- Done proof for 0.1.412 confirmation speech: confirmation-gated final replies
  are no longer blanket-silenced. Jarvis can say the safe final sentence, such
  as "I would use the Default Codex chat, but I need confirmation before sending
  anything," while still requiring approval and not sending anything. Focused
  server speech tests passed 5/5; full `python3 -m unittest tests.test_safety`
  passed 600/600; no-prompt verifier passed 12/12 at
  `runtime/verification_no_prompt/verify-no-prompt-20260614-231454.json`; live
  silent speech audit passed at `runtime/voice_loop_qa/20260614-231448/report.json`
  with 2 speech payloads and 0 leaks.
- Done proof for 0.1.415 regression matrix: live bundled Jarvis 0.1.415 build
  415 passed the full eight-prompt speech-audit matrix 8/8 at
  `runtime/regression_prompt_matrix/20260614-235525/summary.json`. This run
  verified Teams bookmark routing, LocalOS music routing, real RAM readout,
  Default Codex chat confirmation, Calendar schedule with duplicate Flag Day
  collapsed, safe Gemma 3 4B MacBook Air/local fallback planning, Ms. Sharpay
  contact-confirmation wording with the honorific preserved, and Magic Keyboard
  USD-to-yuan conversion. Full `python3 -m unittest tests.test_safety` passed
  604/604; no-prompt verifier passed 12/12 at
  `runtime/verification_no_prompt/verify-no-prompt-20260614-235444.json`.
- 0.1.415 specifics: Calendar all-day duplicate events now collapse even when
  duplicate holiday calendars disagree on location; public product price pages
  get a retry before Jarvis gives up; LocalOS alias replies use Leo's `LocalOS`
  spelling; email sender entities restore honorifics from the original prompt
  when the first model drops them; and actual email inspection requests recover
  through local email metadata if the first model fails without choosing a tool.
- Done proof for 0.1.416 honesty/voice-memory pass: live bundled Jarvis 0.1.416
  build 416 passed full `python3 -m unittest tests.test_safety` 606/606,
  no-prompt verification 12/12 at
  `runtime/verification_no_prompt/verify-no-prompt-20260615-004114.json`, and
  the eight-prompt speech-audit matrix 8/8 at
  `runtime/regression_prompt_matrix/20260615-004209/summary.json`. The runtime
  hygiene check showed one `jarvis-menu-bar`, one `jarvis-status-helper`, one
  bundled worker, only `output/Jarvis.app` in active output, and no `afplay`.
- 0.1.416 specifics: Teams handoff now states that Jarvis opens signed-in
  Chrome but has not inspected the assignment until a later page/screen read
  succeeds; Teams page-read failures now use product language instead of raw
  JavaScript/AppleScript internals; contact aliases preserve Leo's human label
  such as `Ms Sharpay` while tolerating `Sharpay` and dictated `his Sharpay`;
  and the wake/overlay plan uses visible `Listening.` instead of a routine
  spoken `Hello sir` greeting so Leo can keep speaking immediately.
- Done proof for verifier private-port fix: full `python3 scripts/verify_safe.py`
  passed 100/100 at `runtime/verification/verify-safe-20260615-010549.json`.
  The temporary bundle self-test now uses a private local port instead of
  fighting the live Jarvis worker on `127.0.0.1:8765`.
- Done proof for 0.1.417 native visible-screen read: live bundled Jarvis
  0.1.417 build 417 passed full `python3 -m unittest tests.test_safety`
  612/612, Swift routing self-test, no-prompt verification 12/12 at
  `runtime/verification_no_prompt/verify-no-prompt-20260615-013054.json`,
  the eight-prompt live regression matrix 8/8 at
  `runtime/regression_prompt_matrix/20260615-013149/summary.json`, and full
  `python3 scripts/verify_safe.py` 100/100 at
  `runtime/verification/verify-safe-20260615-013807.json`.
- 0.1.417 specifics: `screen.visible_text` is now a concrete native route.
  Explicit commands such as `read the visible Teams screen` or `what is on
  this page` use the macOS app's Apple Vision OCR path, send extracted text
  only to the local worker, scan it as untrusted content, omit private digest
  text from audit logs, and do not store screenshots by default. Normal
  `teams.assignment` still opens signed-in Chrome first and remains honest
  until a visible read succeeds.
- Done proof for 0.1.418 Teams handoff plus OCR follow-up: live bundled Jarvis
  0.1.418 build 418 passed full `python3 -m unittest tests.test_safety`
  612/612, Swift routing self-test, no-prompt verification 12/12 at
  `runtime/verification_no_prompt/verify-no-prompt-20260615-014800.json`,
  runtime hygiene with one app/helper/worker and no `afplay`, and full
  `python3 scripts/verify_safe.py` 100/100 at
  `runtime/verification/verify-safe-20260615-015136.json`.
- 0.1.418 specifics: after a `teams.assignment` command opens signed-in
  Chrome, Jarvis now automatically attempts a read-only native visible Teams
  OCR pass for assignment-reading requests such as "look in Teams for my
  newest Music assignment"; it does not trigger for submit/turn-in/send/delete
  wording, and it still treats OCR text as untrusted private local data.
- Done proof for 0.1.419 Teams assignment OCR digest: live bundled Jarvis
  0.1.419 build 419 passed full `python3 -m unittest tests.test_safety`
  613/613, no-prompt verification 12/12 at
  `runtime/verification_no_prompt/verify-no-prompt-20260615-015853.json`,
  runtime hygiene with one app/helper/worker and no `afplay`, a muted live
  `/api/screen/visible-text` probe that extracted assignment-related lines,
  and full `python3 scripts/verify_safe.py` 100/100 at
  `runtime/verification/verify-safe-20260615-020222.json`.
- 0.1.419 specifics: visible Teams OCR now prefers assignment-relevant lines
  such as class, assignment title, due time, instructions, and rubric over
  generic Teams navigation text. Audit redaction now also omits
  `assignment_digest_items`, after a focused test caught that private digest
  content was still being logged.
- Done proof for 0.1.420 Teams follow-up questions: live bundled Jarvis
  0.1.420 build 420 passed full `python3 -m unittest tests.test_safety`
  614/614, no-prompt verification 12/12 at
  `runtime/verification_no_prompt/verify-no-prompt-20260615-020859.json`,
  a muted live `/api/screen/visible-text` probe that produced five assignment
  follow-up questions, runtime hygiene with one app/helper/worker and no
  `afplay`, and full `python3 scripts/verify_safe.py` 100/100 at
  `runtime/verification/verify-safe-20260615-021232.json`.
- 0.1.420 specifics: the automatic Teams OCR follow-up now preserves Leo's
  original command, so prompts asking Jarvis to "ask me questions" can produce
  concise local follow-up questions about topic, visual style, personal
  details, hidden teacher requirements, and rubric priorities.
- Done proof for the current 0.1.420 regression pass: live bundled Jarvis
  0.1.420 passed the eight-prompt speech-audit matrix 8/8 at
  `runtime/regression_prompt_matrix/20260615-021451/summary.json`, covering
  Teams assignment handoff, LocalOS music routing, RAM, Codex Default-chat
  confirmation, Calendar, model-test planning, Ms. Sharpay contact handling,
  and Magic Keyboard yuan conversion with no spoken-output leaks.
- Done proof for 0.1.421 status-line polish: live bundled Jarvis 0.1.421 build
  421 passed full `python3 -m unittest tests.test_safety` 615/615, Swift
  routing self-test, no-prompt verification 12/12 at
  `runtime/verification_no_prompt/verify-no-prompt-20260615-023001.json`, full
  `python3 scripts/verify_safe.py` 100/100 at
  `runtime/verification/verify-safe-20260615-023431.json`, and focused
  speech-audit probes for Teams and Ms. Sharpay status lines with zero leaks.
- Full 0.1.421 matrix proof: live bundled Jarvis passed the eight-prompt
  speech-audit matrix 8/8 at
  `runtime/regression_prompt_matrix/20260615-023711/summary.json`, covering the
  same overnight target prompts after the status-line polish.
- 0.1.421 specifics: Jarvis no longer says `newest email` for a plural
  `all emails from Ms Sharpay in the past month` request. The spoken status is
  now `Checking emails from Ms Sharpay over the past month now.` and local STT
  transcribed it with 1.0 similarity. Teams assignment status lines now say
  `Opening Teams now.` instead of exposing an internal `assignment plan`.
- Done proof for 0.1.422 Teams OCR contract alignment: live bundled Jarvis
  0.1.422 build 422 passed full `python3 -m unittest tests.test_safety`
  615/615, Swift routing self-test, no-prompt verification 12/12 at
  `runtime/verification_no_prompt/verify-no-prompt-20260615-024457.json`, and
  full `python3 scripts/verify_safe.py` 100/100 at
  `runtime/verification/verify-safe-20260615-024858.json`.
- 0.1.422 specifics: the Teams assignment plan metadata now matches the real
  Swift app behavior. It reports `chrome_handoff_then_native_visible_read`,
  marks automatic follow-up as available only after the Teams bookmark is ready,
  and recommends `screen.visible_text` instead of the older `browser.read_page`
  placeholder, while still saying no assignment has been inspected until the
  visible-screen OCR follow-up succeeds.
- Done proof for 0.1.423 Teams live OCR hardening: live bundled Jarvis
  0.1.423 build 423 passed full `python3 -m unittest tests.test_safety`
  618/618, Swift menu-bar/status-helper self-tests, no-prompt verification
  12/12 at `runtime/verification_no_prompt/verify-no-prompt-20260615-032753.json`,
  full `python3 scripts/verify_safe.py` 100/100 at
  `runtime/verification/verify-safe-20260615-033049.json`, and the eight-prompt
  speech-audit matrix 8/8 at
  `runtime/regression_prompt_matrix/20260615-033624/summary.json`.
- 0.1.423 specifics: native visible-screen OCR now falls back from a low-text
  targeted Chrome window capture to a main-display capture, which fixed the
  real Teams handoff case where Chrome was visible but Apple Vision returned
  zero or one useless line from the targeted window. The Teams assignment
  digest now strips browser tab/menu noise and Teams sidebar crumbs before
  asking follow-up questions.
- 0.1.423 also passes the main app PID to `jarvis-status-helper`; the helper
  exits when its parent Jarvis app disappears, preventing orphan menu-bar heads
  after rebuild/relaunch cycles.
- Done proof for 0.1.424 speech status sync: live bundled Jarvis 0.1.424 build
  424 launched from bundled app resources with worker_launch_matches_bundle=true.
  Full `python3 -m unittest tests.test_safety` passed 618/618, no-prompt
  verification passed 12/12 at
  `runtime/verification_no_prompt/verify-no-prompt-20260615-034959.json`, and
  full `python3 scripts/verify_safe.py` passed 100/100 at
  `runtime/verification/verify-safe-20260615-035341.json`.
  The reusable eight-prompt speech-audit matrix passed 8/8 at
  `runtime/regression_prompt_matrix/20260615-040014/summary.json`.
- 0.1.424 specifics: the Swift panel now starts a lightweight speech-mute status
  sync task with worker monitoring and cancels it on shutdown, so the visible
  `Speech On` / `Muted` chip and menu state follow backend mute changes from
  the helper or verification endpoints. Live speech is currently muted.
- Matrix runner note: `scripts/run_regression_prompt_matrix.py` now owns the
  overnight eight-prompt gate and defaults to local STT plus suppressed speech
  and audio actions, so future builds can rerun the same proof without
  permission prompts or accidental music playback.
- Done proof for 0.1.425 Teams OCR retry: live bundled Jarvis 0.1.425 build
  425 launched from bundled app resources with worker_launch_matches_bundle=true.
  Full `python3 -m unittest tests.test_safety` passed 619/619, no-prompt
  verification passed 12/12 at
  `runtime/verification_no_prompt/verify-no-prompt-20260615-041133.json`, and
  full `python3 scripts/verify_safe.py` passed 100/100 at
  `runtime/verification/verify-safe-20260615-041502.json`.
- 0.1.425 specifics: after opening Teams in signed-in Chrome, the Swift app no
  longer performs just one fixed delayed OCR read. It now retries native
  visible-screen OCR up to four times and stops early once the response has
  useful assignment/page text, making the flagship Teams assignment prompt more
  likely to inspect the real page instead of racing Teams load time.
- Done proof for 0.1.426 persistent Shut Up: live bundled Jarvis 0.1.426 build
  426 launched from bundled app resources with worker_launch_matches_bundle=true.
  Full `python3 -m unittest tests.test_safety` passed 620/620, no-prompt
  verification passed 12/12 at
  `runtime/verification_no_prompt/verify-no-prompt-20260615-042825.json`, and
  full `python3 scripts/verify_safe.py` passed 100/100 at
  `runtime/verification/verify-safe-20260615-043159.json`.
- 0.1.426 specifics: `voice.speech_mute` now persists to
  `runtime/state/speech_mute.json`, the worker loads that state on startup, and
  the regression test isolates unit tests from Leo's real mute file. Post-
  verifier live state showed `muted=true`, `active_speech=false`, one
  `jarvis-menu-bar`, one parent-bound `jarvis-status-helper`, one bundled
  worker, and no `afplay`/QuickTime mystery audio.
- Full 0.1.426 matrix proof: live bundled Jarvis passed the eight-prompt
  speech-audit matrix 8/8 at
  `runtime/regression_prompt_matrix/20260615-043740/summary.json`, covering
  Teams assignment handoff, LocalOS music routing, RAM, Codex Default-chat
  confirmation, Calendar, model-test planning, Ms. Sharpay contact handling,
  and Magic Keyboard yuan conversion with no spoken-output leaks.
- Done proof for 0.1.427 Calendar speech polish: live bundled Jarvis 0.1.427
  build 427 launched from bundled app resources with worker_launch_matches_bundle=true.
  Full `python3 -m unittest tests.test_safety` passed 621/621, no-prompt
  verification passed 12/12 at
  `runtime/verification_no_prompt/verify-no-prompt-20260615-045256.json`, and
  full `python3 scripts/verify_safe.py` passed 100/100 at
  `runtime/verification/verify-safe-20260615-045903.json`.
- 0.1.427 specifics: Calendar reply text now strips Chinese course labels from
  spoken/visible summaries, softens all-caps subject names, and uses natural
  times such as `8 AM` and `8:55 AM` while preserving raw event details in the
  structured private result. The live Calendar probe returned clean English
  class names and stayed muted.
- Full 0.1.427 matrix proof: live bundled Jarvis passed the eight-prompt
  speech-audit matrix 8/8 at
  `runtime/regression_prompt_matrix/20260615-045930/summary.json`. The Calendar
  case produced `Today's Calendar schedule: 7 Math - 2 at 8 AM; 7 English - 1
  at 8:55 AM; ...` with no Chinese characters in the visible reply.
- Process hardening after 0.1.427: `scripts/run_regression_prompt_matrix.py`
  now accepts explicit `--no-permission-prompts` and `--stt-provider local`
  flags, still defaults to the unattended local-STT path, and fails closed if a
  caller asks for Apple Speech without `--allow-apple-speech`. Focused safety
  source-contract test passed, CLI help accepts the exact quiet flags, and the
  unsafe Apple-STT path errors before starting a run.
- Done proof for 0.1.428 LocalOS wording polish: live bundled Jarvis 0.1.428
  build 428 launched from bundled app resources with worker_launch_matches_bundle=true.
  The closest-match music phrasing now says `closest LocalOS match` instead of
  `closest LocalOS file`, so spoken replies are less filesystem-ish while still
  honest about approximate matches. Focused LocalOS music tests passed 5/5,
  full `python3 -m unittest tests.test_safety` passed 621/621, no-prompt
  verification passed 12/12 at
  `runtime/verification_no_prompt/verify-no-prompt-20260615-051951.json`, full
  `python3 scripts/verify_safe.py` passed 100/100 at
  `runtime/verification/verify-safe-20260615-052733.json`, and the quiet
  eight-prompt matrix passed 8/8 at
  `runtime/regression_prompt_matrix/20260615-052819/summary.json`.
- Done proof for 0.1.429 command payload hardening: live bundled Jarvis 0.1.429
  build 429 launched from bundled app resources with worker_launch_matches_bundle=true.
  `/api/command` and `/api/command/stream` now preserve the user's utterance
  when callers send `message`, `text`, or `prompt` instead of only `command`,
  and reject empty command bodies with HTTP 400 instead of routing an empty
  prompt into generic conversation. Focused payload helper test passed, full
  `python3 -m unittest tests.test_safety` passed 622/622, no-prompt
  verification passed 12/12 at
  `runtime/verification_no_prompt/verify-no-prompt-20260615-054148.json`, full
  `python3 scripts/verify_safe.py` passed 102/102 at
  `runtime/verification/verify-safe-20260615-054939.json`, and the quiet
  eight-prompt matrix passed 8/8 at
  `runtime/regression_prompt_matrix/20260615-055020/summary.json`.
- Additional 0.1.429 verifier hardening: focused isolated-worker checks passed
  32/32 after adding packaged HTTP-stream coverage for `message` alias payloads
  and empty `/api/command/stream` rejection.
- Done proof for completed price lane: first model selected
  `commerce.price_convert`; live bundled app returned
  "Magic Keyboard (USB-C) - US English is $99.00..." with about 671 yuan; full
  `tests.test_safety` passed 572/572; `verify_safe.py` passed 98/98 at
  `runtime/verification/verify-safe-20260614-112440.json`.
- Return point: continue the larger regression prompt suite, especially browser
  task execution, Codex chat routing, final speech/action verification, and
  real app-control/browser follow-through.
- Done proof for this pass: full `python -m unittest tests/test_safety.py`
  passed 579/579; `python scripts/verify_safe.py` passed 99/99 at
  `runtime/verification/verify-safe-20260614-125612.json`; live bundled app
  health reports Jarvis 0.1.389 build 389 from bundled app resources; speech is
  muted; `jarvis-status-helper` is running.
- Live 0.1.387 probe for `Play Waving Through a Window` exposed the next real
  blocker: macOS denied direct Chrome JavaScript automation (`Access not
  allowed`, -1723). Jarvis 0.1.388 adds a fallback that opens the LocalOS Music
  Player in Chrome through LaunchServices, waits for its normal polling bridge,
  then queues playback through the bridge.
- Leo saw multiple LocalOS music tabs during testing. Jarvis 0.1.389 adds a
  player-open cooldown marker plus a recent-heartbeat guard, so a stale-but-
  recently-seen LocalOS page does not trigger another Chrome tab.
- Quiet live check for `Play Waving Through a Window.` with speech/audio
  suppressed did not open or play audio. It selected the existing Dear Evan
  Hansen Tony Awards file through the Waving alias; this still needs a real
  audible LocalOS confirmation later, preferably without annoying Leo.
- Leo explicitly asked to test the same music prompt live while listening and
  to enable the LocalOS bridge/direct-control path in the next version. Bridge
  status before the 0.1.390 test was live: v2, polling active, current track
  Dear Evan Hansen | 2017 Tony Awards, playing true.
- Live 0.1.390 audible-path test sent `Play Waving Through a Window.` through
  `/api/command` with Jarvis speech suppressed and audio actions enabled.
  LocalOS bridge command `music-edaccbcef3c5` reported `playing`; follow-up
  snapshots showed `playing: true`, current track `Dear Evan Hansen | 2017 Tony
  Awards`, artist `Matt Hagmeier Curtis`, and no bridge error. After that,
  0.1.391 changes the tool/planner status from `queued` to `playing` when the
  bridge confirms actual playback.
- Live 0.1.391 check returned `status: playing`, `playback_confirmation:
  playing`, and summary `Started Local OS Music playback.` for the same prompt.
  A later snapshot reflected a newer user music request for `Justin Bieber -
  Beauty And A Beat`, proving the bridge continues accepting live commands.
- Leo approved a primitive music fast path: for `play ...` requests, Jarvis may
  directly extract the song phrase, search LocalOS library metadata, and queue
  the chosen song without a model deciding the tool. 0.1.392 adds tiny dictation
  tolerance such as `beauty and the beast` matching `Beauty And A Beat`.
- LocalOS bridge v3 adds a muted-autoplay retry for Jarvis-originated commands
  because Chrome sometimes rejects `audio.play()` with "user didn't interact
  with the document first" after the page is refreshed or loses activation.
- Live 0.1.392 test: `play me beauty and the beast` matched and played
  `Justin Bieber - Beauty And A Beat` through LocalOS bridge v3. `play for
  forever` then found the right track but the bridge picked it up after Jarvis's
  short confirmation timeout, so 0.1.393 treats delayed polling on an already
  live bridge as `queued` instead of a false failure.
- Live 0.1.393 test: `play natural` found the right song but Chrome still
  rejected LocalOS page audio with the user-interaction/autoplay error. 0.1.394
  adds a primitive native fallback: when the LocalOS page finds the track but
  Chrome blocks audio, Jarvis plays the exact LocalOS MP3 file with `afplay`.
- Live 0.1.394 test exposed a race: LocalOS briefly returned `playing` for
  `Natural`, then flipped to the Chrome user-interaction failure. 0.1.395 waits
  for a short stable-playing window before claiming success, so the native
  fallback can catch that delayed failure.
- Live 0.1.395 proved the native fallback can play the exact LocalOS MP3, but
  it also exposed a serious ownership bug: `afplay` could keep playing after
  Jarvis and Chrome/LocalOS tabs were closed. 0.1.396 tracks the fallback PID,
  cleans it on server startup/exit, and adds `localos.music_stop` for
  Jarvis-owned fallback music only. Do not leave background audio unowned again.
- Live 0.1.396 health reported bundled app resources, version 0.1.396 build
  396. A no-audio live `stop the music` command routed to `localos.music_stop`
  and returned idle without speaking. Full `python -m unittest
  tests/test_safety.py` passed 587/587.
- 0.1.397 fixes the wrong LocalOS reconnect contract. If Jarvis finds a song
  but the LocalOS Music bridge is unknown, stale, or not polling, Jarvis should
  try to open/reconnect the LocalOS Music Player automatically. A stale
  recently-seen snapshot is no longer a reason to strand Leo with "refresh it
  yourself." Keep duplicate-tab prevention secondary to actually completing
  playback.
- 0.1.398 fixes duplicate Jarvis Dock/menu-bar icons. The launcher must use
  normal `open`, never `open -n`, because `open -n` creates a second app
  instance. The visible menu-bar head belongs to `jarvis-status-helper`; the
  main app status item is off by default and only re-enabled with
  `JARVIS_SHOW_MAIN_STATUS_ITEM=yes` for debugging. Live verification after
  `build_and_launch_app.sh`: exactly one `jarvis-menu-bar`, one
  `jarvis-status-helper`, and one `run_dashboard.py` worker.
- Done proof for 0.1.430 sound-loop/status pass: the live bundled Jarvis
  0.1.430 build 430 reports `Jarvis 0.1.430 build 430 is online and ready.`
  instead of reading model names, worker internals, or Codex debug details.
  `scripts/voice_loop_qa.py` now records per-stage timings and reuses the
  final speech-audit audio instead of synthesizing the same final reply twice.
  File-level sound-in/sound-out loops passed for status
  (`runtime/voice_loop_qa/20260615-113030/report.json`, total 24.592s),
  calendar (`runtime/voice_loop_qa/20260615-113134/report.json`, total
  35.710s), LocalOS music (`runtime/voice_loop_qa/20260615-113243/report.json`,
  total 33.659s), and RAM (`runtime/voice_loop_qa/20260615-113332/report.json`,
  total 22.704s). This is not yet a physical room speaker/microphone capture;
  it verifies the audio files Jarvis would hear and say.
- LocalOS bridge restoration after 0.1.430: the sibling
  `localOSroot/localOS/localFiles/HTMLfiles/!musicPlayer.html` had lost the
  Jarvis control bridge code. Restored bridge v4 polling, snapshot publishing,
  LocalOS-owned `playTrackById`/pause handling, and human autoplay-blocked
  wording. Focused bridge test passed, full `python3 -m unittest
  tests.test_safety` passed 623/623, no-prompt verifier passed 12/12 at
  `runtime/verification_no_prompt/verify-no-prompt-20260615-114850.json`, and
  the LocalOS inline script parsed successfully with Node.
- Codex CLI proxy benchmark preparation: Codex CLI is present as
  `codex-cli 0.139.0`. The external proxy timing test was not executed because
  it sends prompts through Codex/proxy settings and requires Leo's direct
  safety-code approval in the current chat. Added
  `scripts/codex_cli_proxy_benchmark.py`, which dry-runs by default and only
  contacts Codex with `--execute`. It compares `control_no_proxy`,
  `clash_local_127`, and `tailscale_air_proxy`, can include a Jarvis streaming
  baseline, and writes reports under `runtime/codex_cli_proxy_benchmarks/`.
  Dry-run report:
  `runtime/codex_cli_proxy_benchmarks/codex-cli-proxy-benchmark-20260615-171543.md`.
  Focused harness tests passed, and full `python3 -m unittest tests.test_safety`
  passed 625/625.
- Done proof for 0.1.431 Codex CLI proxy speed patch: authorized benchmark
  `runtime/codex_cli_proxy_benchmarks/codex-cli-proxy-benchmark-20260615-173252.md`
  showed `control_no_proxy` completed in 19.858s after websocket reconnect/DNS
  failures, `clash_local_127` completed in 6.400s, `tailscale_air_proxy` timed
  out because `10.3.73.198:7890` was unreachable, and the Jarvis stream
  baseline returned in 0.539s. Jarvis now auto-detects a reachable local
  ClashX proxy at `127.0.0.1:7890` for Codex child processes only, leaving
  normal shell/system proxy settings alone. A direct `run_codex_delegate("hi")`
  proof selected `local_clash` and completed in 9.586s. Full
  `python3 -m unittest tests.test_safety` passed 627/627; rebuilt live bundled
  app reports Jarvis 0.1.431 build 431 with worker_launch_matches_bundle=true;
  no-prompt verifier passed 12/12 at
  `runtime/verification_no_prompt/verify-no-prompt-20260615-174653.json`;
  speech remained muted/inactive and runtime hygiene showed one app, one
  status helper, one worker, and no `afplay`.
- Done proof for 0.1.432 Codex speed status visibility patch: `codex speed
  status` now includes the active Codex child-process proxy route and the
  latest sanitized proxy benchmark, while dropping private prompt/reply text
  from the serialized benchmark data. Live bundled Jarvis returned
  `local ClashX 6.4s, no proxy 19.9s, Air proxy timed out`; full
  `python3 -m unittest tests.test_safety` passed 628/628; rebuilt live bundled
  app reports Jarvis 0.1.432 build 432 with worker_launch_matches_bundle=true;
  no-prompt verifier passed 12/12 at
  `runtime/verification_no_prompt/verify-no-prompt-20260615-180020.json`.
- Non-music regression harness safety valve: `scripts/run_regression_prompt_matrix.py`
  now supports `--only` and `--exclude` filters with tags such as `non-music`
  and `music`, so Codex can keep proving Teams/RAM/Codex/Calendar/model/email/
  price behavior without accidentally triggering the LocalOS music lane. Focused
  selection tests passed, `--list-cases` shows tags, live `--only ram` matrix
  passed 1/1 at `runtime/regression_prompt_matrix/20260615-181144/summary.json`,
  and full `python3 -m unittest tests.test_safety` passed 629/629.
- Done proof for 0.1.433 Keep Blabbering speech recovery patch: the status
  helper now posts `speechMuteChanged` after toggling Shut Up / Keep Blabbering,
  and the main app refreshes backend mute state before native app-side status
  speech. This closes the stale Swift `isSpeechMuted` path where the backend was
  unmuted but the app still suppressed local status speech. Live bundled
  `output/Jarvis.app` reports Jarvis 0.1.433 build 433 with
  `worker_launch_matches_bundle=true`; `/api/speech/mute` reports unmuted;
  `/api/speech/status` with `Jarvis speech is back.` returns `spoken=true` using
  the macOS provider; full `python3 -m unittest tests.test_safety` passed
  629/629; no-prompt verifier passed 12/12 at
  `runtime/verification_no_prompt/verify-no-prompt-20260615-183150.json`;
  output contains only one `Jarvis.app` bundle and runtime hygiene shows one
  menu-bar app, one status helper, one worker, and no stray `say`, `afplay`, or
  Piper process.
- Done proof for 0.1.434 LocalOS music prompt-contract hardening: the
  model-facing `localos.music_play` and `localos.music_stop` descriptions no
  longer advertise a tracked local fallback or Chrome-autoplay hidden player
  route. The catalog now states that LocalOS owns normal music playback and
  Jarvis must not start a separate hidden player; stop-preview metadata now says
  `localos_music_and_emergency_cleanup`. Focused planner tests passed 4/4;
  full `python3 -m unittest tests.test_safety` passed 630/630; the non-music
  prompt matrix passed 7/7 at
  `runtime/regression_prompt_matrix/20260615-191619/summary.json`; full
  post-rebuild eight-prompt matrix passed 8/8 at
  `runtime/regression_prompt_matrix/20260615-192359/summary.json`; full
  `python3 scripts/verify_safe.py` passed 104/104 at
  `runtime/verification/verify-safe-20260615-191916.json`; live `output/Jarvis.app`
  reports Jarvis 0.1.434 build 434 with `worker_launch_matches_bundle=true`;
  `/api/plan` for `stop the music` returns `localos_music_and_emergency_cleanup`;
  no-prompt verifier passed 12/12 at
  `runtime/verification_no_prompt/verify-no-prompt-20260615-192252.json`.
- Done proof for 0.1.435 report freshness: the master report renderer now
  includes the 0.1.430 through 0.1.434 product work before the older 0.1.429
  checkpoint, and `swift-shell/scripts/build_and_launch_app.sh` regenerates
  `/overnight-report/` and `/overnight-workboard/` after the live worker is
  healthy. The Morning Snapshot now derives its Python suite count from the
  newest proof item, so it shows `631/631 Python tests` instead of stale
  `568/568 Python tests`. Focused report/launcher tests passed 3/3; full
  `python3 -m unittest tests.test_safety` passed 631/631; no-prompt verifier
  passed 12/12 at
  `runtime/verification_no_prompt/verify-no-prompt-20260615-203433.json`;
  live `/overnight-report/` has `Live bundle: Jarvis 0.1.435 build 435`,
  `631/631 Python tests`, no `568/568 Python tests`, and the launch script
  printed the report-refresh confirmation after rebuilding `output/Jarvis.app`.
  Playwright screenshot:
  `output/playwright/jarvis-report-0.1.435-after-proof-fix.png`.
- Done proof for 0.1.436 mute-source tracing: speech mute writes now record
  their source, so a future silent Jarvis can distinguish main-app toggles,
  status-helper menu toggles, and raw API probes from the persisted
  `runtime/state/speech_mute.json` file. `JarvisClient.setSpeechMuted` sends
  `main_app` by default, the always-visible status helper sends
  `status_helper`, and the backend sanitizes payload sources. Focused checks
  passed 5/5; full `python3 -m unittest tests.test_safety` passed 633/633;
  `REPLACE_APP=1 swift-shell/scripts/build_and_launch_app.sh` launched live
  `Jarvis 0.1.436 build 436` with `worker_launch_matches_bundle=true` and
  refreshed `/overnight-report/`; no-prompt verifier passed 12/12 at
  `runtime/verification_no_prompt/verify-no-prompt-20260615-205415.json`;
  a live `/api/speech/mute` unmute probe returned `speech_mute_source:
  codex_probe` and left `muted=false`; runtime hygiene showed one menu-bar app,
  one status helper, one worker, and no stray `say`, `afplay`, or Piper process.
- Done proof for 0.1.437 readiness truthfulness: `/api/readiness` now separates
  `planned_unavailable_ids` from `actionable_unavailable_ids`, so future-gated
  `ui.automation` and `screen.ocr` no longer look like broken live tools.
  Source-level readiness returned no actionable unavailable IDs and planned
  future tools `ui.automation, screen.ocr`; focused readiness/report tests
  passed 3/3; full `python3 -m unittest tests.test_safety` passed 634/634;
  live `REPLACE_APP=1 swift-shell/scripts/build_and_launch_app.sh` launched
  `Jarvis 0.1.437 build 437` with `worker_launch_matches_bundle=true` and
  refreshed `/overnight-report/`; live readiness returned
  `actionable_unavailable_ids: []`, `planned_unavailable_ids: [ui.automation,
  screen.ocr]`, and note `Planned future tools not enabled yet: ui.automation,
  screen.ocr.`; no-prompt verifier passed 12/12 at
  `runtime/verification_no_prompt/verify-no-prompt-20260615-210830.json`.
- Done proof for 0.1.438 proof-surface freshness: `scripts/report_refresh.py`
  centralizes report/workboard rerendering, and both
  `scripts/smoke_fast_latency.py` and `scripts/run_regression_prompt_matrix.py`
  refresh `/overnight-report/` plus `/overnight-workboard/` after writing proof
  artifacts unless `--no-refresh-report` or `--no-report` suppresses it. Direct
  script entrypoints now add the project root to `sys.path`, fixing the
  `ModuleNotFoundError: No module named 'scripts'` found during live smoke.
  Focused refresh/entrypoint tests passed; full
  `python3 -m unittest tests.test_safety` passed 640/640; live
  `REPLACE_APP=1 swift-shell/scripts/build_and_launch_app.sh` launched
  `Jarvis 0.1.438 build 438` with `worker_launch_matches_bundle=true`; live
  fast latency smoke wrote
  `runtime/model_benchmarks/localhost-fast-latency-20260615-213953.md` and
  printed the report-refresh confirmation with first visible 0.658s; no-prompt
  verifier passed 12/12 at
  `runtime/verification_no_prompt/verify-no-prompt-20260615-213956.json`;
  live readiness still had no actionable unavailable tools.
- Done source proof for 0.1.439 speech truthfulness: `/api/speech/mute` now
  reports whether automatic TTS is enabled, whether the selected voice provider
  is available, and why speech is unavailable; unmuting prewarms Piper when that
  is the configured provider; the Swift `SpeechMuteResponse` decodes those
  fields; and the main app chip can show `Speech Off` or `Voice Missing` instead
  of falsely saying `Speech On`. The Keep Blabbering system message and the
  backend `/api/speech/mute` reply now also say when automatic speech is off or
  the voice provider is missing, instead of saying `Jarvis speech is on.`
  Focused speech/readiness tests passed, the Swift menu/status contract passed,
  and full
  `python3 -m unittest tests.test_safety` passed 666/666 after the spoken
  diagnostic firewall, hidden-tool-call firewall, empty-after-sanitization tests,
  stable conversation/wake latest-artifact/report-refresh tests, and report-refresh
  quiet failure-path/latest-backfill tests were added. Git commit is blocked
  by the sandbox's `.git/index.lock` write restriction until Leo can approve
  git writes again. Swift bundle build is also blocked in this sandbox because
  SwiftPM needs user cache and `sandbox-exec` access; do not claim a live
  0.1.439 launch until that can be rebuilt outside the restricted sandbox. The
  master report now has a short `Headline` section above the long history, and
  the workboard says the latest live bundle separately from the source-tested
  0.1.439 patch so it does not imply the unbuilt patch is already live.
- Follow-up 0.1.439 proof-surface polish: `scripts/smoke_conversation_context.py`
  and `scripts/smoke_wake_threshold.py` now write stable `latest.json` and
  `latest.md` files in their runtime folders, matching the fast-latency and
  voice-loop QA proof surfaces so the master HTML can always link the newest
  conversation-memory and wake-threshold evidence.
- Follow-up 0.1.439 report freshness polish: those conversation-context and
  wake-threshold smoke scripts now refresh the master report/workboard after
  writing proof artifacts, unless run with `--no-refresh-report` or
  `--no-report`.
- Follow-up 0.1.439 report honesty polish: `supporting_section` labels missing
  local supporting artifacts as `not generated yet` instead of displaying every
  local path as if it definitely exists. The current ignored runtime latest
  files for fast latency, regression matrix, conversation context, wake
  threshold, and voice-loop QA were regenerated locally after this check.
- Follow-up 0.1.439 report self-healing polish: `report_refresh` now backfills
  stable latest proof artifacts from the newest timestamped fast-latency,
  conversation-context, wake-threshold, and regression-matrix reports before it
  renders the master report/workboard.
- Follow-up 0.1.439 stale-progress polish: Swift progress-nudge tasks now exit
  immediately when they wake after the active turn has ended, instead of
  continuing through later delayed `Still working` nudges. The source-contract
  test checks the `stillCurrent` guard.
- Same 0.1.439 speech truthfulness patch: the Swift app now starts with
  `Speech Check` and `isAutomaticSpeechAvailable = false` until the backend
  answers. This avoids briefly showing `Speech On` on launch before Jarvis has
  verified that automatic spoken replies can actually work.
- Follow-up 0.1.439 source fix: if the menu/status path is still in
  `Speech Check`, the user-facing message now says voice readiness is still
  being checked instead of falling through to `Jarvis speech is on.`
- Follow-up 0.1.439 source fix: pressing Keep Blabbering now optimistically
  shows `Speech Check` while the backend unmute/readiness call is in flight,
  instead of briefly showing an unavailable or ready state before proof.
- Follow-up 0.1.439 source/report fix: `scripts/run_regression_prompt_matrix.py`
  now writes both the timestamped run `latest.json` and a root-level
  `runtime/regression_prompt_matrix/latest.json` plus `latest.md`, giving
  reports and future checks stable pointers to the newest matrix run.
- Follow-up 0.1.439 speech polish: `_sanitize_spoken_text` now removes
  backend/debug lines such as `Tool time`, `Fast model time`, `First visible`,
  Groq/Ollama status rows, Worker, Verification, App perms, Codex Activity, and
  CLI Tail before TTS, including colonless variants such as `Tool time 0.2s`
  or `Model gpt-oss-120b-cloud`. This is a final firewall in case visible
  technical metadata accidentally enters an automatic speech path.
- Follow-up 0.1.439 speech polish: the same diagnostic firewall now also strips
  pipe-separated inline diagnostics without colons, such as
  `| Tool time 0.2s | Model gpt-oss-120b-cloud | Backend groq`, before TTS.
  Focused sanitizer tests and full `python3 -m unittest tests.test_safety`
  passed 666/666 after this patch.
- Follow-up 0.1.439 fast-chat streaming polish: `_FastChatVisibleStreamBuffer`
  now treats backslash-letter fragments as pending until they prove they are a
  real hidden call. Future named hidden calls such as
  `\localos.music_play({...})` are still hidden immediately, but ordinary text
  such as `C:\Users\Leo` is preserved in the live visible stream. Focused
  streaming tests and full `python3 -m unittest tests.test_safety` passed
  666/666 after this patch.
- Follow-up 0.1.439 first-model routing polish: the fast-chat parser now accepts
  direct named hidden tool calls such as `\localos.music_play({"query":"..."})`
  when and only when that exact tool id is present in the current allowed tool
  catalog. Unknown or unlisted named calls still fail closed. Focused parser and
  streaming tests plus full `python3 -m unittest tests.test_safety` passed
  666/666 after this patch.
- Follow-up 0.1.439 named-call robustness: direct named hidden tool calls also
  accept `{"entities": {...}}` wrappers and direct brace form such as
  `\localos.music_play{"query":"..."}`. Catalog gating still applies. Focused
  parser tests and full `python3 -m unittest tests.test_safety` passed 666/666.
- Follow-up 0.1.439 model-context cleanup: assistant history is sanitized before
  it is sent back to the fast model. Hidden tool calls and backend diagnostics
  such as `Tool time` and `Backend groq` are stripped from prior Jarvis messages
  while preserving useful visible assistant text. Focused history tests and full
  `python3 -m unittest tests.test_safety` passed 666/666.
- Follow-up 0.1.439 context plumbing: `_fast_chat_history_messages` now accepts
  both `text` and `content` fields at the model boundary, so history payloads
  already normalized by the server and raw app-style aliases both reach the fast
  model. Focused context tests and full `python3 -m unittest tests.test_safety`
  passed 666/666.
- Follow-up 0.1.439 Codex-continuation context fix: planner waiting detection
  now reads both `content` and normalized `text` history fields. A plain code
  reply such as `123456` can continue a waiting Codex job even when the previous
  assistant prompt was normalized to `text`. Focused planner tests and full
  `python3 -m unittest tests.test_safety` passed 666/666.
- Follow-up 0.1.439 Codex target precision: `_latest_codex_resume_target` now
  uses a `codex-...` job id named in recent history when that job exists, instead
  of always selecting the newest/waiting job. This prevents a plain code reply
  from continuing a newer unrelated Codex job when the visible chat named the
  actual waiting job. Focused continuation tests and full
  `python3 -m unittest tests.test_safety` passed 666/666.
- Follow-up 0.1.439 hidden-tool-call firewall: TTS sanitization now strips future
  backslash tool-call shapes such as `\Calendar(...)` or
  `\localos.music_play({...})`, not only the original `\tool` and `\Email`
  forms, while preserving ordinary backslash text that is not a tool call.
  The voice-loop QA leak detector now flags the same generalized hidden-call
  syntax, so future audio audits do not miss newer tool names.
- Follow-up 0.1.439 speech diagnostic polish: if automatic TTS text becomes
  empty only because the sanitizer removed debug-only metadata, `speak_text_async`
  now returns `status: empty_after_sanitization` with empty spoken diagnostics,
  rather than the generic `empty` status or any raw debug preview.
- Follow-up 0.1.439 proof-surface polish: `scripts/smoke_fast_latency.py` now
  still writes timestamped JSON/Markdown reports, but also updates
  `runtime/model_benchmarks/latest.json` and `latest.md` for stable report links.
- Follow-up 0.1.439 report self-healing hardening: `scripts/report_refresh.py`
  now walks timestamped proof artifacts from newest to oldest and uses the first
  valid JSON/Markdown render. If a newer report is corrupt or half-written, it
  records the skipped source and backfills `latest.json`/`latest.md` from the
  newest valid older artifact instead of breaking the master report. Focused
  report-refresh tests passed, and full `python3 -m unittest tests.test_safety`
  passed 668/668 after this no-approval patch.
- Follow-up 0.1.439 fast-model speech contract: the first-model prompt now keeps
  displayed/spoken replies concise, English-first, voice-friendly, and explicit
  that non-English names or titles may be preserved only when necessary while
  the explanation remains English. The raw speech-dictation instruction was
  moved earlier in the prompt so diagnostics still show it, and the prompt stays
  under the existing first-model size budget. Focused prompt tests passed, and
  full `python3 -m unittest tests.test_safety` passed 669/669.
- Follow-up 0.1.439 status-helper keepalive: `JarvisMenuBarApp` now keeps a
  `shouldKeepStatusHelperRunning` flag and schedules a short restart if the
  separate `jarvis-status-helper` process exits unexpectedly while the main app
  is still running. Normal app shutdown clears the flag before terminating the
  helper, and the delayed restart checks the flag again after sleeping so quit
  does not resurrect a helper. The existing Shut Up/menu-bar source contract now
  checks both restart guards, and full `python3 -m unittest tests.test_safety`
  passed 670/670 after the later server speech-firewall test was added.
- Follow-up 0.1.439 server speech firewall proof: server auto-speech attachment
  now has a regression test for debug-only final replies. If a reply is only
  backend/model diagnostics such as Groq, Tool time, Model, or Backend text, the
  attached speech payload stays `empty_after_sanitization` with empty
  `text_preview` and `spoken_text`, and no raw diagnostic text appears in the
  speech fields. Swift `notePotentialSpeech` also treats
  `empty_after_sanitization` as a blocked/non-speaking status, so stripped debug
  payloads do not extend the app's speech-active window. Full
  `python3 -m unittest tests.test_safety` passed 670/670.
- Follow-up 0.1.439 suppressed-speech audit guard: `suppress_speech=True` command
  paths now have regression coverage for debug-only replies too. Their speech
  payload stays `suppressed_by_request` with an empty sanitized preview/length,
  and no Groq, Tool time, or model-name diagnostics survive in the auditable
  speech fields. Full `python3 -m unittest tests.test_safety` passed 671/671.
- Follow-up 0.1.439 verifier speech-preview guard: `verify_safe` already
  rejects empty TTS previews when checking that muted/quiet verification matched
  visible text. Added explicit regression assertions for empty string, `None`,
  and empty-reply/empty-preview cases so a silent final answer cannot be treated
  as a valid spoken match. Focused verifier test passed, and full
  `python3 -m unittest tests.test_safety` passed 671/671.
- Follow-up 0.1.439 regression-matrix evidence upgrade: the overnight prompt
  matrix summary now carries `speech_payload_count` and `speech_leak_count` from
  each nested `voice_loop_qa` report, and the Markdown summary prints status,
  payloads, and leaks in the table. Added a direct extractor test so a green
  matrix row can no longer hide whether it actually saw auditable spoken
  payloads or internal speech leaks. The top-level matrix JSON/Markdown now also
  totals speech payloads and leaks across all selected cases. Full
  `python3 -m unittest tests.test_safety` passed 673/673.
- Follow-up 0.1.439 report polish: the overnight workboard no longer prints
  `Jarvis unknown build unknown` when no live bundle metadata is available in
  the no-approval lane. It now says that live bundle metadata was unavailable
  during the source-only run, while preserving the normal
  `Jarvis 0.1.x build y` wording when metadata exists. The launch pill now says
  `not inspected` instead of `unknown` when the live app was not inspected. The
  report also reads `output/Jarvis.app/Contents/Info.plist` as a no-launch
  fallback, so source-only runs can still show the current output bundle file
  version without claiming it was live-launched. Full
  `python3 -m unittest tests.test_safety` passed 673/673.
- Follow-up 0.1.439 streaming hidden-call fix: the fast-model streaming visible
  buffer now resumes visible text after a hidden tool call closes. This means a
  streamed model output shaped like `Checking your em\Email(...)ail now.` renders
  and speaks as `Checking your email now.` while still stripping the hidden call.
  Direct buffer and streamed Groq-response regression tests cover the behavior,
  and full `python3 -m unittest tests.test_safety` passed 675/675.
- Follow-up 0.1.439 history-injection guard: server-normalized history and
  model-bound fast-chat history now accept only `user` and `assistant` rows from
  client payloads. Client-provided `system` rows are ignored, so Copy Chat JSON
  or direct API history cannot smuggle a second system prompt into the model.
  Focused history tests and full `python3 -m unittest tests.test_safety` passed
  675/675.
- Follow-up 0.1.439 middle-planner guard: broader tool planning now applies the
  same history boundary before GPT-OSS/Llama-style planning. Client-provided
  `system` rows are ignored, `jarvis` history is normalized to `assistant`, and
  assistant rows are sanitized so hidden tool calls plus backend diagnostics do
  not get fed back into the next model. Focused planner tests and full
  `python3 -m unittest tests.test_safety` passed 676/676.

## Regression Prompts Leo Expects Jarvis To Handle

- "Look in Teams for my newest Music assignment and ask me a list of questions
  to answer so that you have enough information to finish the assignment."
- "Play Waving Through a Window."
- "Check in Activity Monitor how much RAM my computer is using."
- "Open Codex and send a prompt called 'test' in the Default chat."
- "Check my calendar for my schedule today."
- "Test the Gemma 3 4B model for me."
- "Summarize all the emails from Ms. Sharpay in the past month."
- "Jarvis, could you search up the price of the Magic Keyboard and tell me its
  price converted to yuan?"

## Notes On Expected Behavior

- Teams task: use browser/bookmarks and logged-in Chrome when needed; do not
  pretend WebKit inherited Chrome cookies.
- Music task: LocalOS should control/play the Music app/player. Jarvis should
  only claim playback after confirmation. `accepted` is only an internal status;
  after the evidence window closes without actual audio, Jarvis should say
  LocalOS did not start the audio. If LocalOS is not connected, Jarvis should
  attempt to open/reconnect the music player automatically before asking Leo to
  do anything.
- RAM task: read real system memory state, ideally through a reliable native or
  terminal-backed tool.
- Codex task: choose the correct Codex chat, defaulting weird/general prompts to
  the Default chat, and mark prompts as Jarvis-generated.
- Calendar task: use local Calendar cache when permissions allow it; explain
  permission problems clearly.
- Model test task: avoid heavy local runs on Leo's 16 GB MacBook Pro when a
  cloud or MacBook Air route is safer.
- Ms. Sharpay task: infer contact aliases from local sender metadata and store
  useful contact mappings without leaking private content.
- Magic Keyboard task: `commerce.price_convert` now uses Apple's official U.S.
  Magic Keyboard (USB-C) product page plus a live USD-CNY public exchange-rate
  source. Keep replies short and spoken-friendly; source URLs stay in JSON.

## Product Principles From Leo

- No fake keyword-only skill demos. Let the model choose tools from clear tool
  definitions and keep tool calls hidden from spoken output.
- Overnight work must avoid approval prompts. If a command would need
  escalation, GUI control, network access, git writes, privileged builds, or
  another user approval while Leo is asleep, skip that lane, log the blocker,
  and continue with repo-local no-approval work instead.
- If Jarvis starts doing work, it should visibly and audibly acknowledge the
  task naturally, then return with the result.
- Spoken output must be clean English unless a specific non-English phrase is
  necessary.
- Never speak internal model/tool/debug details to Leo.
- If Jarvis is speaking, a visible menu-bar head with Shut Up must be available.
- Use tests for every bug Leo catches, especially speech mismatch, music false
  positives, and UI/menu-bar regressions.

## 2026-06-16 No-Approval Overnight Notes

- Follow-up 0.1.439 model-input guard: model-context diagnostics now normalize
  history before counting or previewing it, so fake `system` rows and hidden
  assistant tool/diagnostic fragments do not appear as if they were model input.
  Codex continuation helper functions also ignore non-user/non-assistant rows,
  preventing direct helper calls from treating hostile system-history text as a
  waiting Codex job. Focused tests and full `python3 -m unittest
  tests.test_safety` passed 678/678.
- Follow-up 0.1.439 regression-matrix speech gate: the matrix no longer trusts a
  nested green status alone. Each row now also requires speech-audit status
  `passed`, at least one spoken payload, and zero internal speech leaks. A
  malformed row with `payload_count: 0` now fails with `Speech audit captured no
  spoken payloads.` Focused matrix tests and full `python3 -m unittest
  tests.test_safety` passed 679/679.
- Follow-up 0.1.439 full spoken-output audit guard: suppressed speech payloads
  now include `spoken_text`, and voice-loop QA audits `spoken_text` before
  falling back to `text_preview` so internal speech leaks after preview
  boundaries cannot be missed. Focused tests and full `python3 -m unittest
  tests.test_safety` passed 680/680.
- Follow-up 0.1.439 safe-verifier spoken-output guard: `verify_safe.py` now
  prefers full `spoken_text` over `text_preview` when checking muted final
  speech against the visible reply, and rejects cases where a short preview
  matches but the full spoken text leaks extra internal words. Focused verifier
  tests and full `python3 -m unittest tests.test_safety` passed 682/682.
- Follow-up 0.1.439 Gemma audio probe safety default: `probe_gemma3n_audio.py`
  is now dry-run by default and requires `--execute-network` before uploading
  generated test audio to the public Hugging Face Space. Source-contract test
  and full `python3 -m unittest tests.test_safety` passed 683/683.
- Follow-up 0.1.439 streaming benchmark safety default:
  `benchmark_streaming_models.py` is now dry-run by default and requires
  `--execute-network` before contacting Groq for streaming timings.
  Source-contract test, compile check, and full `python3 -m unittest
  tests.test_safety` passed 684/684.
- Follow-up 0.1.439 fast-model benchmark safety default:
  `benchmark_fast_models.py` is now dry-run by default and requires
  `--execute-network` before running `ollama list`, discovering Groq models, or
  benchmarking local/cloud candidates. Source-contract test, compile check, and
  full `python3 -m unittest tests.test_safety` passed 685/685.
- Follow-up 0.1.439 middle-model comparison safety default:
  `compare_middle_models.py` is now dry-run by default and requires
  `--execute-network` before calling Ollama tags/ps, Groq, Ollama generate, or
  optional audio probes; dry-run also avoids reading Leo's `~/.jarvis.env`.
  Source-contract test, compile check, and full `python3 -m unittest
  tests.test_safety` passed 686/686.
- Follow-up 0.1.439 executable dry-run proof: added tests that actually run the
  default dry-run entrypoints for `benchmark_fast_models.py`,
  `benchmark_streaming_models.py`, `probe_gemma3n_audio.py`, and
  `compare_middle_models.py` with patched network/model paths that would fail if
  touched. Focused dry-run tests, compile check, diff check, and full
  `python3 -m unittest tests.test_safety` passed 690/690.
- Follow-up 0.1.439 LocalOS music honesty: queued, accepted,
  bridge-not-polling, and unconfirmed LocalOS music states now say playback has
  not started yet unless LocalOS confirms actual audio. The planner summary no
  longer says queued playback as if it were success. Focused LocalOS music tests
  passed 6/6, compile/diff checks were clean, the report was refreshed, and full
  `python3 -m unittest tests.test_safety` passed 690/690.
- Follow-up 0.1.439 direct voice-loop no-approval default:
  `scripts/voice_loop_qa.py` now defaults to `--stt-provider local` and treats
  that as no-permission mode, so running the harness directly cannot trigger the
  Jarvis.app Apple Speech path by accident. Apple Speech now requires explicit
  `--allow-apple-speech`, and `--no-permission-prompts` rejects non-local STT
  modes. Focused tests passed 4/4, compile/diff checks were clean, and full
  `python3 -m unittest tests.test_safety` passed 694/694.
- Follow-up 0.1.439 unattended speech-side-effect cleanup: fast-latency smoke,
  conversation-context smoke, and direct voice-loop QA no longer carry stale
  helper code for `/api/speech/mute`; they rely on per-request
  `suppress_speech`/`suppress_audio_actions`. No-prompt verifier diagnostics
  for model context, wake debug, wake echo, and repeated wake now also stay
  quiet per request instead of muting/unmuting the whole app. The dedicated
  speech-mute endpoint test is now the only verifier lane that changes global
  mute state. Focused tests passed 11/11, and full `python3 -m unittest
  tests.test_safety` passed 694/694.
- Follow-up 0.1.439 voice-loop report label cleanup: if an older or partial
  voice-loop QA report lacks `stt_provider`, both the per-run markdown and the
  master report voice-loop summary now default the displayed provider to
  `local` instead of the old `auto`, matching the no-permission unattended
  default. Focused markdown/report tests passed 4/4, and full `python3 -m
  unittest tests.test_safety` passed 694/694.
- Follow-up 0.1.439 Gemma audio probe plan-only dry-run: the default
  `probe_gemma3n_audio.py` run no longer calls macOS `say`, Piper, or
  `afconvert`; it writes planned sample rows with `planned_dry_run` status.
  Local audio generation now requires `--synthesize-local`, and cloud upload
  still requires `--execute-network`. Focused Gemma probe tests passed 3/3, and
  full `python3 -m unittest tests.test_safety` passed 694/694.
- Follow-up 0.1.439 report Git honesty: the master report/workboard header now
  includes a `Worktree: clean/dirty` pill from `git status --porcelain`, so a
  source commit hash is not mistaken for a fully committed source state during
  no-approval overnight work. Focused report test passed, and full
  `python3 -m unittest tests.test_safety` passed 694/694.
- Follow-up 0.1.439 report proof clarity: the report/workboard header now
  separates `Python suite: 697/697 passed` from `Safe verifier: 104/104 passed`
  instead of using the vague `Verification` label for the older live-safe
  artifact. `git status` failures also show `Worktree: unknown` instead of
  silently claiming clean. Focused renderer tests passed 3/3, and full
  `python3 -m unittest tests.test_safety` passed 696/696 before the STT repair
  helper hardening.
- Follow-up 0.1.439 local STT repair safety default:
  `scripts/repair_local_stt_model.py` is now dry-run by default, exits cleanly
  as a plan-only check, and requires `--execute-network` before downloading the
  faster-whisper model blob. Focused STT repair tests passed 3/3, compile and
  diff checks were clean, and full `python3 -m unittest tests.test_safety`
  passed 697/697.
- Follow-up 0.1.439 TTS audition online-voice safety default:
  `scripts/generate_tts_audition.py` now keeps online Edge TTS candidates behind
  `--include-online-voices`; the default candidate pool stays local even when
  `edge-tts` is installed. The CLI also rejects external output directories
  unless `--allow-external-output-dir` is explicit, so the sample cleanup path
  cannot casually delete MP3s outside the project; the direct `generate(...)`
  function now has the same guard. Focused TTS audition behavior tests passed
  5/5, compile and diff checks were clean, and full `python3 -m unittest
  tests.test_safety` passed 704/704.
- Overnight operating rule from Leo, 2026-06-16: when Leo is asleep or has said
  not to expect responses, do not run anything that needs approval. Stay inside
  repo-local reads, repo-local edits, local tests, compile checks, and report
  rendering. Do not launch apps, change settings, call network/model services,
  stage/commit/push Git, send notifications, or request escalation until Leo is
  present again and explicitly authorizes that lane.
- Follow-up 0.1.439 Codex proxy benchmark proof surface:
  `scripts/codex_cli_proxy_benchmark.py` now writes stable
  `runtime/codex_cli_proxy_benchmarks/latest.json` and `latest.md` pointers for
  dry-run or explicitly approved runs. The master report links those files.
  Focused proxy/report tests passed, compile/diff checks were clean, and full
  `python3 -m unittest tests.test_safety` passed 705/705.
- Follow-up 0.1.439 report-refresh self-healing for Codex proxy proof:
  `scripts/report_refresh.py` now backfills the Codex CLI proxy benchmark
  `latest.json` and `latest.md` from the newest timestamped benchmark report,
  matching the existing fast-latency, conversation, wake, matrix, and voice-loop
  proof surfaces. Focused report-refresh/proxy tests passed, compile/diff checks
  were clean, and full `python3 -m unittest tests.test_safety` passed 705/705.
- Follow-up 0.1.439 proof-surface consistency cleanup:
  `scripts/codex_cli_proxy_benchmark.py` now writes artifact path fields before
  serializing timestamped and `latest.*` reports, so `latest.json` carries its
  own stable paths. `scripts/report_refresh.py` also seeds an empty local
  `runtime/memory/contact_aliases.json` surface when no contact aliases exist
  yet, without overwriting existing data. The master report now says
  `Python suite: 706/706 passed`, the contact alias link is no longer marked
  missing, focused checks passed, compile/diff checks were clean, and full
  `python3 -m unittest tests.test_safety` passed 706/706.
- Follow-up 0.1.439 unattended status URL safety:
  `scripts/morning_status.py` now refuses non-loopback `JARVIS_URL`,
  `JARVIS_BASE_URL`, or `--base-url` values before making any status request.
  This prevents a misconfigured overnight report/status command from contacting
  a non-local server while Leo is asleep. Focused morning-status/report tests
  passed, compile/diff checks were clean, the master report was refreshed, and
  full `python3 -m unittest tests.test_safety` passed 707/707.
- Follow-up 0.1.439 overnight report URL safety:
  `scripts/render_overnight_status.py` and `scripts/report_refresh.py` now
  normalize and reject non-loopback base URLs before making any worker health
  request. This gives the master report/workboard renderer the same unattended
  safety posture as `morning_status.py`. Focused renderer/report-refresh tests
  passed, compile/diff checks were clean, the master report was refreshed, and
  full `python3 -m unittest tests.test_safety` passed 709/709.
- Follow-up 0.1.439 local-worker smoke URL safety:
  `scripts/smoke_fast_latency.py`, `scripts/smoke_conversation_context.py`,
  and `scripts/voice_loop_qa.py` now reject non-loopback base URLs before
  streaming any command to a worker. This keeps unattended latency/context/voice
  probes local even if a bad base URL is passed. Focused smoke/report tests
  passed, compile/diff checks were clean, the master report was refreshed, and
  full `python3 -m unittest tests.test_safety` passed 710/710.
- Follow-up 0.1.439 verifier URL safety:
  `scripts/verify_safe.py` and `scripts/verify_no_prompt.py` now reject
  non-loopback base URLs before verifier HTTP helpers make requests.
  `wait_for_health` treats bad base URLs as unavailable, so the full verifier
  can fall back to a temporary local worker instead of contacting an external
  address. Focused verifier/report tests passed, compile/diff checks were clean,
  the master report was refreshed, and full `python3 -m unittest
  tests.test_safety` passed 711/711.
- Follow-up 0.1.439 regression-matrix URL safety and LocalOS test stability:
  `scripts/run_regression_prompt_matrix.py` now rejects non-loopback base URLs
  before it starts any child `voice_loop_qa.py` process or report refresh. The
  LocalOS Waving Through a Window/Wyndham alias unit tests now pin bridge
  liveness and playback confirmation to deterministic local fakes, so they test
  alias selection and queued control payloads without accidentally running slow
  Chrome/player recovery lanes. Focused matrix/report/LocalOS tests passed,
  compile/diff checks were clean, the master report was refreshed, and full
  `python3 -m unittest tests.test_safety` passed 712/712.
- Follow-up 0.1.439 wake-threshold refresh URL safety:
  `scripts/smoke_wake_threshold.py` now rejects non-loopback base URLs before
  refreshing the master report after an offline wake-threshold run. This extends
  the same no-approval overnight safety contract to the wake scorer surface.
  Focused local-smoke/report tests passed, compile/diff checks were clean, the
  master report was refreshed, and full `python3 -m unittest tests.test_safety`
  remains 712/712 because this extended an existing test.
- Follow-up 0.1.439 Codex proxy baseline URL safety:
  `scripts/codex_cli_proxy_benchmark.py` now normalizes and rejects non-loopback
  Jarvis base URLs before timing the optional `/api/command/stream` baseline.
  Dry-run behavior is unchanged, and approved Codex proxy benchmarks still keep
  proxy variables scoped to child processes. Focused Codex benchmark/report
  tests passed, compile/diff checks were clean, the master report was refreshed,
  and full `python3 -m unittest tests.test_safety` passed 713/713.
- Follow-up 0.1.439 Codex proxy CLI refusal polish:
  The Codex proxy benchmark CLI now catches unsafe Jarvis baseline URLs and
  returns a clean code 2 with `Refused unsafe base URL` instead of showing a
  Python traceback. Focused Codex benchmark/report tests passed, compile/diff
  checks were clean, the master report was refreshed, and full `python3 -m
  unittest tests.test_safety` passed 714/714.
- Follow-up 0.1.439 emergency music-stop wording honesty:
  `localos_music_stop()` now says `Muted system audio to stop the music.` when
  normal LocalOS/page/media pause fails and Jarvis mutes system output as the
  last-resort emergency stop. The planner now uses the tool's returned reply
  for `localos.music_stop`, so the visible summary does not falsely claim
  normal playback stopped. Focused planner/tool/report tests passed,
  compile/diff checks were clean, the master report was refreshed, and full
  `python3 -m unittest tests.test_safety` passed 715/715.
- Follow-up 0.1.439 middle-model cleanup safety:
  `scripts/compare_middle_models.py` no longer stops installed local Ollama
  candidate models during `--execute-network` cloud comparison unless the caller
  also passes the explicit `--cleanup-local-models` opt-in. This separates
  cloud model testing from changing local model/runtime state. Focused
  comparison/report tests passed, compile/diff checks were clean, the master
  report was refreshed, and full `python3 -m unittest tests.test_safety` passed
  716/716.
- Follow-up 0.1.439 fast-model local Ollama opt-in:
  `scripts/benchmark_fast_models.py` no longer discovers or benchmarks
  installed local Ollama models during `--execute-network` unless the caller also
  passes `--include-local-ollama`. This keeps fast-model benchmarking cloud-first
  by default and avoids surprise local model load on Leo's 16 GB Mac. Focused
  benchmark/report tests passed, compile/diff checks were clean, the master
  report was refreshed, and full `python3 -m unittest tests.test_safety` passed
  717/717.
- Follow-up 0.1.439 middle-model audio-probe privacy:
  `scripts/compare_middle_models.py` now rejects `--audio-probe` paths outside
  the Jarvis project unless the caller passes `--allow-external-audio-probe`.
  This prevents accidental upload or model-processing of arbitrary Desktop,
  Downloads, or personal audio files during comparison runs. Focused
  comparison/report tests passed, compile/diff checks were clean, the master
  report was refreshed, and full `python3 -m unittest tests.test_safety` passed
  718/718.
- Follow-up 0.1.439 cloud-only middle-model local-inspection safety:
  `scripts/compare_middle_models.py` no longer calls local Ollama tags or ps
  during cloud-only `--execute-network` runs. Local Ollama inspection now happens
  only when local models are allowed, cleanup is requested, or the caller passes
  `--inspect-local-ollama`. Focused comparison/report tests passed, compile/diff
  checks were clean, the master report was refreshed, and full `python3 -m
  unittest tests.test_safety` passed 719/719.
- Follow-up 0.1.439 fast-model benchmark wording:
  `scripts/benchmark_fast_models.py` now describes `--execute-network` as
  discovering and benchmarking cloud fast-model candidates, while local Ollama
  remains behind `--include-local-ollama`. This was a wording/source-contract
  correction on top of the same behavior. Focused fast-benchmark source test
  passed, compile/diff checks were clean, the master report was refreshed, and
  full `python3 -m unittest tests.test_safety` remained 719/719.
- Follow-up 0.1.439 summon overlay wording:
  The polished top-right summon overlay no longer says `The debug window has
  details.` on errors. Both the fallback overlay view and runtime finish path now
  say `Check the Jarvis window for details.` so the user-facing surface stays
  product-like instead of debug-console-like. Focused summon/report tests passed,
  diff checks were clean, the master report was refreshed, and full `python3 -m
  unittest tests.test_safety` remained 719/719.
- Follow-up 0.1.439 summon in-progress wording:
  The polished summon overlay no longer exposes implementation wording such as
  `Choosing the best route.` or `Writing the response.`. The visible states now
  say `Finding the best way to help.` and `Preparing the answer.`. Focused
  summon/report tests passed, diff checks were clean, the master report was
  refreshed, and full `python3 -m unittest tests.test_safety` remained 719/719.
- Follow-up 0.1.439 LocalOS autoplay wording:
  LocalOS autoplay-blocked replies now stay product-facing. If the music player
  needs one click before audio can start, Jarvis no longer says Chrome is
  blocking playback. Focused LocalOS/report tests passed, diff checks were
  clean, the master report was refreshed, and full `python3 -m unittest
  tests.test_safety` remained 719/719.
- Follow-up 0.1.439 normal-app identity guard:
  The bundle build script default app identity is now source-tested as normal
  `Jarvis` with bundle id `local.leo.jarvis`, and the report says test-only
  LocalOS-only names cannot quietly become the main app. This added one source
  contract test, bringing the full local safety suite to 720/720. The master
  report/workboard were refreshed and now show 720/720.
- Follow-up 0.1.439 warm Piper chunking:
  The warm Piper worker now keeps normal and medium replies in one synthesized
  audio chunk, splitting only unusually long speech. This trades a tiny amount
  of first-audio optimization for fewer chunk boundaries, which should reduce
  choppy pauses and skipped/repeated phrase risk. Focused Piper/report tests
  passed, diff checks were clean, the master report was refreshed, and full
  `python3 -m unittest tests.test_safety` remained 720/720.
- Follow-up 0.1.439 Notifications permission wording:
  The Notifications permission tile now explains that notification access is
  optional unless timers or background alerts need macOS notifications.
  Accessibility remains the computer-control permission, so the permissions
  panel should be less confusing. Focused permission/report tests passed, diff
  checks were clean, the master report was refreshed, and full `python3 -m
  unittest tests.test_safety` remained 720/720.
- Follow-up 0.1.439 menu-bar head click behavior:
  The separate Jarvis status-helper head now opens the Jarvis window on a
  normal click and keeps the emergency menu on right-click or Control-click.
  This preserves fast access to `Shut Up` and `Stop Music` without making the
  normal icon click feel unlike a Mac app. Focused helper/report tests passed,
  the Swift package compiled with only pre-existing Outlook screenshot-reader
  deprecation warnings, the master report was refreshed, and full `python3 -m
  unittest tests.test_safety` passed 720/720.
- Follow-up 0.1.439 report proof correction:
  The master report no longer claims SwiftPM was blocked. The correct caveat is
  that 0.1.439 source and Swift compile proof are green, but the app was not
  live-launched because the no-approval overnight pass avoids GUI launch, app
  replacement, and Git publishing while Leo is asleep.
- Follow-up 0.1.439 status-helper self-test proof:
  The compiled `jarvis-status-helper --self-test` passed after the menu-bar
  click behavior change, and the master report now includes that proof line.
- Follow-up 0.1.439 status-helper click split self-test:
  The status helper now has a pure click-decision function and its executable
  self-test proves that left-click opens the Jarvis window while right-click or
  Control-click opens the emergency menu. The report now says the helper
  self-test covers this click split.
- Follow-up 0.1.439 overnight status false-stale fix:
  `overnight_work_status()` now keeps compact proof items for display but uses
  the full Proof So Far text for integrity checks. This fixed the live false
  stale warning where latest verifier and voice-QA artifact paths existed later
  in the report but were outside the first 80 proof rows. Real local status now
  reports `Report integrity is current.` The new regression brings the full
  safety suite to 721/721, and the regenerated master report now shows 721/721.
- Follow-up 0.1.439 tool-catalog diagnostic honesty:
  Direct `tool_catalog_status()` calls no longer say the first model sees 0
  tools when the planner did not attach the first-model catalog. Planner-routed
  `tool catalog status` still reports the real first-model count, currently 66.
  The added direct/planner diagnostics tests bring the full safety suite to
  723/723, and the regenerated master report now shows 723/723.
- Follow-up 0.1.439 capability-status counting:
  `capabilities_status()` now adds explicit prepared capability rows for Wake
  Lab and STT audition. The visible summary says `2 prepared` instead of
  saying `0 prepared` while also listing prepared surfaces.
- Follow-up 0.1.439 overnight-status bundle wording:
  `overnight_work_status()` now accepts `Output bundle`, `Live bundle`, or
  `Bundle` report pills when extracting the current bundle. The live local
  status reply now names `Jarvis 0.1.438 build 438` again instead of only
  naming the source commit.
  That report-bundle checkpoint brought the full safety suite to 724/724 before
  the later final-speech fallback regression raised the current proof count.
- Follow-up 0.1.439 final-speech field fallback:
  Server auto-speech now sanitizes each candidate speech field before selecting
  it. If `spoken_summary` is only Groq/model/tool-time diagnostics, Jarvis skips
  it and speaks the safe visible `reply` instead. The full safety suite passed
  725/725, and the regenerated master report shows 725/725.
- Follow-up 0.1.439 duplicate menu-bar head hardening:
  The main app now keeps its legacy status item disabled even if the old
  `JARVIS_SHOW_MAIN_STATUS_ITEM=yes` debug variable is present, so the native
  status helper remains the single visible menu-bar head. Python source-contract
  test passed, and `jarvis-status-helper --self-test` passed with repo-local
  Swift caches. `jarvis-menu-bar --self-test` compiled but stopped at its worker
  startup section because no live worker was started in this no-approval pass.
- Follow-up 0.1.439 commerce no-network proof path:
  `commerce_price_convert(..., allow_network=False)` now returns a plan-only
  Magic Keyboard price-conversion path with `external_network_lookup=false` and
  proves `_fetch_public_web_text` is not called. Normal mocked conversion tests
  still pass, so product web lookup remains available when execution is allowed.
  The full safety suite passed 726/726, and the regenerated master report shows
  726/726.
- Follow-up 0.1.439 clean email speech preference:
  Server auto-speech now prefers `email_summary` over `reply` after
  `spoken_summary`, so if a tool returns both a plain summary and a logistical
  "I checked/scanned..." reply, Jarvis speaks the plain summary. Focused speech
  regressions passed, the full safety suite passed 727/727, and the regenerated
  master report shows 727/727.
- Follow-up 0.1.439 LocalOS autoplay classifier:
  LocalOS music now treats additional browser media errors such as
  `NotAllowedError`, "user has not interacted", and interrupted play requests as
  the same one-click-needed autoplay state. This keeps Jarvis on the
  LocalOS-only playback contract instead of reporting a generic failure or
  starting hidden playback. Focused LocalOS regressions passed, the full safety
  suite passed 728/728, and the regenerated master report shows 728/728.
- Follow-up 0.1.439 status-helper restart speed:
  The main app now restarts the separate `jarvis-status-helper` after 0.25s
  instead of 0.8s if it exits while Jarvis is still running. This tightens the
  "Shut Up must be reachable while Jarvis can speak" contract without changing
  normal quit behavior. Focused source-contract/report tests passed, the full
  safety suite passed 729/729, and the regenerated master report shows 729/729.
- Follow-up 0.1.439 markdown-link speech polish:
  `_sanitize_spoken_text` now collapses markdown links to their plain labels
  before URL stripping, so Jarvis says "the short feedback form" instead of
  awkward URL/parentheses/link clutter. Focused sanitizer/report tests passed,
  the full safety suite passed 730/730, and the regenerated master report shows
  730/730.
- Follow-up 0.1.439 Swift client loopback guard:
  `JarvisClient.fromEnvironment()` now rejects non-loopback `JARVIS_URL` and
  `JARVIS_BASE_URL`, and the status helper ignores non-loopback explicit
  `--base-url` arguments instead of contacting them. This brings the visible app
  client path in line with the no-remote-worker guard already added to the
  unattended Python scripts. Focused source/report tests passed, the full safety
  suite passed 731/731, and the regenerated master report shows 731/731.
- Follow-up 0.1.439 progress wording polish:
  Swift long-task nudges now say "I'm still working on it." and "This is taking
  longer than usual. I'm still on it." instead of the old "Still working. Wait a
  sec..." phrasing. The active-turn guard remains unchanged; this is a UX polish
  for long-running tasks. Focused source/report tests passed, the full safety
  suite passed 732/732, and the regenerated master report shows 732/732.
- Follow-up 0.1.439 workboard honesty polish:
  The overnight workboard no longer says the sandbox blocked a fresh Swift
  rebuild after Swift compile proof already passed. Its current-focus paragraph
  now says source-side Swift compile proof is green and the live bundle simply
  was not relaunched during the no-approval run. A standalone renderer
  regression covers this wording, the full safety suite passed 733/733, and the
  regenerated master report shows 733/733 with no stale 732/732 label.
- Follow-up 0.1.439 workboard scope polish:
  The workboard remaining-gap sentence no longer says the only product gap is
  proving Teams on Leo's real page. It now names the actual current scope:
  live relaunch plus real-device voice, music, browser, Teams, and app-control
  QA when Leo is present. A standalone renderer regression covers the broader
  wording, the full safety suite passed 734/734, and the regenerated report
  shows 734/734 with no stale 733/733 label.
- Follow-up 0.1.439 speech sanitizer polish:
  TTS now drops implementation-only checklist sections headed Actions, Next
  steps, technical details, debug, diagnostics, tool results, model details, or
  backend details, while keeping the surrounding answer. This directly targets
  the "Jarvis read all the logistics/actions aloud" bug class. Focused sanitizer
  and report tests passed, the full safety suite passed 735/735, and the
  regenerated master report shows 735/735 with no stale 734/734 label.
- Follow-up 0.1.439 speech sanitizer non-bulleted section polish:
  The internal-section stripper now treats Actions/debug/diagnostic/tool/model
  sections as whole sections even when their lines are not bullets. It skips
  those logistics until a blank line or a public heading such as Summary,
  Answer, Result, or Reply, then resumes with the actual answer. Focused
  sanitizer/report tests passed, the full safety suite passed 736/736, and the
  regenerated master report shows 736/736 with no stale 735/735 label.
- Follow-up 0.1.439 speech sanitizer natural-heading polish:
  The same internal-section stripping now also catches natural logistics
  headings such as What I did, Steps taken, Process, and Implementation notes.
  The public-heading cleanup now strips Answer, Result, and Reply labels from
  speech too, so Jarvis speaks the answer instead of label text. Focused
  sanitizer/report tests passed, the full safety suite passed 737/737, and the
  regenerated master report shows 737/737 with no stale 736/736 label.
- Follow-up 0.1.439 speech sanitizer reasoning-section polish:
  Standalone Reasoning, Rationale, and Notes sections are now treated as
  internal/logistical TTS sections, while inline public note text after Reply
  still stays speakable. Focused sanitizer/report tests passed, the full safety
  suite passed 738/738, and the regenerated master report shows 738/738 with no
  stale 737/737 label.
- Follow-up 0.1.439 fast-model prompt anti-internal-section polish:
  The fast-chat system prompt now tells the first model not to format normal
  visible/spoken replies with internal sections such as What I did, Steps
  taken, Reasoning, or Tool results. The instruction was kept short and placed
  after the speech-dictation policy so model-context previews still show the
  dictation rule and compact tool prompts stay under their size budgets.
  Focused prompt/report/size tests passed, the full safety suite passed 739/739,
  and the regenerated master report shows 739/739 with no stale 738/738 label.
- Follow-up 0.1.439 assistant-history internal-section cleanup:
  Assistant history passed back to the fast model now runs through the same
  internal-section stripper used for speech, plus public Answer/Result/Reply
  label cleanup. Old Jarvis replies with What I did, Steps taken, Reasoning, or
  tool-log sections no longer teach the next answer to imitate logistics.
  Focused history/report tests passed, the full safety suite passed 740/740,
  and the regenerated master report shows 740/740 with no stale 739/739 label.
- Follow-up 0.1.439 compact no-internal-headings prompt proof:
  The fast-chat system prompt now uses the compact exact rule "No internal
  headings: Actions, What I did, Steps taken, Reasoning, Notes, Tool results."
  so the first model gets the anti-logistics instruction without breaking prompt
  size budgets. The renderer contract now reports the matching proof count.
  Focused prompt/report/size tests passed, the full safety suite passed 741/741,
  and the regenerated master report shows 741/741 with no stale 740/740 label.

- Follow-up 0.1.439 readiness/preflight status-route cache:
  `JarvisServer` now keeps a shared 2-second cache for the heavy
  `/api/readiness` and `/api/preflight` payloads, and mode changes invalidate
  that cache immediately. Proof: full safety suite passed 759/759, live
  `verify_safe.py` passed 104/104 at
  `runtime/verification/verify-safe-20260616-221437.json`, warmed live
  `/api/readiness` improved from about 3231 ms average to about 382 ms average,
  warmed live `/api/preflight` improved from about 2478 ms average to about
  0.7 ms average, and cold calls after cache expiry measured about
  1.25-1.58 seconds instead of the old 2.26-4.39 seconds.

- Follow-up 0.1.439 shared system-status health cache:
  `JarvisServer.health()` now uses a shared 1-second `system_status()` cache,
  and the readiness/preflight builder reuses that same cached status snapshot
  instead of recomputing it. Proof: full safety suite passed 760/760, live
  `verify_safe.py` passed 104/104 at
  `runtime/verification/verify-safe-20260616-222651.json`, warmed live
  `/api/health` now sits around 2-13 ms after the first poll, warmed live
  `/api/readiness` sits around 1 ms after the first hit, and warmed live
  `/api/preflight` sits around 1 ms.

- Follow-up 0.1.439 compact first-model tool layer:
  The first fast-chat model no longer sees the full 66-tool catalog for casual
  conversation. Large first-layer tool lists are now compacted to a 29-tool
  core catalog with `tools.more` as the broader handoff path, and the model
  context preview now reflects that real compact layer. Core direct routes kept
  in the compact layer include email, LocalOS music play/stop, device, memory
  usage, calendar, browser open/read/search/bookmarks, app open/focus/status,
  price conversion, Codex chat/activity/job, permissions, overnight status, and
  stop speaking. Proof: targeted compact-catalog regressions passed, the full
  safety suite passed 762/762, live `verify_safe.py` passed 104/104 at
  `runtime/verification/verify-safe-20260616-225231.json`, and live
  `smoke_fast_latency.py` showed `hello Jarvis` first visible at about 0.78 s
  and the five-bullets prompt at about 0.866 s on one clean post-rebuild run.

- Current speed caveat:
  The worst live outlier seen after the compact-catalog patch was a
  `tell me a short joke` run that fell off Groq and landed on the local
  `qwen3:0.6b` fallback, producing about 6.08 s to first visible text and
  about 6.76 s total. The latest benchmark JSON showed backend `ollama`,
  model `qwen3:0.6b`, and model-reported total about 3.66 s, which strongly
  suggests a Groq timeout plus local fallback rather than a steady primary-path
  regression. Direct `_run_groq_fast_chat('tell me a short joke')` sampling
  during the same session returned 8/8 successful Groq completions at about
  0.60-2.07 s. Next likely speed lane: improve or smooth the `primary_timeout`
  fallback path so transient Groq misses do not collapse into slow local-Qwen
  replies.

- Follow-up 0.1.439 streamed Groq-miss fallback:
  The streaming fast-chat path now keeps Groq `http_error` and `network_error`
  fallbacks in a streamed Ollama lane instead of immediately collapsing into
  the old final non-stream fallback path. Tool-aware streaming fallbacks now
  prefer the configured middle model (`gpt-oss:120b-cloud`) before local Qwen,
  while no-tool streaming fallbacks can still stay on the lighter local model.
  Proof: targeted streamed fallback regressions passed, the full safety suite
  passed 763/763, and live `verify_safe.py` passed 104/104 at
  `runtime/verification/verify-safe-20260616-231530.json`.

- Return point:
  Next likely speed lane is deeper live benchmarking of transient Groq misses
  so the benchmark report shows `primary_status` and `fallback_trigger`
  directly, plus deciding whether the no-tool fallback should also prefer the
  cloud middle model or stay on local Qwen for raw speed.

- Follow-up 0.1.439 live regression matrix instrumentation:
  `scripts/voice_loop_qa.py` now timestamps streamed command events and stores
  `command_response_result` plus `stream_timing` in speech-audit reports.
  `scripts/run_regression_prompt_matrix.py` now surfaces backend/model route,
  fallback flags, compact-catalog usage, first visible/status/final/speech
  timing, and summary aggregates like fallback count, max first visible, and
  slowest case. `scripts/report_refresh.py` now runs the matrix payload through
  the same enrichment hook before rewriting `latest.json` and `latest.md`.
  Proof: focused matrix/voice-loop tests passed, the full safety suite passed
  765/765, and report surfaces refreshed cleanly. Important caveat: older saved
  matrix runs that predate this instrumentation still render `n/a` for those
  new timing/route cells because the historical per-case `report.json` files
  never recorded them; the next live matrix run will populate those fields.

- Follow-up 0.1.439 hidden `\tool(...)` stream leak:
  A fresh live matrix run showed the `sharpay_month` email-contact case was
  streaming visible deltas from the fast model before the later tool status,
  and the hidden machine call leaked a stray closing `)` into the visible text
  when the final parenthesis arrived in its own chunk. The fix lives in
  `_fast_chat_hidden_call_span()` plus the `_FastChatVisibleStreamBuffer`
  completion check so `\tool({...})` stays hidden until the wrapper really
  closes. Proof: targeted runtime-surface tests for split wrapper chunks passed,
  the bundled app was rebuilt and relaunched, the live raw stream for
  "Summarize all the emails from Ms. Sharpay in the past month." no longer
  shows the stray `)`, and the full safety suite passed 768/768.

- Follow-up 0.1.439 client-side late-status smoothing:
  The Swift `JarvisShellModel` no longer lets a late backend `status` event
  yank the UI back into a thinking state after streamed visible answer text has
  already started. In the `onStatus` closure, once `streamedReply` is non-empty
  the model records the latest status text but returns before changing the
  summon surface, placeholder bubble, or progress-nudge flow. This keeps the
  backend status route available for early speech while avoiding the visible
  "answering, then thinking again" wobble in the app. Proof: focused runtime
  surface tests passed, `swift build --package-path swift-shell` passed, the
  full safety suite passed 768/768, and the bundled app was rebuilt/relaunched
  with worker identity still matching the app bundle.

- Follow-up 0.1.439 structured tool-status refinement:
  Stream `status` events now carry explicit metadata for tool-routing
  refinements: `kind` and `replace_streaming_preview`. The Swift client
  decodes that richer event and, when a tool-request status arrives after a
  provisional streamed line has already shown up, it can replace the placeholder
  bubble text with the authoritative status instead of silently ignoring it or
  yanking the whole UI backward into a fresh thinking phase. This keeps fast
  first-visible feedback while letting prompt-aware status wording like
  "Checking your second email now." beat a more generic provisional stream.
  Proof: `swift build --package-path swift-shell` passed, targeted runtime and
  source-contract tests passed, the full safety suite passed 769/769, and the
  live `sharpay_month` regression probe passed at
  `runtime/regression_prompt_matrix/20260617-002941/summary.json` with
  `first_visible_seconds` about 1.104 s, `first_status_seconds` about 1.291 s,
  `first_final_seconds` about 5.344 s, and zero speech leaks.

- Follow-up 0.1.439 contact-alias inference hardening:
  Email contact inference now scores person-like sender matches more
  intelligently when Leo uses honorific aliases. The scorer now looks at the
  alias without the honorific, rewards exact lead-name matches and core-token
  subset matches, and penalizes obvious non-person senders whose lead token is
  a service brand like Microsoft or SharePoint. That lets `Ms Sharpay` resolve
  automatically to `Sharpay Cao 曹宗悦` from recent mail metadata, while making
  it harder for fake aliases like `Mr Teams` to auto-store `Microsoft Teams`.
  The voice-loop regression harness also gained `expect_visible_not_contains`,
  so the `sharpay_month` case can explicitly reject the old fallback lines about
  confirming the contact first or not summarizing an unrelated newest email.
  Proof: targeted contact-inference and harness tests passed, the full safety
  suite passed 771/771, `REPLACE_APP=1 swift-shell/scripts/build_and_launch_app.sh`
  rebuilt the bundled worker with matching app identity, and the live
  `sharpay_month` regression passed at
  `runtime/regression_prompt_matrix/20260617-005917/summary.json` with visible
  reply `- Sharpay Cao replied regarding the Talent Show host invitation,
  likely providing confirmation or discussing details.`

- Follow-up 0.1.439 native visible-screen probe packaging:
  The Teams/browser weak point is now easier to verify honestly. I moved
  `JarvisNativeOutlookReader` into a shared Swift target
  `JarvisMacNative`, added a new bundled executable
  `jarvis-visible-screen-probe`, and updated the app-bundle build script to
  package it inside `Jarvis.app`. This does not yet wire the probe into the
  prompt-matrix harness automatically, but it gives Jarvis a real native OCR
  capture tool outside the main window process so future Teams/Chrome follow-up
  checks can read the visible page with the same Apple Vision path the app
  already uses. Proof: `swift build --package-path swift-shell` passed, the
  full Python safety suite passed 772/772, and
  `REPLACE_APP=1 swift-shell/scripts/build_app_bundle.sh` rebuilt the bundle
  with `output/Jarvis.app/Contents/MacOS/jarvis-visible-screen-probe` present.

- Follow-up 0.1.439 Teams/browser regression follow-through:
  The Python voice-loop and prompt-matrix harnesses now have a real
  native visible-screen follow-up path for browser handoffs. For
  `teams.assignment`, `voice_loop_qa.py` can now:
  1. keep the primary plan tool/result,
  2. open the Teams URL in Google Chrome,
  3. run the bundled `jarvis-visible-screen-probe`,
  4. post the captured OCR text into `/api/screen/visible-text` with
     `suppress_speech=True`,
  5. audit the follow-up spoken payload offline, and
  6. require that the matrix row really reaches a useful
     `screen.visible_text` follow-up before it counts as passed.
  The first live proof is important: this stronger contract exposed that
  Chrome is currently landing on a profile/password page instead of usable
  Teams content. The live artifact at
  `runtime/regression_prompt_matrix/20260617-015423/summary.json`
  failed honestly with `visible_screen_follow_up_status:
  response_not_useful`, while preserving the initial visible reply
  about signed-in Chrome. The OCR capture showed:
  `Leo Is Even Cooler / Enter Password / Touch ID of Enter Password`.
  So the next real product task is not the harness anymore; it is getting
  the Chrome handoff onto an actually readable Teams page.

- Follow-up 0.1.439 Teams/browser permission truth:
  I patched the Swift app and the Python harness so authenticated Chrome
  handoff now reuses the running Chrome session through `/usr/bin/osascript`
  instead of Launch Services `open -a Google Chrome ...`. That launcher change
  is source-tested, Swift-compiled, and live-launched in build 439. The
  deeper blocker is now confirmed by direct proof, not guesswork:
  Chrome already has logged-in `https://teams.cloud.microsoft/` tabs open, but
  local Chrome page reads are denied by macOS Automation permission with
  `Access not allowed. (-1723)`. I added a direct loopback endpoint
  `/api/browser/read-page` plus Swift `readChromeActivePage(...)` follow-up
  support so Jarvis can prefer the clearer permission-aware browser reply
  before falling back to flaky OCR. Proof: `python3 -m unittest tests.test_safety`
  passed 776/776, `swift build --package-path swift-shell` passed, the bundled
  app relaunched on build 439, and a live POST to `/api/browser/read-page`
  returned `status: automation_not_allowed` with the honest spoken summary
  about needing Automation access to Google Chrome.

- Follow-up 0.1.439 Teams/browser proof polish + speed:
  The regression harness now treats the blocked Chrome page-read follow-up as
  the effective final visible result, so the live Teams artifact no longer
  hides behind the older `Opening your Teams bookmark...` line. It now shows
  `final_visible_tool: browser.read_page` with the visible reply
  `macOS is blocking Jarvis from controlling Google Chrome...`. The speech
  audit also captures that permission-aware spoken payload as the third spoken
  item, still with zero internal leaks. I also cut wasted retry time by
  limiting OCR retries to one attempt when the browser page-read already says
  Chrome Automation is blocked. Proof:
  `runtime/regression_prompt_matrix/20260617-025017/summary.json` showed the
  clearer blocker at 34.185s, and the tightened run at
  `runtime/regression_prompt_matrix/20260617-025519/summary.json` kept the
  same truthful blocker while dropping total case time to 22.49s.

- Follow-up 0.1.439 Teams/browser fast-fail reorder:
  I tightened the visible-screen follow-up path again so it checks the current
  `browser.read_page` result before it reopens or refocuses Chrome. That means
  a live `automation_not_allowed` result now stops immediately instead of
  wasting a few more seconds on a doomed Chrome-open step. I added focused
  tests proving both branches: blocked Chrome Automation now avoids the browser
  reopen entirely, while a merely unhelpful first read still opens Chrome and
  retries the page-read path before OCR. Proof: focused tests passed for the
  native permission probe, the voice-loop reorder, the regression blocked-case
  accounting, and the Swift permission footer contract; the live Teams matrix
  stayed honestly blocked at
  `runtime/regression_prompt_matrix/20260617-034336/summary.json`, but total
  case time dropped again from 21.073s to 18.676s while first final visible
  latency stayed about 0.89s and the follow-up no longer attempted to reopen
  Chrome (`browser_open_attempted: false`).

- Follow-up 0.1.439 native Chrome page-read route:
  I added a new bundled executable `jarvis-browser-page-probe` plus
  `JarvisNativeBrowserReader.swift`, then wired Python `browser.read_page`
  to prefer that native helper before any `/usr/bin/osascript` fallback.
  The helper still cannot actually read signed-in Chrome page text on this Mac
  because macOS returns `Access not allowed. (-1723)`, but the important change
  is that Jarvis now gets that answer through its own bundled native route and
  reports it honestly as a Chrome Automation block. I also corrected the nested
  `chrome_automation` payload so a blocked runtime read now surfaces
  `needs_automation_access` instead of a misleading optimistic preflight state.
  Proof: focused browser/package tests passed, the full safety suite passed
  786/786, direct live `/api/browser/read-page` calls now report
  `used_native_browser_probe: true`, and the final live Teams matrix at
  `runtime/regression_prompt_matrix/20260617-040143/summary.json` stayed
  honestly blocked with a clean permission reply, `browser_open_attempted:
  false`, follow-up duration `0.861s`, and total case time `18.536s`.

- Follow-up 0.1.439 Chrome status wording truth:
  After the native page-read work, the remaining user-facing mismatch was the
  old optimistic Chrome permission wording. I changed the native permission
  helper so a successful `AEDeterminePermissionToAutomateTarget` preflight is
  now labeled `preflight_ready` / `Preflight Ready` rather than plain `Ready`,
  and the detail now says Chrome page-read access is only proven after a live
  read succeeds. `browser.status` now reflects that same nuance in its reply.
  Proof: focused browser/permission tests passed, the live bundle relaunched,
  the full safety suite passed 786/786 again, and a direct local
  `browser_status()` call now says `Chrome Automation preflight is preflight
  ready; actual Chrome page-text access is only proven after a successful local
  page read.`

- Follow-up 0.1.439 login-gate truth in live Chrome page reads:
  I finished the next step after the native browser probe work: when the
  browser page-read fallback sees visible Chrome text like `Enter Password` or
  `Touch ID`, Jarvis no longer pretends that was useful page content. Source
  changes now surface a dedicated `visible_screen_login_gate` browser status,
  the voice-loop follow-up stops immediately on that state instead of chewing
  through pointless OCR retries, and the regression matrix records it as an
  honest `login_gate` failure rather than an environment block. Proof:
  `python3 -m unittest tests.test_safety` passed 791/791, build 439 relaunched
  from `output/Jarvis.app`, a direct live POST to `/api/browser/read-page`
  returned `status: visible_screen_login_gate` with the reply
  `I read Google Chrome, but it is showing a password or sign-in gate...`,
  and the targeted live matrix at
  `runtime/regression_prompt_matrix/20260617-043011/summary.json` failed 0/1
  for the correct reason with `blocking_reason: login_gate`, speech audit
  `3 payloads / 0 leaks`, final visible tool `browser.read_page`, and total
  case time `18.902s`.

- Follow-up 0.1.439 app-shell duplicate OCR retry removal:
  After tracing the Swift live command flow, I found that the app was still
  paying for a second native visible-screen pass in `runTeamsBrowserFollowUp`
  even when the earlier `browser.read_page` call had already used the backend
  visible-screen fallback and returned a terminal `visible_screen_login_gate`
  result. I added `browserPageReadShouldSkipVisibleScreenRetry(...)` so the
  app now returns the first browser response immediately in those cases instead
  of waiting through the extra OCR retry loop. Proof: the Swift source contract
  test passed, `swift build --package-path swift-shell` passed, the full Python
  safety suite still passed 791/791, and the rebuilt live bundle relaunched as
  `output/Jarvis.app` build 439.

- Follow-up 0.1.439 speech-audit matrix speedup:
  I parallelized the per-payload speech audit in `scripts/voice_loop_qa.py`
  with a small worker pool so TTS/STT verification no longer runs strictly
  one payload at a time. This keeps the same leak-detection and transcript
  checks, but it cuts the regression wait materially. I also tuned the one
  unit test that had assumed serial transcript-call order. Proof: focused
  speech/login/Swift contract tests passed, the full safety suite passed
  791/791 again, and the live one-case Teams regression matrix improved from
  `18.69s` at `runtime/regression_prompt_matrix/20260617-044522/summary.json`
  to `13.96s` at
  `runtime/regression_prompt_matrix/20260617-045025/summary.json` while keeping
  `speech_payload_count: 3`, `speech_leak_count: 0`, and the same honest
  `blocking_reason: login_gate`.

- Follow-up 0.1.439 stream final-speech deferral for Teams auto-follow-up:
  I removed the redundant middle spoken payload from the streamed
  `teams.assignment` lane without changing the direct command/API behavior.
  The planner now marks bookmark-ready Teams plans with
  `defer_stream_final_speech: true`, and `jarvis/server.py` turns that into a
  `deferred_to_follow_up` speech state only for streaming final events, so the
  app waits for the real browser/screen follow-up instead of speaking the
  intermediate Chrome handoff reply. Proof: focused new tests passed, the full
  safety suite passed `793/793`, the live app rebuilt and relaunched as
  `output/Jarvis.app` version/build `0.1.439 / 439`, and the targeted live
  Teams matrix at
  `runtime/regression_prompt_matrix/20260617-050330/summary.json` improved from
  `speech_payload_count: 3` / `13.96s` to `speech_payload_count: 2` /
  `9.462s` while still honestly failing with `blocking_reason: login_gate`.

- Follow-up 0.1.439 Swift Teams handoff cleanup and immediate OCR first attempt:
  I tightened the macOS shell path after the backend speech deferral work. The
  app no longer injects a redundant local `Reading Teams now.` chat bubble or
  spoken status once the initial `Opening Teams now.` acknowledgement has
  already happened, and `runNativeChromePageRead(...)` no longer burns an extra
  `client.mode()` round-trip before the Chrome page-read request. I also fixed
  `runNativeVisibleScreenReadWithRetry(...)` so attempt 1 happens immediately
  and only later retries wait for
  `teamsVisibleReadRetryDelayNanoseconds`. Proof: focused Swift source-contract
  tests passed, `swift build --package-path swift-shell` passed, the full
  safety suite still passed `793/793`, the bundle rebuilt at
  `output/Jarvis.app`, and a clean sequential
  `swift run --package-path swift-shell jarvis-menu-bar --self-test` passed.

- Follow-up 0.1.439 browser panel truth from final Chrome response:
  I wired the Swift shell to update `browserStatusText` from the actual final
  browser response instead of leaving the panel stuck on generic Chrome-handoff
  wording. The browser panel now switches to explicit statuses like
  `Chrome blocked by sign-in gate`, `Chrome needs Automation permission`,
  `Read current Chrome page`, or `Chrome page text unavailable` based on the
  final `browser.read_page` result and its `next_steps`. Proof: focused Swift
  contract test passed, `swift build --package-path swift-shell` passed, the
  full safety suite still passed `793/793`, the app rebuilt in
  `output/Jarvis.app`, and a clean sequential
  `swift run --package-path swift-shell jarvis-menu-bar --self-test` passed.

- Follow-up 0.1.439 browser panel next-step hint:
  I added `browserHintText` to the Swift shell and taught the browser panel to
  show a second line of guidance under the short browser status. That means the
  panel now keeps the explicit status (`Chrome blocked by sign-in gate`,
  `Chrome needs Automation permission`, etc.) while also surfacing the first
  concrete next step from the backend, instead of hiding that guidance only in
  the chat transcript. I also stopped the panel from overriding the model's
  browser status with a generic `Loading` label, so the stronger state remains
  visible. Proof: focused Swift contract test passed, `swift build --package-path swift-shell`
  passed, the full safety suite still passed `793/793`, the app rebuilt in
  `output/Jarvis.app`, and a clean sequential
  `swift run --package-path swift-shell jarvis-menu-bar --self-test` passed.

- Follow-up 0.1.439 browser status pinning against WebKit churn:
  After adding the browser status + hint line, I found that later WebKit
  navigation callbacks could still overwrite those stronger backend-derived
  states with generic `Chrome handoff active` / `Loaded ...` text. I added
  `browserStatusPinned` to the Swift shell so final browser states such as
  sign-in gate, Automation permission needed, successful page read, or page
  text unavailable stay pinned until a new browser action starts. Proof:
  focused Swift contract test passed, `swift build --package-path swift-shell`
  passed, the full safety suite still passed `793/793`, the app rebuilt in
  `output/Jarvis.app`, and a clean sequential
  `swift run --package-path swift-shell jarvis-menu-bar --self-test` passed.

- Follow-up 0.1.439 stale Chrome-open callback guard:
  There was still one asynchronous race left after browser-status pinning: the
  `NSWorkspace.shared.open(...completionHandler:)` callback inside
  `openURLInChrome(...)` could arrive later and overwrite a pinned blocked/read
  state with a stale generic `Opened in Chrome` message. I added a guard so the
  completion callback exits immediately whenever `browserStatusPinned` is
  already true. Proof: focused Swift contract test passed, `swift build --package-path swift-shell`
  passed, the full safety suite still passed `793/793`, the app rebuilt in
  `output/Jarvis.app`, and a clean sequential
  `swift run --package-path swift-shell jarvis-menu-bar --self-test` passed.

- Follow-up 0.1.439 browser panel loading indicator split from status:
  After the browser status + hint work, the panel still had one clarity issue:
  WebKit loading would override the real browser status with a generic
  `Loading` label. I changed `JarvisBrowserPanelView` so the truthful status
  text remains visible and a separate small loading indicator + `Loading` label
  appears beside it only while the embedded browser is actively loading. Proof:
  focused Swift contract test passed, `swift build --package-path swift-shell`
  passed, the full safety suite still passed `793/793`, the app rebuilt in
  `output/Jarvis.app`, and a clean sequential
  `swift run --package-path swift-shell jarvis-menu-bar --self-test` passed.

- Follow-up 0.1.439 regression matrix needs-action reporting:
  The live regression matrix was still technically honest but not very humane:
  a Teams run stopped by a Chrome sign-in wall showed `Passed: 0/1` and
  `Blocked: 0`, which made it read like a generic failure instead of a
  user-action gate. I updated `scripts/run_regression_prompt_matrix.py` so
  login-gated follow-up failures now surface `user_action_required`, the
  summary tracks `Needs action`, and the markdown row label becomes
  `needs action` instead of plain `fail` while environment problems still stay
  in the `blocked` bucket. Proof: the full safety suite passed `795/795`, the
  refreshed live artifact at
  `runtime/regression_prompt_matrix/latest.md` now shows `Needs action: 1`,
  and the Teams row reads `needs action` with
  `login_gate_visible:browser.read_page`.

- Follow-up 0.1.439 live window diagnostics vs lock screen:
  I chased a suspicious live symptom where the freshly launched `Jarvis.app`
  process reported healthy backend state but `System Events` and Computer Use
  both saw zero Jarvis windows. A full-screen screenshot showed the real cause:
  the Mac was sitting on the lock screen, so UI automation could not inspect
  the live app surface. To stop that from masquerading as a main-window bug, I
  added `--window-self-test` to `jarvis-menu-bar`. It launches a diagnostic
  app instance, records phased snapshots after opening the panel, after status
  helper startup, and after `model.refresh()`, and now also reports
  `session_locked` from `CGSessionCopyCurrentDictionary()`. Proof: `swift build --package-path swift-shell`
  passed, the full safety suite stayed green at `797/797`, and the bundled
  probe at
  `output/Jarvis.app/Contents/MacOS/jarvis-menu-bar --window-self-test`
  now exits nonzero while showing `panel_is_visible: true` together with
  `session_locked: true`, which correctly explains the false zero-window
  external checks.

- Follow-up 0.1.439 verifier/report lock-screen truthfulness:
  The first implementation of the bundled window probe was correct in concept
  but wrong in plumbing: `jarvis-menu-bar --window-self-test` exits quickly in
  the shell, but Python `subprocess.run(..., capture_output=True)` can hang on
  it for the full timeout. I fixed `scripts/verify_safe.py` to capture the
  bundled probe through a temp log file instead of a pipe, then threaded that
  result into `scripts/render_overnight_status.py` and
  `scripts/morning_status.py`. Proof: the full safety suite passed `802/802`,
  `python3 scripts/verify_safe.py` finished green at `105/105` with the new
  `output_bundle_window_self_test` row, and `python3 scripts/morning_status.py`
  now prints `Window probe: session locked; bundled window probe created the
  Jarvis panel, but live foreground visibility is blocked by the lock screen`.

- Overnight Jun 18 2026 checkpoint / Chrome cleanup promise:
  Leo explicitly asked that before the 6:30 AM report I close Chrome tabs
  created by Codex/Jarvis, while preserving his personal tabs. Known cleanup
  targets from this run are the LocalOS Music Player test tabs, especially
  `http://127.0.0.1:8787/localFiles/HTMLfiles/!musicPlayer.html` and the older
  `file://.../!musicPlayer.html` tab if still open. Do not close Leo's YouTube
  or normal New Tab unless it is clearly created by this run.

- Follow-up 0.1.450 LocalOS music bridge truth:
  The live LocalOS music failure had three layers. First, the host-served music
  URL was fixed to `http://127.0.0.1:8787/localFiles/HTMLfiles/!musicPlayer.html`.
  Second, Jarvis lacked `OPTIONS` CORS handling, so Chrome preflight blocked
  LocalOS snapshot POSTs before Jarvis ever saw them; 0.1.450 now answers
  LocalOS music CORS preflight and the heartbeat snapshot became fresh again.
  Third, Chrome still blocks autoplay until a real user gesture. I fixed the
  broken activation AppleScript (`end if`, not `end tell`), added a LocalOS
  global Space-key play/pause handler, and added a System Events Space fallback.
  Live proof: the bridge heartbeat refreshed within seconds and playback
  commands reached LocalOS, but macOS currently blocks the fallback with
  `osascript is not allowed to send keystrokes (1002)`. So Jarvis no longer
  lies, but true hands-free LocalOS playback still needs either the proper
  Accessibility/keystroke permission path or a native LocalOS playback surface.

- Follow-up 0.1.450 LocalOS music activation-required wording:
  I changed the LocalOS Music Player so Chrome autoplay blocks publish
  `activation_required` instead of plain `failed`, and Jarvis now preserves
  that state through the bridge/direct fallback parser. User-facing wording is
  now: Jarvis found the song, queued it in LocalOS, and LocalOS needs one click
  before Chrome can start audio. This keeps ownership honest and prevents the
  old hidden `afplay` fallback from returning. Proof: the focused LocalOS music
  tests passed, then the full `tests.test_safety` suite passed `820/820`.

- Follow-up 0.1.452 Chrome cleanup helper:
  Leo asked that Codex/Jarvis-created Chrome tabs be closed before the 6:30 AM
  Jun 18 report. I added `scripts/cleanup_chrome_test_tabs.py`, a dry-run by
  default helper that targets only the LocalOS music-player HTTP URL and the
  older LocalOS music-player file URL, preserving YouTube, New Tab, reports,
  and normal personal tabs. Focused tests prove target matching, dry-run
  behavior, and execute behavior. Live dry-run at 02:48 CST found
  `target_count: 0`. The 6:30 handoff should run it again with `--execute`
  before refreshing the final report.

- Follow-up 0.1.453 LocalOS music ownership and reopen fix:
  Removed the dead `_play_localos_music_native_fallback` path so Jarvis can no
  longer start LocalOS music through hidden `afplay`; Stop Music still keeps
  the orphaned-afplay cleanup path for old builds. The recent-open LocalOS
  music marker now checks whether Chrome still has a music-player tab before
  refusing to reopen, fixing the case where cleanup closed the tab but Jarvis
  still thought it had recently opened LocalOS. Live bundle rebuilt and
  launched as Jarvis 0.1.453 build 453. Proof: focused music tests passed,
  full safety suite passed 826/826, no-prompt verifier passed 12/12, safe
  verifier passed 105/105, and Chrome cleanup execute found 0 target tabs.

- Follow-up 0.1.453 live speech-route music proof:
  The reusable matrix on Jarvis 0.1.453 passed 7/8 with the same Teams/Chrome
  environment blocker and 0 speech leaks. The LocalOS music row routed to
  `localos.music_play` with audio actions suppressed and answered honestly. A
  direct no-permission voice-loop for `Hey Jarvis, play Waving Through a
  Window.` passed with routed command `play waving through a window`, tool
  `localos.music_play`, visible reply matching spoken transcript at 0.963,
  no `afplay`/tool-time leak, and no live playback requested or observed.

- Follow-up 0.1.453 post-launch reliability sweep:
  Fresh speed smoke passed after the 0.1.453 launch: first visible 0.006s for
  hello, 0.607s for a short joke, and 0.940s for five speed bullets.
  Conversation-context smoke passed in 1.25s and used prior history.
  Wake-threshold smoke passed 10/10 at threshold 0.860 with the closest reject
  still below threshold. Runtime hygiene showed exactly one menu app, one status
  helper, one worker, and no afplay/say/Piper playback leftovers.

- Overnight Jun 19 2026 checkpoint / pre-build proof gate:
  Current live app is still Jarvis 0.1.454 build 454, but the quality gate is
  now much stronger. Commits `2f6d9fb`, `8c3b36f`, and `f1a114b` put the 8/8
  full-loop proof in the overnight headline, add `scripts/pre_build_gate.py`,
  and link that gate in the master report. The gate runs the Python safety
  suite, all eight spoken-command full-loop cases, Chrome test-tab cleanup, and
  report refresh. Latest real gate pass:
  `runtime/pre_build_gate/20260619-005023/summary.json`, 4/4 steps, 173.764s.
  Latest full safety suite is 858/858. The morning report currently shows source
  commit `f1a114b`, worktree clean, 8/8 full-loop, and links to the pre-build
  proof artifacts. Next priorities: keep this gate green, keep cleaning
  Codex/Jarvis-created Chrome tabs before morning, and continue converting Leo's
  dream prompts into dedicated full-loop cases instead of ad hoc manual tests.

- Overnight Jun 19 2026 checkpoint / Music auto-start regression hardening:
  The Music full-loop case no longer fails before exercising Jarvis when the
  native Music bridge preflight is down. Preflight is now recorded as evidence,
  then the actual voice-loop command runs so Jarvis's auto-start path has to
  prove playback. Added a unit test for the down-preflight scenario. Proof:
  focused tests passed, full `tests.test_safety` passed 861/861, and the real
  pre-build gate passed 4/4 at
  `runtime/pre_build_gate/20260619-012728/summary.json` in 171.563s. The
  full-loop step passed all eight cases; Music took 11.575s and the email
  Sharpay case took 51.006s, which is currently the slowest case to improve.

- Overnight Jun 19 2026 checkpoint / full-loop latency budgets:
  Added explicit latency budgets to every full-loop case so a test cannot pass
  merely by eventually succeeding. Budgets are currently generous enough for
  real local variance: Music 30s, RAM 30s, Calendar 30s, Magic Keyboard/yuan
  45s, Gemma plan 35s, Codex plan 35s, Teams assignment honesty 45s, and
  Sharpay month email 75s. Proof: full `tests.test_safety` passed 863/863 and
  the real pre-build gate passed 4/4 at
  `runtime/pre_build_gate/20260619-013451/summary.json` in 158.133s. Full-loop
  case timings were: Music 16.015s, RAM 6.55s, Calendar 9.66s, Magic Keyboard
  21.458s, Gemma 11.035s, Codex 10.395s, Teams 12.046s, Sharpay email 45.327s.

- Overnight Jun 19 2026 checkpoint / bounded Sharpay proof scan:
  The real user-facing email tool still defaults to the configured 250-message
  recent scan, but `outlook_read_only_check` now accepts an explicit bounded
  `scan_limit_override` for proof harnesses. The Sharpay full-loop proof uses
  `limit=1` and `scan_limit_override=75` after the voice-loop command has
  already proven the visible/spoken answer, avoiding a second unconditional
  full-window scan. Proof: focused email tests passed, full `tests.test_safety`
  passed 865/865, and the real pre-build gate passed 4/4 at
  `runtime/pre_build_gate/20260619-015008/summary.json` in 160.75s. Latest
  full-loop timings: Music 12.18s, RAM 7.117s, Calendar 13.821s, Magic Keyboard
  21.936s, Gemma 15.218s, Codex 6.936s, Teams 11.369s, Sharpay email 46.624s.

- Overnight Jun 19 2026 checkpoint / canonical full-loop latest protection:
  Focused `scripts/full_loop_regression.py --case ...` runs now write their own
  timestamped summary but do not overwrite `runtime/full_loop_regression/latest.*`.
  Only `--case all` runs are marked `canonical_latest=true`, and
  `scripts/report_refresh.py` filters full-loop candidates so focused one-off
  runs cannot accidentally downgrade the morning report to 1/1. Proof: focused
  writer/refresh tests passed, a real focused email run left latest total at 8,
  full `tests.test_safety` passed 868/868, and the pre-build gate passed 4/4 at
  `runtime/pre_build_gate/20260619-015829/summary.json` in 161.854s. Latest
  canonical full-loop timings: Music 11.582s, RAM 15.104s, Calendar 14.015s,
  Magic Keyboard 22.527s, Gemma 11.014s, Codex 10.859s, Teams 11.733s, Sharpay
  email 39.276s.

- Overnight Jun 19 2026 checkpoint / report truth and email proof speed:
  Commits `0c53a5c`, `db8c11b`, and `b6385fa` corrected the morning report so
  native Music app bridge playback is the proven primary path, made the Sharpay
  full-loop proof reuse Jarvis's own redacted command-result metadata before
  falling back to a direct Mail rescan, and added total full-loop
  `duration_seconds` to canonical summaries and the report proof line. Proof:
  full `tests.test_safety` passed 870/870, focused email run used
  `proof_source=voice_loop_command_result`, and the real pre-build gate passed
  4/4 at `runtime/pre_build_gate/20260619-022927/summary.json`. The report now
  shows source commit `b6385fa`, worktree clean, Python 870/870, and latest
  full-loop total runtime. Next priorities: keep reducing the slowest
  user-facing cases, keep the native Music bridge as the primary playback proof,
  close only Jarvis/Codex-created Chrome tabs before morning, and avoid letting
  focused one-off runs replace canonical all-case proof.
## 2026-06-19 03:34 CST - report proof counter / Chrome cleanup lane
- Current task: cleanup/report trust pass after the Sharpay, Music cleanup, and spoken-payload QA fixes.
- Found and fixed report-only bug: running `scripts/render_overnight_status.py` as a script could show `1/1 Python tests` because unittest saw a failed import stub. The renderer now inserts `PROJECT_ROOT` into `sys.path` and treats a `_FailedTest` singleton as zero instead of proof.
- Verified current report text says `8/8 passed full-loop real-action checks, 876/876 Python tests`.
- Chrome cleanup dry-run at 03:31 CST found 0 target LocalOS music-player tabs. The helper remains allowlisted to only the LocalOS music player URL/file path, not Leo's ordinary Chrome tabs.

## 2026-06-19 03:42 CST - Teams visible-screen honesty upgrade
- Current task completed: full-loop Teams assignment proof no longer stops at Chrome Automation permission if native visible-screen OCR can still read the frontmost Chrome window.
- `scripts/voice_loop_qa.py` now tries one visible-screen OCR attempt after a Chrome page-read permission block, without opening extra tabs when the current Chrome state is already available.
- `assignment_subject_mismatch` and `login_gate_visible` statuses from visible-screen summaries are preserved instead of being flattened back into `browser_permission_blocked`.
- `scripts/full_loop_regression.py` now judges Teams honesty from the combined first reply plus visible-screen follow-up reply. Live focused proof: `runtime/full_loop_regression/20260619-034146/summary.json` passed and recorded `follow_up_status=assignment_subject_mismatch`, `honest_wrong_subject=true`, and the visible Geography assignment text. Cleanup closed 0 new Teams tabs.

## 2026-06-19 03:48 CST - post-Teams-patch official gate
- Official pre-build gate passed 4/4 at `runtime/pre_build_gate/20260619-034519/summary.json` in 143.616s.
- Full safety suite step passed 879/879 in 25.349s. Full-loop step passed 8/8 in 117.867s.
- Latest canonical full-loop run `runtime/full_loop_regression/20260619-034545/summary.json` passed all eight cases in 117.585s. Teams case passed in 16.195s with `follow_up_status=assignment_subject_mismatch`; Sharpay email was the slowest remaining case at 31.715s.
- Chrome cleanup step passed and closed 0 test tabs.

## 2026-06-19 03:50 CST - Music hidden-audio guard
- Current task completed: the Music full-loop case now snapshots `afplay` before playback and after cleanup, writes `afplay-before.json`, and fails if any new hidden `afplay` process survives cleanup.
- This is specifically for Leo's mystery-audio complaint: a green Music case can no longer leave a new Jarvis-started `afplay` process running behind the visible Music app bridge.
- Focused tests passed, and live focused Music proof `runtime/full_loop_regression/20260619-034949/summary.json` passed with `verified_stopped=true`, `afplay_processes_after=[]`, and `new_afplay_processes_after=[]`.

## 2026-06-19 03:57 CST - official gate after hidden-audio guard
- Official pre-build gate passed 4/4 at `runtime/pre_build_gate/20260619-035329/summary.json` in 165.347s.
- Full safety suite step passed 881/881 in 25.178s. Full-loop step passed 8/8 in 139.769s.
- Latest canonical full-loop run `runtime/full_loop_regression/20260619-035354/summary.json` includes Music `new_afplay_processes_after=[]` and Teams `follow_up_status=assignment_subject_mismatch`.

## 2026-06-19 04:08 CST - Teams final speech payload fix
- Current task completed: Teams assignment honesty now audits the final visible-screen mismatch response, not only the first "Opening Teams now" status line.
- `scripts/voice_loop_qa.py` treats `assignment_subject_mismatch` and `login_gate_visible` follow-ups as auditable final payloads, even when they are not marked as a successful useful page read.
- `jarvis/tools.py` now provides a short voice-safe `spoken_summary` for wrong-subject Teams screens: "I can see Lesson 2: The Geography of Greece Group Assignment, but it does not look like the Music assignment."
- Live proof after rebuilding `output/Jarvis.app`: focused Teams run `runtime/full_loop_regression/20260619-040451/summary.json` passed in 17.626s with `payload_count=2` in its voice-loop report.
- Full `tests.test_safety` passed 882/882 after the rebuild. Next step: run a fresh official pre-build gate when enough time remains, then keep improving the slowest full-loop cases and morning report polish.

## 2026-06-19 04:11 CST - official post-speech-fix gate
- Official gate passed 4/4 at `runtime/pre_build_gate/20260619-040832/summary.json` in 151.404s after commit `56a8a87`.
- Full-loop step passed all eight real-action cases in 126.147s. Notable timings: Magic Keyboard/yuan 20.911s, Teams assignment honesty 17.779s, Sharpay month email 37.818s.
- Chrome cleanup executed and found 0 target tabs. Report refresh updated `runtime/overnight_status/report.html` and `runtime/overnight_status/index.html`.
- Current next priorities: reduce slowest case latency, inspect morning report for concise product language, and keep the 06:30 report/Chrome-cleanup deadline visible.

## 2026-06-19 04:16 CST - morning report product polish
- Current task completed: the master report now shows a visible `Tonight's Shipped Highlights` section with seven high-value bullets, then keeps the long historical shipped list under a collapsed `Full Shipped Archive`.
- This keeps Leo's 06:30 report product-announcement-like while preserving the deep changelog for audit/search.
- Focused renderer contract passed, then full `tests.test_safety` passed 882/882.

## 2026-06-19 04:22 CST - Sharpay email speedup
- Current task completed: Apple Mail sender-recent summaries now collect candidate indexes during the first bounded scan instead of rescanning the same 75-message window for every selected message.
- This preserves the 75-message scan window and all matching-sender proof, but live Sharpay focused full-loop improved from 37.818s to 22.677s.
- Mail read time inside the voice loop dropped from 20.148s to 8.449s while still reporting `scanned_count=75`, `match_count=12`, `message_count=5`, and `selection_mode=sender_recent`.
- Proof: `runtime/full_loop_regression/20260619-041950/summary.json`; full `tests.test_safety` passed 882/882.

## 2026-06-19 04:26 CST - official post-email-speedup gate
- Official gate passed 4/4 at `runtime/pre_build_gate/20260619-042259/summary.json` in 152.873s after commit `a5ca3df`.
- Full-loop step passed 8/8 in 127.08s. Email Sharpay took 25.821s in the all-case gate; latest full-loop total was 126.736s.
- Chrome cleanup executed and found 0 target tabs. Report refresh completed.
- Current next priorities: watch Teams/Music timing variance, keep report concise, run final cleanup/report refresh before 06:30 CST, and keep the worktree clean between changes.

## 2026-06-19 04:30 CST - Jarvis 0.1.455 live identity
- Current task completed: the packaged app default version/build was bumped to Jarvis 0.1.455 build 455 so Leo sees a fresh version number for the overnight reliability pass.
- Live build proof: `swift-shell/scripts/build_and_launch_app.sh` launched `output/Jarvis.app`, and `/api/health` reported `version=0.1.455`, `build=455`, and `worker_launch_matches_bundle=true`.
- Full `tests.test_safety` passed 882/882 after the version bump.

## 2026-06-19 04:33 CST - official live 0.1.455 gate
- Official gate passed 4/4 at `runtime/pre_build_gate/20260619-043002/summary.json` in 157.426s on live Jarvis 0.1.455 build 455.
- `/api/health` confirmed `version=0.1.455`, `build=455`, `worker_launch_matches_bundle=true`, and `worker_source_kind=bundled app resources`.
- Full-loop step passed 8/8 in 131.866s. Case timings: Music 15.851s, RAM 15.349s, Calendar 9.975s, Magic Keyboard/yuan 21.8s, Gemma 10.546s, Codex 5.815s, Teams 20.736s, email Sharpay 31.309s.
- Chrome cleanup executed and found 0 target tabs; report refresh completed.

## 2026-06-19 04:40 CST - behavior matrix Teams honesty fix
- Current task completed: the older regression prompt matrix now treats `assignment_subject_mismatch` from `screen.visible_text` as a successful honest Teams follow-up instead of a failure.
- Focused live Teams matrix passed at `runtime/regression_prompt_matrix/20260619-043726/summary.json`.
- Full live matrix passed 8/8 at `runtime/regression_prompt_matrix/20260619-043745/summary.json` with 16 speech payloads, 0 leaks, max first-visible 0.976s, and Teams duration 6.333s.
- Full `tests.test_safety` passed 884/884.

## 2026-06-19 04:44 CST - current official baseline
- Official gate passed 4/4 at `runtime/pre_build_gate/20260619-044054/summary.json` in 146.047s on live Jarvis 0.1.455.
- Full-loop step passed 8/8 in 120.845s. Notable timings: Teams 8.875s, Magic Keyboard/yuan 16.562s, Codex 14.44s, email Sharpay 33.944s.
- Chrome cleanup executed and found 0 target tabs; report refresh completed. Worktree was clean before recording this memory note.

## 2026-06-19 04:52 CST - behavior matrix canonical latest guard
- Current task completed: focused behavior-matrix runs no longer overwrite the global `runtime/regression_prompt_matrix/latest.json` / `latest.md` pointers used by the master report.
- `scripts/run_regression_prompt_matrix.py` now writes timestamped focused artifacts plus per-run `latest.json`/`latest.md`, but only all-eight canonical runs update the matrix root latest files.
- `scripts/report_refresh.py` filters regression-matrix backfills through `run_regression_prompt_matrix.is_canonical_summary`, and `scripts/render_overnight_status.py` falls back to the newest canonical all-eight summary if a stale focused latest ever appears.
- Full `tests.test_safety` passed 885/885. Live focused Teams matrix `runtime/regression_prompt_matrix/20260619-045022/summary.json` passed 1/1 with `Canonical latest: no`; global latest remained the 8/8 canonical run at `runtime/regression_prompt_matrix/20260619-043745`.
- User-facing result: Leo can run one-case matrix probes during debugging without the morning report falsely shrinking from the real all-prompt proof to a single focused test.

## 2026-06-19 04:55 CST - post-canonical-guard official gate
- Official pre-build gate passed 4/4 at `runtime/pre_build_gate/20260619-045148/summary.json` after commit `2c037d2`.
- This gate re-ran the full proof stack after the focused-matrix latest guard, confirming the report-refresh filter did not break the normal all-case canonical path.
- Next priorities: keep reducing live full-loop flakiness/latency, run final Chrome cleanup before the 06:30 report, and keep the report surface concise and product-like.

## 2026-06-19 05:05 CST - Sharpay email summary completeness polish
- Current task completed: email summaries now merge deterministic local form/link context into model summaries when the model omits those form messages.
- Repeated feedback-form/link messages collapse into one voice-friendly count sentence, e.g. "gave two links to feedback forms that you may need to fill in", with raw URLs still suppressed.
- Full `tests.test_safety` passed 887/887. Rebuilt live `output/Jarvis.app` and focused email proof `runtime/full_loop_regression/20260619-050444/summary.json` passed in 27.382s; speech STT confirmed the final spoken answer included the two feedback-form links as one aggregate sentence.

## 2026-06-19 05:09 CST - post-email-summary official gate
- Official gate passed 4/4 at `runtime/pre_build_gate/20260619-050648/summary.json` after commit `b79650f`.
- Full-loop step passed 8/8 in 120.758s. Case timings: Music 15.808s, RAM 10.967s, Calendar 14.227s, Magic Keyboard/yuan 15.943s, Gemma 11.0s, Codex 10.219s, Teams 16.214s, email Sharpay 25.883s.
- Cleanup closed 0 target tabs and report refresh completed.

## 2026-06-19 05:16 CST - full-text voice-loop proof fields
- Current task completed: voice-loop QA reports now keep full `visible_reply`, full `reply_expected_text`, and full per-item `spoken_text` beside their compact preview fields.
- This fixes the debugging gap where long Sharpay email answers proved the spoken transcript included the final feedback-form sentence, but the JSON preview cut off before that ending.
- Focused email proof `runtime/full_loop_regression/20260619-051446/summary.json` passed and showed all three full-text fields ending with the feedback-form sentence. Full `tests.test_safety` passed 888/888.

## 2026-06-19 05:20 CST - post-full-text-QA official gate
- Official gate passed 4/4 at `runtime/pre_build_gate/20260619-051641/summary.json` after commit `8e0c1f2`.
- Full-loop step passed 8/8 in 105.988s. Case timings: Music 11.734s, RAM 6.565s, Calendar 9.912s, Magic Keyboard/yuan 13.33s, Gemma 15.462s, Codex 14.277s, Teams 13.262s, email Sharpay 21.005s.
- Cleanup closed 0 target tabs and report refresh completed.

## 2026-06-19 05:22 CST - Jarvis 0.1.456 live package
- Current task completed: bundle defaults now package Jarvis 0.1.456 build 456.
- Live build proof: `swift-shell/scripts/build_and_launch_app.sh` launched `output/Jarvis.app`, and `/api/health` reported `version=0.1.456`, `build=456`, and `worker_launch_matches_bundle=true`.
- Full `tests.test_safety` passed 888/888 after the version bump.

## 2026-06-19 05:25 CST - official live 0.1.456 gate
- Official gate passed 4/4 at `runtime/pre_build_gate/20260619-052222/summary.json` on live Jarvis 0.1.456 build 456.
- Full-loop step passed 8/8 in 115.953s. Case timings: Music 7.497s, RAM 10.115s, Calendar 5.546s, Magic Keyboard/yuan 16.021s, Gemma 15.307s, Codex 10.014s, Teams 20.037s, email Sharpay 30.924s.
- Chrome cleanup executed and found 0 target tabs; report refresh completed.

## 2026-06-19 05:27 CST - live 0.1.456 behavior matrix
- Canonical behavior matrix passed 8/8 at `runtime/regression_prompt_matrix/20260619-052531/summary.json` on live Jarvis 0.1.456.
- Matrix proof: 16 speech payloads, 0 leaks, max first-visible 0.703s. Slowest case was `sharpay_month` at 21.792s.
- Because this was canonical, it updated `runtime/regression_prompt_matrix/latest.json` instead of being filtered as a focused probe.

## 2026-06-19 16:20 CST - music playback status honesty lane
- Current task stack: top-level Jarvis hardening -> Music playback ownership/status -> planner-facing wording for uncertain playback.
- Immediate subtask: prevent the planner status line from sounding successful when LocalOS/Music only accepted, queued, or failed to confirm playback.
- User-facing reason: Leo has repeatedly hit cases where Jarvis says or implies music is playing while no sound starts. Even when backend results are honest, the visible status must not over-sell uncertain playback.
- Source changes in progress: `jarvis/planner.py` now reserves "Started Local OS Music playback" for confirmed `playing`; accepted/not-confirmed paths say playback did not start or is not confirmed. `JARVIS_BUG_BACKLOG.md` and the overnight report shipped list now record this lane.
- Proof so far: focused planner tests for no-track, accepted-without-audio, queued/bridge-not-polling, confirmed playing, and Your Pick queued wording passed 5/5.
- Completed proof: full `tests.test_safety` passed 896/896, live Jarvis 0.1.465 build 465 passed app self-test 105/105, and official pre-build gate passed 4/4 at `runtime/pre_build_gate/20260619-162808/summary.json`. Commit: `1fd6073 Make uncertain music playback status honest`.

## 2026-06-19 16:36 CST - optional Notifications missing-list fix
- Current task stack: top-level Jarvis hardening -> Permissions UX -> native permission status reply wording.
- Immediate subtask: make the detailed `Missing:` line use the same blocking-permission filter as the `App perms` chip, so optional Notifications are not presented as required missing access.
- User-facing reason: Leo previously saw Jarvis say Notifications were missing even though macOS had not been asked for notification access. That makes the app look broken and sends Leo to System Settings for something optional.
- Completed proof: `JarvisShellModel.permissionStatusReply` filters missing permissions with `!isReady && isBlocking`, full `tests.test_safety` passed 896/896, live Jarvis 0.1.466 build 466 passed app self-test 105/105, and official gate passed 4/4 at `runtime/pre_build_gate/20260619-164245/summary.json`. Commit: `cbc9869 Keep optional notifications out of missing permissions`.

## 2026-06-19 16:55 CST - contact-memory privacy clarity lane
- Current task stack: top-level Jarvis hardening -> Email/contact memory -> privacy-safe metadata disclosure.
- Immediate subtask: make every contact-memory tool result say whether private metadata or email bodies were read, instead of overloading `read_private_content`.
- User-facing reason: Leo wants Jarvis to learn names like "Ms. Sharpay", but it must be clear that the inference uses recent Mail sender metadata only, not email bodies, and stores the alias locally.
- Completed proof: contact status/lookup/remember now report `read_private_metadata=false` and `read_email_content=false`; email-based inference reports `read_private_metadata=true`, `read_email_content=false`, and a short metadata privacy note. Focused contact/version tests passed 4/4, full `tests.test_safety` passed 896/896, live Jarvis 0.1.467 build 467 self-check returned `ok=true`, and official gate passed 4/4 at `runtime/pre_build_gate/20260619-165406/summary.json`.

## 2026-06-19 17:00 CST - stale working-row proof lane
- Current task stack: top-level Jarvis hardening -> UI task honesty -> stale progress rows after completed turns.
- Immediate subtask: prove the old "Opened Outlook, then Still working" class is guarded by Swift lifecycle tests instead of only being mentioned in report copy.
- User-facing reason: Leo should never see Jarvis finish a task and then append another "Still working" bubble from the old turn.
- Completed proof: added a regression that requires progress nudges to stop before final answer display and before the busy flag is cleared in deferred cleanup; backlog is reclassified to partially fixed/proved for stale progress rows while keeping music/Teams task-honesty subcases separate. Focused Swift lifecycle tests passed 3/3 and full `tests.test_safety` passed 897/897.

## 2026-06-19 17:07 CST - menu-bar Stop Music visibility lane
- Current task stack: top-level Jarvis hardening -> Music emergency controls -> menu-bar Stop Music feedback.
- Immediate subtask: make the helper-owned menu-bar Stop Music action leave visible evidence in the main Jarvis app when the app is running.
- User-facing reason: Leo complained that mystery audio could keep playing and he needed a menu-bar brake he could trust. A silent helper-only stop is less reassuring than a visible Jarvis result row.
- Completed proof: `jarvis-status-helper` now posts a dedicated `local.leo.jarvis.statusHelper.stopMusic` notification after sending the backend stop command; the main app observes that notification and runs `model.stopMusic()` so the Jarvis window records the stop result. Jarvis 0.1.468 build 468 launched with worker identity matched. Focused menu tests passed 2/2, Swift status-helper/menu-bar self-tests passed, full `tests.test_safety` passed 897/897, focused Gemma full-loop passed after verifier repair, and official gate passed 4/4 at `runtime/pre_build_gate/20260619-171659/summary.json`.
- Gate issue resolved: the first 0.1.468 live gate failed only the Gemma model-plan row because the verifier accepted `remote_macbook_air` wording but rejected the safe `ask_before_local` lane when the MacBook Air was unreachable. Product behavior was safe; verifier now accepts "ask before running ... on this Mac" when remote status is unavailable and no model ran.

## 2026-06-19 17:23 CST - daily utility full-loop backlog reclassification
- Current task stack: top-level Jarvis hardening -> backlog accuracy -> Calendar/RAM/Gemma user-prompt evidence.
- Immediate subtask: stop carrying Calendar, RAM, and Gemma prompts as open/unknown when the current live full-loop gate proves them.
- Completed proof source: official gate `runtime/pre_build_gate/20260619-171659/summary.json` passed 4/4 on Jarvis 0.1.468; embedded full-loop summary `runtime/full_loop_regression/20260619-171754/summary.json` passed 8/8. Relevant rows: `calendar_today_schedule` passed in 13.223s with `source=calendar_sqlite_cache` and `changed_calendar=false`; `ram_activity_monitor` passed in 10.985s with normal memory pressure; `gemma_model_plan` passed in 27.462s with `ran_model=false`, `preferred_lane=ask_before_local`, and `remote_status=unavailable`.

## 2026-06-19 17:25 CST - Chrome bookmark import backlog reclassification
- Current task stack: top-level Jarvis hardening -> Browser/Chrome/Teams -> Chrome bookmarks import proof.
- Immediate subtask: stop carrying Chrome bookmark import as open/unknown when the local toolchain and snapshot already exist.
- Current evidence: `jarvis/tools.py` exposes `browser.bookmarks_import`, `browser.bookmarks_search`, and `browser.bookmark_open`; `runtime/integrations/chrome_bookmarks.json` currently has 23 imported links from 3 Chrome profiles; focused tests prove import/search/open-plan behavior is local, planned-only for opening, and chooses signed-in Chrome for Teams/authenticated URLs without migrating Chrome cookies or sessions.

## 2026-06-19 17:26 CST - visible Jarvis browser panel backlog reclassification
- Current task stack: top-level Jarvis hardening -> Browser/Chrome/Teams -> visible/interactable in-app browser surface.
- Immediate subtask: stop carrying the in-app browser surface as open/unknown when Swift already embeds a WebKit panel.
- Current evidence: `JarvisPanelView` displays `JarvisBrowserPanelView` when `model.isBrowserVisible`; `JarvisBrowserPanelView` wraps a `WKWebView` with address field, Go, back/forward/reload, hide, loading state, status text, and Open Chrome/Open Signed-In Chrome controls. Tests cover browser status, ordinary WebKit URL lane, authenticated Chrome handoff, and the Swift panel/Chrome handoff source contract.

## 2026-06-19 17:34 CST - speech barge-in proof lane
- Current task stack: top-level Jarvis hardening -> Speech safety -> stop current Jarvis speech when Leo starts speaking.
- Immediate subtask: prove the wake-listener transcript path feeds the barge-in speech stop path instead of only testing isolated string matching.
- Current evidence: `onStateChange` calls `handleSpeechBargeInIfNeeded(transcript: snapshot.transcript)`, the handler checks the latest likely speech window, ignores short fragments/echo/captured wake commands, records `speech_barge_in`, clears the speech playback window, and calls `client.stopSpeaking()`. The backlog keeps the remaining risk that this needs Hey Jarvis/listening active and a usable macOS STT transcript.

## 2026-06-19 17:38 CST - Hey Jarvis always-listening proof lane
- Current task stack: top-level Jarvis hardening -> Wake listener reliability -> keep listening after Start until Leo explicitly stops it.
- Immediate subtask: prove the listener does not treat recognition endings, command capture, or restart storms as a reason to stay off.
- Current evidence: `shouldKeepRunning` stays true after Start, captured commands schedule `postCommandRestartDelaySeconds` with `countsTowardStability: false`, recovery restarts use `countsTowardStability: false`, restart tasks guard on `self.shouldKeepRunning`, and self-tests cover restart storms, silent endings, and post-command restart delay. Remaining risk is live macOS Speech/AVAudioEngine availability.

## 2026-06-19 17:43 CST - internal speech leak firewall lane
- Current task stack: top-level Jarvis hardening -> Speech safety -> prevent internal/debug/status text from being spoken.
- Immediate subtask: close a sanitizer gap where single-line dashboard blobs such as `Audit Verification Wake Worker already online... App perms...` survived because they did not start with `Worker` or `Verification`.
- Current evidence: `_strip_spoken_diagnostic_fragments` now drops Audit/Worker/Verification/App perms/Codex Activity/CLI status rows, including copied-message app-permission blobs, and `voice_loop_qa.detect_internal_speech_leaks` flags those normalized phrases if a transcript still contains them.

## 2026-06-19 17:52 CST - native Music bridge reconnect proof lane
- Current task stack: top-level Jarvis hardening -> Music playback reliability -> automatically recover when the preferred Music control bridge is not ready.
- Immediate subtask: prove the preferred native Music bridge opens `Music.app` and retries not only when `/health` is unreachable, but also when the bridge is alive and the control token is initially missing.
- Current evidence: `_music_app_bridge_play` already opens `Music.app`, waits for `/health`, retries `/play`, then checks `/playback-state` before claiming `playing`; tests now cover bridge-down recovery, missing-token recovery, wrong-track rejection, and no fallback to LocalOS/Chrome when the Music bridge fails to confirm playback.

## 2026-06-19 17:56 CST - workboard operator checkpoint lane
- Current task stack: top-level Jarvis hardening -> Report/workboard reliability -> keep goal, subtask, proof, time, and return point visible.
- Immediate subtask: make the overnight workboard show a durable Operator Checkpoint instead of relying on the chat transcript or my memory.
- Current evidence: `render_workboard` now includes Current goal, Current subtask, Proof needed, Time checkpoint, and Return point. The return point names `JARVIS_BUG_BACKLOG.md` plus `.memory.md`; renderer tests pin those labels and text.

## 2026-06-19 18:36 CST - safe Jarvis daily memory surface lane
- Current task stack: top-level Jarvis hardening -> Personal memory -> local daily memory foundation.
- Immediate subtask: move the "Jarvis learns from today" request from vague future work to a safe local JSON surface without scraping raw chats or syncing remotely.
- Current evidence target: `jarvis/tools.py` exposes `JARVIS_DAILY_MEMORY_PATH`, `_load_jarvis_daily_memory`, `_jarvis_daily_memory_snapshot`, and `daily_memory_summary` metadata with raw-chat/read/sync/model safety flags; `scripts/report_refresh.py` seeds the surface; tests cover status, summary, day rollover, and report seeding.
- Return point after interruption: run focused memory/report tests, then full `tests.test_safety`; commit only if green.

## 2026-06-19 18:45 CST - music playback ownership metadata lane
- Current task stack: top-level Jarvis hardening -> Music playback reliability -> make playback ownership visible and prevent silent fallback.
- Immediate subtask: strengthen the copied JSON/API contract so a failed native Music bridge attempt cannot quietly become LocalOS, Chrome, or hidden audio.
- Current evidence: `localos.music_play` now reports `preferred_playback_owner`, `native_music_bridge_enabled`, and `legacy_localos_fallback_allowed`; focused tests assert bridge failure returns `played_by=none`, `control_lane=music_app_bridge`, and no LocalOS/Chrome fallback. `localos.music_stop` reports `emergency_music_brake` and stop surfaces including the native Music bridge and tracked Jarvis afplay.
- Return point after interruption: find the next remaining live-loop bug, ideally a real action proof rather than only metadata.

## 2026-06-19 19:00 CST - Codex chat prepared-prompt lane
- Current task stack: top-level Jarvis hardening -> Codex integration -> plan selected chat plus exact prepared prompt without sending.
- Immediate subtask: make "Open Codex and send a prompt called test in the Default chat" produce an inspectable, confirmation-gated plan with the prepared prompt payload, not just the selected chat.
- Current evidence target: `codex_chat_plan` extracts `prepared_prompt_text`, preserves explicit `in the X chat` phrasing, includes `jarvis_generated_prompt_marker`, and previews the wrapper with both the marker and original request tail. Tests cover Default, STT mishearings of Codex, and explicit Music chat selection.
- Return point after interruption: run full `tests.test_safety`; if green, commit.

## 2026-06-19 19:08 CST - native Music availability lane
- Current task stack: top-level Jarvis hardening -> Music playback reliability -> tool catalog availability.
- Immediate subtask: stop hiding `localos.music_play` just because the LocalOS snapshot is missing when the native Music bridge app is present.
- Current evidence target: `_music_play_tool_available()` returns true for LocalOS snapshot/root or native Music bridge app, and the registry test covers the native-bridge-without-snapshot case.
- Return point after interruption: run full `tests.test_safety`; if green, commit.

## 2026-06-19 19:11 CST - email latest-five scan lane
- Current task stack: top-level Jarvis hardening -> Email reliability -> default scan size.
- Immediate subtask: align ordinary "check my email" reads with Leo's instruction that he rarely has more than 5 new emails and Jarvis should check latest 5, while preserving wider bounded scans for explicit sender/date requests.
- Current evidence target: `EMAIL_DEFAULT_SCAN_MESSAGES` defaults to 5; `outlook_read_only_check` uses it for normal inbox reads and 25 for sender/date filters unless an explicit bounded override is provided. Focused tests cover normal, sender/date, override, and invalid override paths.
- Return point after interruption: run full `tests.test_safety`; if green, commit.

## 2026-06-19 19:17 CST - offline fallback model policy reclassification
- Current task stack: top-level Jarvis hardening -> Model routing safety -> offline fallback policy.
- Immediate subtask: stop treating the offline fallback lane as open when Jarvis already has a proved plan-only guardrail.
- Current evidence target: `models.test_plan` never loads a local model; it prefers the MacBook Air when reachable, asks before local when not, recommends `gemma3n:e4b` as the lightweight offline candidate, treats Qwen 0.6B only as a tiny simple fallback, and blocks heavy local candidates such as `gpt-oss:20b`/DeepSeek-class models. Focused regressions cover remote preference, remote-unavailable ask-before-local, Tailscale-stopped wording, Qwen fallback, and dictated Gemma model-name preservation.
- Return point after interruption: run the focused model-plan tests and full `tests.test_safety`; if green, commit the backlog/memory correction.

## 2026-06-19 19:21 CST - Git worktree hygiene visibility lane
- Current task stack: top-level Jarvis hardening -> Git/release hygiene -> dirty/untracked file visibility.
- Immediate subtask: make Jarvis's own Git diagnostic expose clean/dirty state, not just branch/remote relationship, so Leo can trust scoped commits and dirty-file warnings.
- Current evidence target: `diagnostics.git` adds a read-only `worktree` object with clean, dirty_count, modified_count, untracked_count, bounded status preview, and reply text. Tests cover a clean unrelated-remote case and a dirty repo with one modified plus one untracked file.
- Return point after interruption: run focused Git diagnostic tests and full `tests.test_safety`; if green, commit.

## 2026-06-19 19:29 CST - email ordinal selection across structured routes
- Current task stack: top-level Jarvis hardening -> Email reliability -> second/newest selection correctness.
- Immediate subtask: fix the route where "check my second email" could still summarize the newest message when Apple Mail was unavailable and the flow fell through to Outlook AppleScript or SQLite.
- Current evidence target: Outlook AppleScript now accepts `selection="recent"` and returns enough recent messages for `index:N`; SQLite fallback can also return recent messages instead of unread/newest only. `outlook_read_only_check` applies the same `selection_request` before summarizing Apple Mail, Outlook AppleScript, or SQLite results. Tests prove second-email selection for Apple Mail, Outlook AppleScript, SQLite fallback, planner preview, and streaming status.
- Return point after interruption: run focused email ordinal tests and full `tests.test_safety`; if green, commit.

## 2026-06-19 19:35 CST - natural tool status line guard
- Current task stack: top-level Jarvis hardening -> Speech/status polish -> no "skill" or "identify task" status wording.
- Immediate subtask: prevent model-provided tool-call status text from leaking awkward implementation language into the visible/spoken Jarvis status line.
- Current evidence target: `_parse_fast_chat_tool_request` now passes status text through `_naturalize_fast_chat_tool_status`; bad lines such as "Finding the email skill", "Let me identify the task", or "Choosing the tool call" are replaced with tool-specific natural status lines. Tests cover email and calendar tool requests.
- Return point after interruption: run focused fast-chat status tests and full `tests.test_safety`; if green, commit.

## 2026-06-19 19:40 CST - visible reply internal-output firewall
- Current task stack: top-level Jarvis hardening -> Safety/product quality -> keep tool/model internals out of visible final text.
- Immediate subtask: extend the speech leak firewall to final user-visible result fields so the Jarvis app window does not show `Tool time`, backend/model names, `selected_tool`, or `entities` if a tool/model accidentally returns them in `reply`.
- Current evidence target: `_sanitize_user_visible_text` strips hidden calls, JSON tool fragments, diagnostic rows, and internal sections without forcing English-only text. The server sanitizes final `reply`, `email_summary`, and `spoken_summary` before returning app-visible results or speaking them. Tests cover debug-only replies and mixed useful text plus diagnostics for normal and streaming command paths.
- Return point after interruption: run focused visible/speech sanitizer tests and full `tests.test_safety`; if green, commit.

## 2026-06-19 19:51 CST - visible summary internal-output firewall
- Current task stack: top-level Jarvis hardening -> Safety/product quality -> keep tool/model internals out of visible final text.
- Immediate subtask: extend the same user-visible sanitizer to top-level `summary`, because copied JSON/app status rows can show that text even when `result.reply` is clean.
- Current evidence target: server data sanitization now cleans `summary` before audit and speech attachment, preserves a private pre-sanitization speech-candidate flag, and still returns explicit zero-length speech status when debug-only text is stripped away. Focused tests assert clean summaries for normal and streaming commands plus explicit empty/suppressed speech payloads for diagnostic-only replies.
- Return point after interruption: run full `tests.test_safety`; if green, commit.

## 2026-06-19 19:58 CST - suppressed speech is not active speech
- Current task stack: top-level Jarvis hardening -> Speech/mute safety -> do not treat suppressed/deferred speech payloads as audible playback.
- Immediate subtask: update Swift `notePotentialSpeech` blocked statuses so backend `suppressed_by_request` and `deferred_to_follow_up` payloads do not extend the active speech window or trigger barge-in/helper behavior as if Jarvis were speaking.
- Current evidence target: `JarvisShellModel.swift` blocks those statuses before calling `notePotentialSpeech(text:)`; focused source tests passed and `swift build --package-path swift-shell --product jarvis-menu-bar` succeeded.
- Return point after interruption: run full `tests.test_safety`; if green, commit.

## 2026-06-19 20:05 CST - LocalOS music blocker guidance
- Current task stack: top-level Jarvis hardening -> Music playback reliability -> honest blocker guidance when playback cannot be queued.
- Immediate subtask: make LocalOS music connection failures return structured next-step data, not only prose, for Chrome Automation denial, LocalOS bridge-not-polling, and LocalOS player-unavailable states.
- Current evidence target: `localos_music_play` now adds `permission_issue`, `requires_user_action`, `next_steps`, and `spoken_summary` through `_localos_music_connection_blocker_details`; focused `localos_music_play` tests passed 23/23.
- Return point after interruption: run full `tests.test_safety`; if green, commit.

## 2026-06-19 20:11 CST - permission explanation clarity
- Current task stack: top-level Jarvis hardening -> Permissions/readiness -> explain Accessibility and Notifications without making optional permissions sound blocking.
- Immediate subtask: make native and backend permission surfaces say Accessibility is only for controlled clicking, typing, and app navigation, while normal chat/email/calendar/status remain usable; make backend diagnostics expose Notifications as optional/not requested.
- Current evidence target: focused permission tests passed, and `swift build --package-path swift-shell --product jarvis-menu-bar` succeeded.
- Return point after interruption: run full `tests.test_safety`; if green, commit.

## 2026-06-19 20:18 CST - report proof wording clarity
- Current task completed: user-facing report proof wording now says eight-prompt behavior check instead of live regression matrix, so Leo sees product-language proof while internal artifact paths stay stable.
- Updated tests assert the clearer proof label and reject the old visible label.

## 2026-06-19 20:24 CST - immediate Shut Up interruption
- Current task completed: both the status-helper Shut Up menu item and the main-window mute action now call `stop talking` before setting speech mute, so active Jarvis audio is interrupted immediately instead of only future speech being muted.
- Evidence target: focused Swift source contract test, `swift build --package-path swift-shell --product jarvis-menu-bar`, `swift build --package-path swift-shell --product jarvis-status-helper`, and full `tests.test_safety`.

## 2026-06-19 20:27 CST - workboard behavior-check wording
- Current task completed: refreshed workboard intro now says reusable eight-prompt behavior check instead of reusable behavior matrix, with a test rejecting the old prominent phrase.

## 2026-06-19 20:32 CST - wake acknowledgement wording
- Current task completed: voice-loop tool descriptions and wake-debug next steps now say visual acknowledgement / wake acknowledgement echo instead of implying Jarvis normally speaks its own greeting after Hey Jarvis.
- Evidence target: focused wake-debug/tool-catalog tests plus full `tests.test_safety`.

## 2026-06-19 20:39 CST - routing honesty for direct music
- Current task stack: top-level Jarvis hardening -> Model/tool choice honesty -> expose approved primitive shortcuts instead of hiding them.
- Immediate subtask: ensure executed direct `play ...` music routes carry the same visible `direct_music_play` primitive-exception label as previews, while first-model selected music playback remains labeled `model_tool_call`.
- Current evidence target: focused music-play and keyword-theater tests passed; run full `tests.test_safety`, refresh report/workboard, then commit if green.

## 2026-06-19 20:48 CST - emergency menu before speech
- Current task stack: top-level Jarvis hardening -> Speech/menu-bar safety -> keep Shut Up reachable whenever Jarvis can speak.
- Immediate subtask: make native status-speech and streaming-command paths call a preflight callback that restarts the status helper before possible audio starts, not only after final speech is detected.
- Current evidence target: focused menu-bar/speech-barge tests passed and `swift build --package-path swift-shell --product jarvis-menu-bar` succeeded; run full `tests.test_safety`, refresh report/workboard, then commit if green.

## 2026-06-19 21:00 CST - Music bridge failure specificity
- Current task stack: top-level Jarvis hardening -> Music playback honesty -> do not say or imply success unless Music confirms the requested song is actually playing.
- Immediate subtask: preserve native Music bridge failure details through `localos_music_play`, especially `wrong_track_playing`, and attach structured `permission_issue`, `next_steps`, and `spoken_summary` fields while refusing hidden fallback audio.
- Current evidence target: focused `music_play` tests passed 32/32; run full `tests.test_safety`, refresh report/workboard, then commit if green.

## 2026-06-19 21:08 CST - canonical app bundle guard
- Current task stack: top-level Jarvis hardening -> Build/release hygiene -> prevent duplicate Jarvis-looking apps.
- Immediate subtask: stop runtime status from preferring legacy `Jarvis-Current*.app` bundles and make the real `output/` build refuse non-canonical app names/bundle IDs or numbered replacement bundles unless an explicit experiment override is set.
- Current evidence target: focused morning-status/build-script tests passed and `APP_NAME=Jarvis-Current swift-shell/scripts/build_app_bundle.sh` fails before building; run full `tests.test_safety`, refresh report/workboard, then commit if green.

## 2026-06-19 21:19 CST - canonical app docs cleanup
- Current task stack: top-level Jarvis hardening -> Build/release hygiene -> keep files clear so Leo only sees the real app.
- Immediate subtask: remove stale `Jarvis-Current-17.app` and alternate app-name guidance from user-facing docs/README/current-goal files and add a regression that rejects legacy app instructions.
- Current evidence target: focused docs/build tests passed and `rg` found no legacy bundle guidance in user-facing docs; run full `tests.test_safety`, refresh report/workboard, then commit if green.

## 2026-06-19 21:36 CST - heavy local model double latch
- Current task stack: top-level Jarvis hardening -> Model routing safety -> never let a model comparison accidentally load a huge local model on Leo's 16 GB Mac.
- Immediate subtask completed: `scripts/compare_middle_models.py` now requires both `--allow-local-heavy` and a deliberate `JARVIS_ALLOW_HEAVY_LOCAL_MODELS` unlock value before a heavy local candidate such as `gpt-oss:20b` can run; reports record requested/effective heavy-local state.
- Evidence target: focused model-comparison tests passed, `models.test_plan` exposes the separate heavy-local unlock requirement, and a live comparison invocation with `--allow-local-heavy` but no unlock skipped `gpt-oss:20b` instead of running it.

## 2026-06-19 21:44 CST - duplicate Jarvis menu heads
- Current task stack: top-level Jarvis hardening -> Speech/menu-bar safety -> keep exactly one reachable emergency Jarvis head.
- Immediate subtask completed: the main app now reaps stale duplicate `jarvis-menu-bar` and `jarvis-status-helper` processes from other Jarvis app bundles at launch while leaving the current canonical bundle alone.
- Evidence target: `swift build --package-path swift-shell --product jarvis-menu-bar`, `swift build --package-path swift-shell --product jarvis-status-helper`, `swift run --package-path swift-shell jarvis-menu-bar --self-test`, `swift run --package-path swift-shell jarvis-status-helper --self-test`, and full `tests.test_safety` passed.

## 2026-06-19 21:52 CST - Music selected-but-paused recovery
- Current task stack: top-level Jarvis hardening -> Music playback honesty -> do not claim success until the requested song is actually playing.
- Immediate subtask completed: native Music bridge play now retries with `/resume` when `/play` selected the requested song but `/playback-state` says it is paused, then rechecks playback before returning success.
- Evidence target: focused Music bridge tests passed; run full `tests.test_safety`, then commit if green.

## 2026-06-19 22:04 CST - Hey Jarvis restart-churn guard
- Current task stack: top-level Jarvis hardening -> Wake reliability -> reduce Apple Speech/menu-bar flicker after final/error recognition callbacks.
- Immediate subtask completed: `JarvisWakeListener` now stops the current recognition session before scheduling normal final/error restarts, so old audio taps/tasks do not remain alive through the restart delay.
- Evidence target: focused wake source-contract tests passed, `swift build --package-path swift-shell --product jarvis-menu-bar` passed, and `swift run --package-path swift-shell jarvis-menu-bar --self-test` passed; run full `tests.test_safety`, then commit if green.

## 2026-06-19 22:13 CST - Chrome test-tab cleanup expansion
- Current task stack: top-level Jarvis hardening -> Browser/test hygiene -> do not leave Jarvis-created Chrome tabs around after overnight runs.
- Immediate subtask completed: `scripts/cleanup_chrome_test_tabs.py` now targets Jarvis report/workboard/wake-audition tabs and file-based overnight report tabs, in addition to old LocalOS music-player tabs, while preserving YouTube/newtab/other localhost pages.
- Evidence target: focused cleanup tests passed; run full `tests.test_safety`, then commit if green.

## 2026-06-19 22:23 CST - pre-build speech proof contract
- Current task stack: top-level Jarvis hardening -> Pre-build regression honesty -> make it impossible to confuse quiet speech-payload audits with live spoken playback proof.
- Immediate subtask completed: `scripts/pre_build_gate.py` now writes an explicit speech proof contract into summaries/markdown, labels default full-loop proof as `suppressed_for_probe`, labels opt-in live proof as `live_playback_exercised`, and adds `--require-live-speech` to fail closed if a caller requires live playback but forgets `--exercise-live-speech`.
- Evidence target: focused pre-build gate tests passed; run full `tests.test_safety`, then commit if green.

## 2026-06-19 22:27 CST - hidden afplay proof cleanup
- Current task stack: top-level Jarvis hardening -> Music playback proof -> avoid false hidden-audio alarms in the full-loop report.
- Immediate subtask completed: `scripts/full_loop_regression.py` now treats only processes whose executable is actually `afplay` as hidden `afplay` playback, so a Piper worker command containing `--afplay /usr/bin/afplay` is not misreported as active hidden audio.
- Evidence target: live Music full-loop case passed with the expected Dear Evan Hansen track, verified stopped, and clean `afplay_processes_after: []`; focused afplay command tests passed; run full `tests.test_safety`, then commit if green.

## 2026-06-19 22:32 CST - all target prompts full-loop proof
- Current task stack: top-level Jarvis hardening -> Full-loop target prompt verification -> prove Leo's requested command set through speech-command synthesis, STT, Jarvis routing, action proof, visible/spoken reply audit, and cleanup.
- Immediate subtask completed: `scripts/full_loop_regression.py --case all` passed `8/8` in 130.873s with zero warnings. Cases passed: Music Waving Through a Window, RAM/Activity Monitor, Calendar today, Magic Keyboard yuan conversion, Gemma 3 4B model plan, Codex Default prompt routing, Teams Music assignment honesty, and Ms. Sharpay email month summary.
- Evidence target: report at `runtime/full_loop_regression/20260619-222943/summary.json`; record in backlog/task queue and continue to the next risky bug.

## 2026-06-19 22:36 CST - pre-build gate wrapper proof
- Current task stack: top-level Jarvis hardening -> Pre-build gate verification -> prove the wrapper executes full-loop, cleanup, and report refresh while exposing the new speech proof contract.
- Immediate subtask completed: `scripts/pre_build_gate.py --skip-python-tests` passed `3/3` in 128.093s. The summary records `speech_mode: suppressed_for_probe`, `live_playback_exercised: false`, `physical_speaker_capture: false`, and `physical_microphone_capture: false`.
- Evidence target: report at `runtime/pre_build_gate/20260619-223401/summary.json`; no code changes from this proof-only run.

## 2026-06-19 22:43 CST - speech emergency status guard
- Current task stack: top-level Jarvis hardening -> Speech/menu-bar safety -> surface the dangerous state where Jarvis can speak but no Shut Up control is reachable.
- Immediate subtask completed: `scripts/morning_status.py` now checks both `jarvis-menu-bar` and `jarvis-status-helper`, reads `/api/speech/mute`, and prints `Speech emergency: missing menu helper while speech is unmuted` with an `open_jarvis.sh` action if speech is unmuted/available and no emergency menu process exists.
- Evidence target: focused morning-status tests passed; live status currently reports `Speech emergency: safe (speech muted)`; run full `tests.test_safety`, then commit if green.

## 2026-06-19 22:56 CST - master report current-night highlights
- Current task stack: top-level Jarvis hardening -> Report/proof clarity -> make the single master report sell the newest proof instead of burying it in older build history.
- Immediate subtask completed: `SHIPPED_ITEMS` now opens with five current Jarvis 0.1.468 highlights: all-target-prompt 8/8 proof, precise native Music hidden-audio proof, explicit pre-build speech proof modes, Chrome/Jarvis tab cleanup, and speech-emergency menu status.
- Evidence target: focused render tests passed; run full `tests.test_safety`, refresh report surfaces, then commit if green.

## 2026-06-19 23:06 CST - audio-native model status clarity
- Current task stack: top-level Jarvis hardening -> Model routing clarity -> do not overpromise Gemma/Qwen audio input as a finished Jarvis STT path.
- Immediate subtask completed: `models.test_plan` offline fallback now includes `audio_input_status` with `status: research_only`, `final_stt_path: false`, and concrete requirements before any audio-native model can replace or augment the current STT path.
- Evidence target: focused model-plan tests passed; run full `tests.test_safety`, then commit if green.

## 2026-06-19 23:10 CST - GitHub branch state check
- Current task stack: top-level Jarvis hardening -> Repo/GitHub hygiene -> understand the old GitHub Desktop fetch/push warning without pushing.
- Immediate subtask completed: local branch `codex/jarvis-overnight-20260608` is the full Jarvis repo root and currently reports `ahead 126, behind 0` against `origin/codex/jarvis-overnight-20260608`. `git fetch --dry-run origin codex/jarvis-overnight-20260608` completed without reporting incoming updates.
- Evidence target: current evidence says the old "newer commits on remote" warning is not present for this branch state; do not push while unattended.

## 2026-06-19 23:20 CST - stale Working status row cleanup
- Current task stack: top-level Jarvis hardening -> Task honesty -> show progress while a task is active, then replace it with the real final answer.
- Immediate subtask completed: native Outlook/screen status rows and early streaming status rows now assign the visible status row to `placeholderId`, so the final response replaces that row instead of leaving a permanent "Working" row above the answer.
- Evidence target: focused Swift progress/status tests passed, full `tests.test_safety` passed `931/931`, and `scripts/verify_safe.py` passed `105/105` with report `runtime/verification/verify-safe-20260619-232331.json`.

## 2026-06-19 23:32 CST - status-helper emergency fallback
- Current task stack: top-level Jarvis hardening -> Speech/menu-bar safety -> keep Shut Up reachable whenever Jarvis may speak.
- Immediate subtask completed: the separate `jarvis-status-helper` remains the normal single menu-bar head, but if it is missing or fails to launch, the main app creates a fallback status item with the same emergency controls. When helper launch succeeds, that fallback status item is removed to avoid duplicate Jarvis heads.
- Evidence target: focused status-helper contract test passed, `swift build --package-path swift-shell --product jarvis-menu-bar` passed, `swift run --package-path swift-shell jarvis-menu-bar --self-test` passed, `swift run --package-path swift-shell jarvis-status-helper --self-test` passed, full `tests.test_safety` passed `931/931`, and `scripts/verify_safe.py` passed `105/105` with report `runtime/verification/verify-safe-20260619-233327.json`.

## 2026-06-19 23:43 CST - visible reply link cleanup
- Current task stack: top-level Jarvis hardening -> Speech/visible-output sanitization -> keep app text and Copy Chat JSON free of raw links and backend clutter.
- Immediate subtask completed: `_sanitize_user_visible_text` now strips markdown/raw URLs and email addresses in the same server-visible reply path that already removes internal diagnostics, so the Jarvis window does not show the form URL when a concise label is enough.
- Evidence target: focused visible/speech sanitizer tests passed, full `tests.test_safety` passed `933/933`, and `scripts/verify_safe.py` passed `105/105` with report `runtime/verification/verify-safe-20260619-234237.json`.

## 2026-06-19 23:50 CST - app TTS forced to macOS say
- Current task stack: top-level Jarvis hardening -> Speech/TTS quality -> use the voice path Leo preferred in the live app.
- Immediate subtask completed: `JarvisWorkerSupervisor` now sets `JARVIS_TTS_PROVIDER=macos` unconditionally for app-launched workers, while keeping `JARVIS_TTS_PLAIN_SAY=1`, empty voice, and empty rate. This prevents inherited `piper` environment settings from making the app use the disliked Piper Ryan path again.
- Evidence target: focused worker voice-default tests passed, `swift build --package-path swift-shell --product jarvis-menu-bar` passed, `swift run --package-path swift-shell jarvis-menu-bar --self-test` passed, full `tests.test_safety` passed `933/933`, and `scripts/verify_safe.py` passed `105/105` with report `runtime/verification/verify-safe-20260619-234939.json`.

## 2026-06-19 23:55 CST - canonical app bundle refreshed
- Current task stack: top-level Jarvis hardening -> Build/live app hygiene -> ensure source fixes are present in the app Leo launches.
- Immediate subtask completed: ran `swift-shell/scripts/build_app_bundle.sh` to rebuild the ignored canonical `output/Jarvis.app` bundle at Jarvis `0.1.468` build `468` after the Swift status-helper and worker-supervisor fixes.
- Evidence target: `plutil -lint output/Jarvis.app/Contents/Info.plist` passed, `codesign --verify --deep --strict --verbose=2 output/Jarvis.app` passed, bundled `output/Jarvis.app/Contents/MacOS/jarvis-menu-bar --self-test` passed, bundled `output/Jarvis.app/Contents/MacOS/jarvis-status-helper --self-test` passed, and `output/` remains intentionally ignored by Git.

## 2026-06-20 00:10 CST - live app launch worker startup restored
- Current task stack: top-level Jarvis hardening -> Build/live app hygiene -> ensure the real `output/Jarvis.app` starts its worker and helper on launch.
- Immediate subtask completed: fixed a launch hang in `terminateStaleJarvisProcesses()` where the app could block in the `ps` snapshot before `startWorkerMonitoring()` ran. The snapshot now has a short timeout/terminate path, app launch no longer triggers parallel initial refreshes, and worker monitoring owns the first readiness refresh.
- Evidence target: focused launch/window tests passed, `swift build --package-path swift-shell --product jarvis-menu-bar` passed, live `swift-shell/scripts/build_and_launch_app.sh` succeeded, process proof showed one `jarvis-menu-bar`, one `jarvis-status-helper`, and one bundled worker, full `tests.test_safety` passed `934/934`, and `scripts/verify_safe.py` passed `105/105` with report `runtime/verification/verify-safe-20260620-001017.json`.

## 2026-06-20 00:17 CST - targeted stale Jarvis cleanup
- Current task stack: top-level Jarvis hardening -> App process hygiene -> prevent duplicate Jarvis app/helper processes without scanning the entire process table.
- Immediate subtask completed: `terminateStaleJarvisProcesses()` now uses targeted `/usr/bin/pgrep -fl 'jarvis-menu-bar|jarvis-status-helper'`, protects only the current app PID/current helper parent, and no longer skips stale processes merely because they came from the same `output/Jarvis.app` bundle path.
- Evidence target: focused launch/window tests passed, `swift build --package-path swift-shell --product jarvis-menu-bar` passed, live `swift-shell/scripts/build_and_launch_app.sh` succeeded with one app/helper/worker, full `tests.test_safety` passed `934/934`, and `scripts/verify_safe.py` passed `105/105` with report `runtime/verification/verify-safe-20260620-001717.json`.

## 2026-06-20 02:30 CST - model-first routing honesty expansion
- Current task stack: top-level Jarvis hardening -> Model/tool choice honesty -> stop hidden keyword-like execution for common high-level skills.
- Immediate subtask: make live RAM, Calendar, and public price-conversion commands ask the first model to choose the tool before execution, while keeping previews deterministic and preserving explicit fallback routes for tests or disabled-router runs.
- Current evidence target: focused planner tests passed for Magic Keyboard price conversion, Activity Monitor/RAM, Calendar schedule, email no-route concept talk, and device model-first routing. Next: run full tests, rebuild as Jarvis 0.1.473, verify, refresh gate, then commit if green.

## 2026-06-20 02:50 CST - Jarvis 0.1.473 model-first routing proof
- Current task completed: Jarvis 0.1.473 build 473 is live with first-model execution routing for RAM, Calendar, and public price conversion. Deterministic fallbacks remain explicit and labeled, not hidden as intelligence.
- Bug found and fixed during gate: the full-loop Magic Keyboard case exposed STT hearing "yuan" as "you on", which made the model/tool path default to USD. The planner now recovers that dictation variant as CNY and lets the original prompt override a bad model currency entity.
- Proof: full `tests.test_safety` passed 946/946; `scripts/verify_safe.py` passed 105/105 at `runtime/verification/verify-safe-20260620-024010.json`; focused Magic Keyboard full-loop proof passed at `runtime/full_loop_regression/20260620-024641/summary.json`; official pre-build gate passed 5/5 at `runtime/pre_build_gate/20260620-024701/summary.json`; `scripts/morning_status.py` shows live Jarvis 0.1.473 build 473, latest verification, latest gate, and speech emergency safe.

## 2026-06-20 02:56 CST - full-loop route-source proof
- Current task completed: full-loop regression reports now preserve `routing.source` from the actual Jarvis voice-loop command response and fail RAM, Calendar, and Magic Keyboard cases unless the live route source is `model_tool_call`.
- Proof: focused route-source unit tests passed, live RAM/Calendar/Magic full-loop cases passed at `runtime/full_loop_regression/20260620-025405/summary.json`, `runtime/full_loop_regression/20260620-025414/summary.json`, and `runtime/full_loop_regression/20260620-025420/summary.json` with `route_source: model_tool_call`; full `tests.test_safety` passed 949/949.
- Follow-up proof: official post-route-source pre-build gate passed 5/5 at `runtime/pre_build_gate/20260620-025646/summary.json`.

## 2026-06-20 03:08 CST - wake stop/restart and model-punt guardrails
- Current task stack: top-level Jarvis hardening -> Hey Jarvis reliability -> prove Stop wins over delayed restart tasks, plus fix a full-suite RAM route regression exposed during verification.
- Immediate subtask completed: `JarvisWakeListener.testStopCancelsPendingRestart()` now schedules a pending restart, calls Stop Hey Jarvis, waits past the restart delay, and verifies the listener remains `Off` with `Wake listener off`. This protects against regressions where a delayed Apple Speech restart revives after Leo stops Hey Jarvis.
- Regression fixed during proof: if the first model completes as `conversation.fast_local` for unmistakable read-only RAM, Calendar, or public price-conversion requests, the planner now falls back to the safe tool path and records `model_route_fallback` instead of silently chatting.
- Proof: focused Python planner/wake tests passed; `swift build --package-path swift-shell --product jarvis-menu-bar` passed; `swift run --package-path swift-shell jarvis-menu-bar --self-test` passed; full `python3 -m unittest tests.test_safety` passed 950/950.
- Follow-up proof: after commit `3a711b4`, the current-code release gate passed 5/5 at `runtime/pre_build_gate/20260620-030913/summary.json`.

## 2026-06-20 03:14 CST - model-punt fallback coverage expansion
- Current task stack: top-level Jarvis hardening -> Model/tool choice honesty -> ensure all current first-model read-only skills are protected when the model punts to chat.
- Immediate subtask completed: added focused punt tests for Calendar schedule and Magic Keyboard yuan price conversion alongside RAM. All three verify that `conversation.fast_local` completions fall back to the intended safe tool with `model_route_fallback` recorded.
- Proof: focused planner punt tests passed; full `python3 -m unittest tests.test_safety` passed 952/952.

## 2026-06-20 03:21 CST - Jarvis 0.1.474 live bundle sync
- Current task stack: top-level Jarvis hardening -> Build/live app hygiene -> ensure Leo's normal Jarvis app runs the latest planner guardrails, not stale bundled resources.
- Immediate subtask completed: bumped the canonical app bundle to Jarvis 0.1.474 build 474, rebuilt `output/Jarvis.app`, launched it, and confirmed health reports `worker_launch_matches_bundle: true`, version `0.1.474`, build `474`, and bundled worker source under `output/Jarvis.app/Contents/Resources/JarvisWorker`.
- Proof: focused version/report tests passed; bundle plist lint passed; codesign verify passed; bundled `jarvis-menu-bar --self-test` and `jarvis-status-helper --self-test` passed; full `python3 -m unittest tests.test_safety` passed 952/952; `scripts/verify_safe.py` passed 105/105 at `runtime/verification/verify-safe-20260620-031851.json`; `scripts/pre_build_gate.py --base-url http://127.0.0.1:8765` passed 5/5 at `runtime/pre_build_gate/20260620-031902/summary.json`.

## 2026-06-20 03:29 CST - Jarvis 0.1.475 speech mute clarity
- Current task stack: top-level Jarvis hardening -> Speech/mute UX -> make muted state obvious in the app so Leo does not wonder why Jarvis is not talking.
- Immediate subtask completed: changed the muted status chip from vague `Muted` to `Speech Muted`, bumped and launched Jarvis 0.1.475 build 475.
- Proof: focused Swift/status/report tests passed; `swift build --package-path swift-shell --product jarvis-menu-bar` passed; `swift run --package-path swift-shell jarvis-menu-bar --self-test` passed; live health reports Jarvis 0.1.475 build 475 with bundled worker; full `python3 -m unittest tests.test_safety` passed 952/952; `scripts/verify_safe.py` passed 105/105 at `runtime/verification/verify-safe-20260620-032750.json`; release gate passed 5/5 at `runtime/pre_build_gate/20260620-032802/summary.json`.

## 2026-06-20 03:37 CST - speech-audit proof honesty
- Current task stack: top-level Jarvis hardening -> Voice-loop QA -> keep proof labels honest about generated audio versus physical speaker/microphone capture.
- Immediate subtask completed: speech-audit-only reports now include `measurement_contract`, and console/markdown output shows `physical_speaker_capture: false` and `physical_microphone_capture: false`. This prevents quiet payload/STT audits from being mistaken for room-audio proof.
- Proof: live `scripts/voice_loop_qa.py --command status --speech-audit-only --no-permission-prompts --no-refresh-report` printed `Physical capture: speaker=False | microphone=False`; focused voice-loop tests passed; full `python3 -m unittest tests.test_safety` passed 952/952.

## 2026-06-20 03:38 CST - live 0.1.475 latency refresh
- Current task stack: top-level Jarvis hardening -> Speed proof freshness -> keep the status surface tied to the latest live build.
- Immediate subtask completed: refreshed `scripts/smoke_fast_latency.py --base-url http://127.0.0.1:8765` after the 0.1.475 launch.
- Proof: latency smoke passed 3/3 at `runtime/model_benchmarks/localhost-fast-latency-20260620-033720.md`; first-visible times were 0.005s for hello, 1.29s for a joke, and 0.621s for five bullets.

## 2026-06-20 03:45 CST - physical speech-loop proof fails closed
- Current task stack: top-level Jarvis hardening -> Voice-loop QA -> prevent test reports from claiming room-audio proof before physical speaker/microphone capture exists.
- Immediate subtask completed: added `--require-physical-capture` to `scripts/voice_loop_qa.py` and `scripts/pre_build_gate.py`. Both refuse to pass when that strict proof is requested; the pre-build gate writes an explicit failed `physical_capture_requirement` row and records `require_physical_capture: true`.
- Proof: focused negative CLI checks passed, `scripts/pre_build_gate.py --require-physical-capture --skip-python-tests --skip-full-loop --skip-cleanup` failed intentionally with a physical-capture row, `scripts/voice_loop_qa.py --require-physical-capture` failed intentionally with argparse exit 2, and full `python3 -m unittest tests.test_safety` passed 954/954.

## 2026-06-20 03:51 CST - native Music wrong-track emergency stop
- Current task stack: top-level Jarvis hardening -> Music playback ownership -> stop mystery audio when the native Music bridge plays or reports the wrong track.
- Immediate subtask completed: `_music_app_bridge_play()` now sends `POST /stop` when playback is active but the current track does not match the requested track. The failure result records `wrong_track_stop`, and model-facing music catalogs now say native Music bridge first and never hidden playback.
- Proof: focused music tests passed, including the new `/stop` assertion for wrong-track playback; full `python3 -m unittest tests.test_safety` passed 954/954.

## 2026-06-20 03:57 CST - Jarvis 0.1.476 live bundle sync
- Current task stack: top-level Jarvis hardening -> Build/live app hygiene -> make sure Leo's normal app contains the committed Music wrong-track stop guard.
- Immediate subtask completed: bumped the canonical app bundle defaults to Jarvis 0.1.476 build 476, rebuilt `output/Jarvis.app`, launched it, and confirmed health reports `worker_launch_matches_bundle: true`, version `0.1.476`, build `476`, and bundled worker source under `output/Jarvis.app/Contents/Resources/JarvisWorker`.
- Proof: full `python3 -m unittest tests.test_safety` passed 954/954; `scripts/verify_safe.py` passed 105/105 at `runtime/verification/verify-safe-20260620-035521.json`; release gate passed 5/5 at `runtime/pre_build_gate/20260620-035326/summary.json`.

## 2026-06-20 04:07 CST - Jarvis 0.1.477 Music status wording
- Current task stack: top-level Jarvis hardening -> Music UX honesty -> make play-music working lines match the current native Music bridge path.
- Immediate subtask completed: changed current play-music status text, tool-call examples, and streaming parser tests from old LocalOS wording to `Starting that in Music now.`, then rebuilt and launched Jarvis 0.1.477 build 477.
- Proof: full `python3 -m unittest tests.test_safety` passed 954/954; `scripts/verify_safe.py` passed 105/105 at `runtime/verification/verify-safe-20260620-040323.json`; a transient combined-gate email warning was checked by rerunning `scripts/full_loop_regression.py --case email`, which passed at `runtime/full_loop_regression/20260620-040445/summary.json`; final release gate passed 5/5 at `runtime/pre_build_gate/20260620-040517/summary.json`.

## 2026-06-20 04:12 CST - Teams proof duplicate-test recovery
- Current task stack: top-level Jarvis hardening -> Teams/browser proof honesty -> ensure generic visible Teams/Chrome screens cannot be counted as useful Music-assignment inspection.
- Immediate subtask completed: recovered a silently overwritten test by splitting duplicate method names into two distinct checks: browser-page usefulness rejects a generic `screen.visible_text` result, and visible-screen usefulness rejects generic Chrome/Teams text that does not identify the Music assignment.
- Proof: focused Teams usefulness tests passed; full `python3 -m unittest tests.test_safety` passed 955/955.

## 2026-06-20 04:15 CST - duplicate test-name guard
- Current task stack: top-level Jarvis hardening -> Test-suite integrity -> make sure future regression tests cannot silently overwrite each other in a class.
- Immediate subtask completed: added `test_no_duplicate_test_method_names`, which parses `tests/test_safety.py` with `ast` and fails if any test class defines the same `test_*` method name twice.
- Proof: focused duplicate-name guard passed; full `python3 -m unittest tests.test_safety` passed 956/956.

## 2026-06-20 04:20 CST - speech payload reason contract
- Current task stack: top-level Jarvis hardening -> Voice-loop QA -> make spoken payload metadata explicit enough to debug status speech versus final-answer speech.
- Immediate subtask completed: `VerifySafeScriptTests.test_voice_loop_qa_extracts_spoken_payloads_from_stream_events` now asserts extracted speech payload `reason` values are `status` then `final`, matching the existing `source` contract.
- Proof: focused voice-loop extraction test passed; full `python3 -m unittest tests.test_safety` passed 956/956.

## 2026-06-20 04:24 CST - Chinese text does not truncate speech
- Current task stack: top-level Jarvis hardening -> TTS sanitizer -> keep English speech alive after unknown Chinese characters in model/email/calendar text.
- Immediate subtask completed: added a regression proving `_sanitize_spoken_text()` strips unknown Chinese text while preserving the following English sentence, so macOS `say` receives ASCII speech instead of stopping early.
- Proof: focused Chinese-speech sanitizer tests passed; full `python3 -m unittest tests.test_safety` passed 957/957.

## 2026-06-20 04:30 CST - Music media-surface cleanup proof
- Current task stack: top-level Jarvis hardening -> Music playback ownership -> detect mystery app/browser audio after Jarvis starts music.
- Immediate subtask completed: the live Music full-loop case now snapshots playback surfaces before and after cleanup without reading page text or pausing audio, and fails if a new surface such as Google Chrome, Music, or QuickTime remains after cleanup.
- Proof: live `scripts/full_loop_regression.py --case music --base-url http://127.0.0.1:8765` passed at `runtime/full_loop_regression/20260620-042907/summary.json` with playback at about 4.0s, `new_media_surfaces_after: []`, `new_afplay_processes_after: []`, and verified stopped; full `python3 -m unittest tests.test_safety` passed 959/959.

## 2026-06-20 04:35 CST - current official gate refresh
- Current task stack: top-level Jarvis hardening -> Proof freshness -> keep morning status pointed at proof generated after the latest tracked changes.
- Immediate subtask completed: reran the official pre-build gate and safe verifier after the Music media-surface patch.
- Proof: `scripts/pre_build_gate.py --base-url http://127.0.0.1:8765` passed 5/5 at `runtime/pre_build_gate/20260620-043121/summary.json`; `scripts/verify_safe.py` passed 105/105 at `runtime/verification/verify-safe-20260620-043541.json`.

## 2026-06-20 04:38 CST - explicit Chrome cleanup dry run
- Current task stack: top-level Jarvis hardening -> Overnight cleanup hygiene -> make cleanup tools ergonomic and non-destructive by default.
- Immediate subtask completed: `scripts/cleanup_chrome_test_tabs.py` now accepts `--dry-run` as an explicit alias for the default preview mode, so automation or manual checks can ask for dry-run without triggering an argparse failure.
- Proof: focused cleanup CLI tests passed; live `python3 scripts/cleanup_chrome_test_tabs.py --dry-run --json` returned `ok: true`, `executed: false`, and `target_count: 0`; full `python3 -m unittest tests.test_safety` passed 960/960.

## 2026-06-20 04:41 CST - post-cleanup official gate refresh
- Current task stack: top-level Jarvis hardening -> Proof freshness -> verify the official gate after cleanup tooling changed.
- Immediate subtask completed: reran the official pre-build gate after the explicit cleanup dry-run CLI patch.
- Proof: `scripts/pre_build_gate.py --base-url http://127.0.0.1:8765` passed 5/5 at `runtime/pre_build_gate/20260620-043937/summary.json`.

## 2026-06-20 04:45 CST - current fast-latency refresh
- Current task stack: top-level Jarvis hardening -> Speed proof freshness -> keep status/report surfaces tied to current live Jarvis speed.
- Immediate subtask completed: refreshed the localhost fast-latency smoke against live Jarvis 0.1.477.
- Proof: `scripts/smoke_fast_latency.py --base-url http://127.0.0.1:8765` passed 3/3 at `runtime/model_benchmarks/localhost-fast-latency-20260620-044503.md`; first visible was 0.005s for hello, 0.771s for a joke, and 0.713s for five bullets.

## 2026-06-20 04:50 CST - physical audio proof status is explicit
- Current task stack: top-level Jarvis hardening -> Proof honesty -> keep generated-audio QA clearly separate from physical speaker/microphone loopback proof.
- Immediate subtask completed: `scripts/morning_status.py` now prints `Physical audio loop: not implemented; strict speaker/microphone capture proof fails closed by design`.
- Proof: live morning status printed the new line; focused morning-status test passed; full `python3 -m unittest tests.test_safety` passed 961/961.

## 2026-06-20 04:52 CST - post-physical-status official gate refresh
- Current task stack: top-level Jarvis hardening -> Proof freshness -> verify the official gate after morning-status proof wording changed.
- Immediate subtask completed: reran the official pre-build gate after the physical audio status line landed.
- Proof: `scripts/pre_build_gate.py --base-url http://127.0.0.1:8765` passed 5/5 at `runtime/pre_build_gate/20260620-045053/summary.json`.

## 2026-06-20 04:59 CST - Codex alias route proof
- Current task stack: top-level Jarvis hardening -> Codex integration -> make the `Open Codex and send prompt test in Default` target robust to STT mishearing Codex as Cortex or Kodak.
- Immediate subtask completed: the Codex full-loop case now feeds `codex_chat_plan()` the actual voice-routed command instead of a hard-coded proof string, and `verify_codex_default_plan()` fails unless the voice route used `codex.chat_plan` and preserved a Codex/Cortex/Kodak alias.
- Proof: focused Codex alias tests passed; live `scripts/full_loop_regression.py --case codex --base-url http://127.0.0.1:8765` passed at `runtime/full_loop_regression/20260620-045707/summary.json` with `alias_detected: true`, transcript `Open Cortex...`, `voice_tool: codex.chat_plan`, and `sent_prompt_to_codex: false`; full `python3 -m unittest tests.test_safety` passed 964/964.
